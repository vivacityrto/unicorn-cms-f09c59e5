## Implementation plan: `ai_client_query_usage` (per-user daily query cap)\n\nConfirmed design: per-user-global cap, UTC daily reset, self-read RLS, `ON DELETE CASCADE` on both FKs, staff bypass via `is_vivacity_internal_safe(auth.uid())`.\n\n---\n\n### 1. Migration DDL (single transactional migration)\n\nOrder matters — table first, then constraints, then RLS three-step (enable → policy → policy), then trigger. Everything below runs in one migration file inside an implicit transaction.\n\n```sql\n-- 1.1  Create the table with inline FKs and UNIQUE constraint\nCREATE TABLE public.ai_client_query_usage (\n  id           uuid        PRIMARY KEY DEFAULT gen_random_uuid(),\n  user_id      uuid        NOT NULL\n                 REFERENCES public.users(user_uuid)  ON DELETE CASCADE,\n  tenant_id    bigint      NOT NULL\n                 REFERENCES public.tenants(id)       ON DELETE CASCADE,\n  query_date   date        NOT NULL DEFAULT CURRENT_DATE,\n  query_count  integer     NOT NULL DEFAULT 0,\n  created_at   timestamptz NOT NULL DEFAULT now(),\n  updated_at   timestamptz NOT NULL DEFAULT now(),\n  CONSTRAINT ai_client_query_usage_user_date_unique\n    UNIQUE (user_id, query_date)\n);\n\n-- 1.2  Enable RLS (deny-by-default until policies are added)\nALTER TABLE public.ai_client_query_usage ENABLE ROW LEVEL SECURITY;\n\n-- 1.3  Self-read policy\nCREATE POLICY \