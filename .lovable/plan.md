Step 1: Edit `src/pages/SuggestionRegister.tsx`
- Replace the `isVivacityStaff` constant (lines 42-45) to include `'CSC'`, `'Integrator'`, `'BGT'`, and `'CET'` alongside the existing `'Super Admin'`, `'Team Leader'`, and `'Team Member'` checks.

No other files or logic will be changed.