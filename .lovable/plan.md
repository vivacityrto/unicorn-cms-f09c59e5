Replace `handleNotifClick` in `src/pages/MyNotificationsPage.tsx` (lines 46–54) with a version that adds a `message` type branch resolving the conversation id from `notification.source_id` or the `?conversation=` query param in `notification.link`, then navigates to `/communications?thread={convId}` (or `/communications` if none).