# Codebase Optimization Plan — Progress Log

> Chronological execution history for [the Codebase Optimization and KB Renewal Plan](codebase-optimization-plan-2026-08-28.md). Extracted from the plan itself on 2026-09-08 to keep the plan current-state-only.

## Current reconciliation (2026-09-08)

The current execution/status record is [Phase 2.6 Stabilization and Claude Code
Execution Plan](codebase-optimization/phase-2-6-stabilization/phase-2-6-stabilization-plan.md).
It supersedes the planning-only wording and stale snapshots below without
removing their historical evidence. Current code is `origin/main@afafe1f5a`
(the code state is unchanged from `e1a0013ee` by the intervening docs-only
commit). Phase 2.5 closed at PR #953; Phase 2.6 retirements continued through
the P5-A/P6-B/P4-B/P4-C cohorts; Phase 3 remains unstarted.

Current measurements are 1,702 tracked product files; 480,206 physical lines
(407,168 excluding generated types; 395,650 excluding generated types and
tests); 115 files over 600 lines; 32 over 1,000; six wrapper files (105 lines);
240 routes with zero duplicate paths; and typecheck at zero errors. The
committed lint baseline is 2 errors and 40 rule-attributed warnings (full lint
prints 2 errors and 44 warnings, including four rule-less unused-disable
notices). The remaining errors are the reviewed `InviteUserDialog.tsx`
cross-schema exception and deferred `generate-meeting-recurrence` typing.

The frequently cited **9,209 retired lines** is an approximate Phase 2.6
aggregate. Direct per-PR shortstat summation is approximately 8,867, with a
roughly 342-line difference caused by rounded/cohort accounting. Use the dead-
code investigation's per-cohort evidence and the current metrics above for
exact comparisons; do not treat 9,209 as an exact current LOC total. P1-C live
proof is complete on the dedicated `unicorn-qa` project (run `34179875080`),
and the administrative secret-move/repeat-run tail was intentionally waived.

## Phase 2.5 Edge checkpoints (2026-09-06)

> **Phase 2.5 send-email Edge checkpoint (2026-09-06, PR #888 merged):** Fresh source/config reachability confirmed `send-email` preserves two caller modes: Supabase auth-hook secret and authenticated Admin/Super Admin direct mode. Ten explicit-any JSON/error boundaries were replaced with unknown-safe records; redirect sanitization, Mailgun delivery, auth-link generation, email-log writes, authorization, and response contracts are unchanged. Lint ratchet passed (10→0), frontend (298/15 skipped), Edge (260), build passed; typecheck retained only the five documented baseline errors. Unauthenticated Playwright smoke passed 12/12 desktop/mobile login, reset, activation, protected-route, and 404 flows with no writes; outbound email and auth-link paths were not invoked. Supabase production metadata/source parity remains a follow-up deployment check. Regenerated baseline after merge: 539 total errors (532 `no-explicit-any`), 39 warnings across 130 files. No new L10 bug.

> **Phase 2.5 executive-summary Edge checkpoint (2026-09-06, PR #886 merged):** Fresh source/config reachability confirmed `draft-executive-summary` is configured and called by `useAuditReport` on the protected audit report surface. Seventeen explicit-any database/query/model/error boundaries were replaced with bounded local types and unknown-safe handling; caller authentication/RLS, retrieval, AI gateway, cooldown, audit-log insert, and response contracts are unchanged. Lint ratchet passed (17→0), frontend (298/15 skipped), Edge (260), build passed; typecheck retained only the five documented baseline errors. Authenticated SuperAdmin Playwright loaded and refreshed `/tenant/7547` read-only (1/1, zero page/console errors, no writes); draft generation was not invoked. Supabase reports ACTIVE v322, but the deployed source remains pre-typing; production log querying remains subject to the known schema-field error, so live source parity is not claimed. Regenerated baseline after merge: 549 total errors (542 `no-explicit-any`), 39 warnings across 131 files. No new L10 bug; deployment/source parity remains a follow-up.

> **Phase 2.5 notification-queue Edge checkpoint (2026-09-06, PR #884 merged):** Fresh source/config reachability confirmed `process-notification-queue` is configured as a cron-only worker with no frontend caller. Eighteen explicit-any queue, quiet-hours, notification-data, and error boundaries were replaced with bounded local types and unknown-safe messages; cron authorization, queue status transitions, email/in-app dispatch, and all write contracts are unchanged. Lint ratchet passed (18→0), frontend (298/15 skipped), Edge (260), build passed; typecheck retained only the five documented baseline errors. No Playwright route exists for this cron-only/write-capable function, so it was not invoked. Supabase confirms the function ACTIVE (v147), but the deployed source is still the pre-typing revision; production log querying reproduced the known schema-field error, so live invocation parity is not claimed. Regenerated baseline after merge: 566 total errors (559 `no-explicit-any`), 39 warnings across 132 files. No new L10 bug; deployment/source parity remains a follow-up.

## 22. Execution progress and LOC tracking

Updated after every merged PR, in the same style as `docs/dead-code-cleanup-plan-2026-08-27.md`'s "Lines of code progression" — this section is the running record of what actually shipped against this plan, not a restatement of the candidate register above.

### Measurement method (read before trusting a number below)

Numbers below come from `scripts/architecture-metrics.mjs`, run against a **disposable detached worktree** at the exact commit being measured (`git worktree add --detach <path> <sha>`, then `node scripts/architecture-metrics.mjs --json` invoked with a path *relative to that worktree*, e.g. after `cd`-ing into it). Two gotchas found while building this tracker, both now load-bearing for anyone re-running these numbers later:

1. **The script resolves its own root from `import.meta.url`, not `process.cwd()`.** Running it via an absolute path into a different checkout (e.g. invoking the main checkout's copy of the script while `cd`'d into a worktree) silently measures the *main checkout*, not the worktree — producing a false "0 delta" that looks like a clean measurement but isn't. Always invoke the script using a path that resolves inside the worktree you actually want measured (a bare relative `scripts/architecture-metrics.mjs` after `cd` works; an absolute path into another checkout does not).
2. **The script walks the filesystem (`readdirSync`), not `git ls-files`.** Any local, gitignored, untracked directory sitting in whichever checkout you measure gets counted as if it were repository code. The shared main working directory used for day-to-day sessions has one such directory, `supabase/functions/mcp/` (local MCP tooling scaffold, `!!` in `git status --ignored`, not part of the repo) — it inflates `edgeFunctions.files`/`edgeFunctions.lines` by +1 file / +9 lines versus a true clean measurement. A disposable detached worktree never has this problem, because `git worktree add` only ever materializes tracked content. **Prefer a fresh detached worktree over the shared main checkout for any number that will be written down here.**

Also note: the shared main working directory's local `main` branch had drifted 5 merged PRs behind `origin/main` for the entire Phase 0/1 window (all work happened in feature worktrees branched directly from `origin/main`, so nothing ever fast-forwarded local `main`). Harmless for the actual PRs — each was branched fresh from `origin/main` — but it means `git log -1` in the shared checkout is not a reliable way to check "what's currently on main"; use `git log --oneline -1 origin/main` after a `git fetch`, and pull local `main` (`git pull --ff-only origin main`, safe when the tree is clean) before trusting an in-place measurement.

### LOC and structure progression

| Checkpoint | SHA | Tracked product files | Product LOC (excl. generated/tests) | Files >600 lines | Files >1000 lines | Wrapper files |
|---|---|---:|---:|---:|---:|---:|
| Phase 2 start (= Phase 1 exit) | `ea3ffbfa` | 1,816 | 416,318 | 120 | 36 | 71 |
| After P1.1 (dedupe `/support-tickets`, PR [#482](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/482)) | `94ec75c6` | 1,816 | 416,316 (−2) | 120 | 36 | 71 |

The plan's original 28 Aug baseline (`e91d013d`) measured 413,945 product LOC / 120 files >600 / 36 files >1000 / 71 wrappers. The 416,318 reading above is higher not because Phase 0/1 added product code (both phases were tooling + docs, plus one comment-only line fix) but because ordinary feature work landed on `main` between 28 Aug and 1 Sept alongside this program — expected, and the reason every phase re-measures from the current tip rather than trusting the plan's original numbers. Files >600/1000 and wrapper counts are unchanged from the original baseline, which is coincidence, not evidence nothing moved — P1.1 is route dedup, not a size-reduction slice; the later route-extraction/nested-layout slices are where the wrapper/file counts are expected to move.

### Phase 0 — baseline and verification (complete, 2026-09-01)

8 sub-items across independent PRs (#465–#472). Not re-derived here since none of it touched `src/`/`supabase/functions/**` product code (build/test/lint/metrics/route-manifest/Playwright tooling only — 0 product LOC impact by design).

### Phase 1 — KB truth restoration (complete, 2026-09-01)

5 PRs (#476, #478, #479, #480, #481), `docs/kb/**` and `docs/audit-log/INDEX.md` only, plus one corrected code comment (`ClientLayout.tsx`) and one corrected RLS template inside a doc. 0 product LOC impact by design — this phase fixed documentation truth, not code structure.

### Phase 2 — route/composition simplification (complete, with one known gap — see below)

| ID | Candidate | Status | PR | LOC delta |
|---|---|---|---|---:|
| P1.1 | Remove duplicate `/support-tickets` registration | ✅ Done | [#482](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/482) | −2 |
| P0.6 (already shipped in Phase 0) | Module-aware route manifest/check | ✅ Confirmed still passing after P1.1 (`npm run routes` shows exactly one `/support-tickets` row) | #472 | — |
| P1.5 | Remove stale `navigationConfig.ts` exports after caller/persona check | ✅ Done | [#484](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/484) | −166 |
| — | Characterize legacy `/suggestions → /support-tickets` redirects before extraction | ✅ Done | [#485](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/485) | (tests only) |
| — | Extract one route family out of `App.tsx` (Support Tickets, 11 routes, 3 previously non-adjacent locations) | ✅ Done | [#486](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/486) | — |
| — | Convert Academy Wrapper routes to a nested layout route (11 files) | ✅ Done | [#487](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/487) | −170 |
| — | Convert Client Portal Wrapper routes to a nested layout route (20 files) | ✅ Done | [#488](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/488) | — |
| — | Convert DashboardLayout Wrapper routes to a nested layout route (27 files, 32 routes) | ✅ Done | [#489](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/489) | — |
| — | Fix guard-ordering regression in #489 (`/admin/stages*` mounted the shell before its SuperAdmin child guard ran) | ✅ Done | [#490](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/490) | — |
| — | Consolidate route metadata (title/guard-tier/layout) | ⏳ Not started — flagged as the highest-risk remaining item; no concrete well-scoped candidate yet | — | — |
| — | DashboardLayout direct-composition migration (child plan, 19 core PRs across "orders" 0–18) | ✅ Core sequence done, 2 in-scope files missed — see gap note below | [#492](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/492)–[#503](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/503), [#505](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/505), [#508](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/508), [#510](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/510), [#514](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/514), [#516](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/516)–[#518](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/518) | `grep -rl "<DashboardLayout" src/pages` dropped from 122 files to 9 at the `main` tip this row was written against (`ab356c7c`). See gap note. |

Phase 2 exit gate (from §8): route count and guards unchanged except intentional cleanup; wrapper LOC reduced; route inventory check passes. The mechanical-wrapper slice is complete (Academy/Client/Dashboard, PRs #487–#489, plus the #490 guard-ordering correction) — 58 wrapper files retired across the three portals. LOC deltas above are per-PR, not re-derived from a fresh whole-repo measurement; see the note below on why a full re-measurement is deferred.

**Direct-composition migration, verified 2026-09-03 and reconciled 2026-09-04:** the child plan [`dashboard-direct-layout-migration-plan-2026-09-01.md`](dashboard-direct-layout-migration-plan-2026-09-01.md) proposed 19 core slices ("orders" 0–18) plus 2 explicit design-decision exceptions and a 5-file dead-code cleanup. Mapping each merged PR's file list to the plan's §6 slice table confirms all 19 core slices landed (PR #492 = order 0 through PR #518 = order 18), and the two decision items — `AuditTemplateBuilder.tsx` and `SettingsWrapper.tsx`/`/client/settings` — remain correctly untouched pending their own design decision. The five dead/unrouted candidates named in §5.10 were subsequently retired through verified Phase 2.6 cohorts: `eos/EosIssues.tsx` in #574, `NewSuggestionForm.tsx`/`SuggestionRegister.tsx` in #577, and the legacy Audit shells in #588 (following UUID route convergence in #586). Their compatibility routes/redirects and active replacements remain in place.

**Real gap found by this verification, not previously documented (fixed same day, [#525](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/525)):** `src/pages/ProcessDetail.tsx` and `src/pages/ProcessForm.tsx` were part of the plan's own §5.3 inventory (26 "EOS and processes" files, routed at `/processes/:id`, `/processes/:id/edit`, `/processes/new` in `App.tsx` — still live, not dead code) but landed in neither PR #502 (EOS overview/read-only, 12 of its planned 14 files) nor PR #503 (EOS interactive/detail, exactly its planned 12 files, all `Eos*`). Both files composed `<DashboardLayout>` directly across their multiple render branches, so those three routes fully remounted the staff shell on every navigation — the exact defect this program exists to fix. This was never caught because the child plan's own §12 "final documentation PR" step (regenerate the baseline, report remaining exceptions) was never done. Filed and fixed as its own small follow-up PR (2 files, same mechanical pattern as #502/#503) rather than inline in this docs correction, per the plan's own change-size discipline (§14). The migration is now genuinely 115/115 planned files.

A full LOC re-measurement (per the methodology above — disposable detached worktree, `scripts/architecture-metrics.mjs`) has not been re-run since the P1.1 checkpoint; the mechanical-wrapper PRs' own descriptions record their individual file/line deltas, which is treated as sufficient until the next natural checkpoint (either Phase 2's close, or a specific request for an updated whole-repo table) rather than re-running the full measurement after every single merge.

### Phase 2.5 — lint-debt retirement (historical execution record)

> **Superseded status:** the execution record below describes the opening and
> mid-phase sequence. Phase 2.5 closed at PR #953; current residual work is
> tracked by the stabilization plan linked in the current reconciliation.

The DashboardLayout direct-composition migration (Phase 2's own prerequisite exit gate for this phase, tracked in [`dashboard-direct-layout-migration-plan-2026-09-01.md`](dashboard-direct-layout-migration-plan-2026-09-01.md)) had its 19-PR core sequence land at PR [#518](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/518), unblocking this phase per §8's ordering rule. A 2026-09-03 verification (see that document's §12) found this was 113/115 planned files, not 115/115 — `ProcessDetail.tsx`/`ProcessForm.tsx` were missed and still directly composed `DashboardLayout`. Fixed same day in [#525](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/525); the migration is now genuinely 115/115.

#### Goal: reach the Phase 2.5 exit gate (set 2026-09-04, after PR #594)

§8's exit gate is qualitative ("the targeted baseline is lower with no compensating increase in other rules... each batch has before/after metrics and reviewed exceptions") — every batch to date has already satisfied it batch-by-batch. This goal makes it concrete and trackable to an actual finish line, since "in progress" has now spanned 11+ any-retirement batches with no stated end state:

- **Target**: `@typescript-eslint/no-explicit-any` at 0 across `src/**` and `supabase/functions/**`, or every remaining instance individually reviewed and recorded in [`lint-exception-policy.md`](lint-exception-policy.md) as a deliberate exception (not silently left as debt).
- **Baseline at goal-setting (2026-09-04, SHA `38cec19b3`, post-PR-#594)**: 2545 findings across 419 files (full repo scan: `npx eslint "src/**/*.{ts,tsx}" "supabase/functions/**/*.ts"`, filtered to this rule). Largest remaining concentrations by file, for next-batch selection: `ask-viv-assistant/index.ts` (76), `TeamCommunicationsPage.tsx` (73), `useAuditWorkspace.ts` (49), `AdminStageDetail.tsx` (47), `tga-rto-sync/index.ts` (43), `useClientCommunications.ts`/`ManageDocuments.tsx`/`TasksManagement.tsx` (42 each), `PackageDetail.tsx` (41 — **excluded**, see below), `MyKpiSignOffSection.tsx` (39).
- **Standing exclusion**: `PackageDetail.tsx` and any other file the live Phase 2.6 register or an active characterization packet (e.g. [`codebase-optimization/phase-2-6-stabilization/task-dialog-characterization.md`](codebase-optimization/phase-2-6-stabilization/task-dialog-characterization.md)) names as an in-flight consolidation/retirement candidate. Re-check the register at the start of every batch — it moves.
- **Method**: continue the established cluster-by-cluster approach (grep real consumers, check generated DB types before removing a cast, `.select<Query, Result>()` for TS2589 nested-embed errors, `catch (error) { error instanceof Error ? ... }` narrowing) — one file or a small same-feature-area group per PR, each independently verified (`lint:ratchet` → `typecheck` → `test:frontend` → `test:edge`, then live Playwright) before merging.
- **Standing authorization** (2026-09-04): auto-merge each batch once its own full verification (all 4 checks + Playwright) passes; only pause and notify Carl if a batch finds something critical (a security issue, data-loss risk, or a fix requiring a product/schema decision that can't be safely deferred) rather than a routine documented-not-fixed finding.
- **Exit is declared** only once the repo-wide scan above returns 0 (or is fully reconciled against the exception policy) — track progress via `lint-baseline.json`'s regenerated totals after each batch, not by hand-summing individual batch deltas (see batch 7a's process note on why that drifts).
- **Progress checkpoint (2026-09-04/05, after PR #619, any-batch 30)**: 1849 findings across 429 files (down from the 2545/419 baseline at goal-setting — 696 findings retired across batches 1-30, all with independently verified before/after deltas in the table below). Batches 12-20 were tracked only in `codebase-optimization/phase-2-6-stabilization/l10-real-bugs-found.md`/session memory at the time and were backfilled into this table's row-by-row record after the fact — the regenerated `lint-baseline.json` total is what actually gates exit, not this table, so that gap never affected correctness, only historical legibility. Real bugs found this stretch (batches 21-30) beyond the ones already listed in `codebase-optimization/phase-2-6-stabilization/l10-real-bugs-found.md`: none new — see individual batch rows below for the two most significant (batch 19's `stage_instances.status_id`, batch 22's `calendar_events` insert).

| ID | Candidate | Status | PR | Notes |
|---|---|---|---|---|
| Phase 2.5 PR 1 | Publish committed, machine-readable rule/source-class/directory/file baseline and exception policy | ✅ Done | [#519](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/519) | `scripts/generate-lint-baseline.mjs` → [`lint-baseline.json`](lint-baseline.json); policy at [`lint-exception-policy.md`](lint-exception-policy.md). Measured at branch-cut SHA `3f419e29`: 647 files with findings, 3,827 errors, 191 warnings (rule-attributable) — a small, expected drift from PR #515's 2026-09-03 doc-table snapshot (4,059/651) since `main` moved between the two measurements. One exception recorded: `src/integrations/supabase/types.ts` (generated). |
| Phase 2.5 PR 2 | Zero-behavior-risk mechanical cleanup: `prefer-const`, `no-useless-catch`, `no-useless-escape`, `no-empty-object-type`, `no-require-imports`, `ban-ts-comment`, `no-control-regex` (50 findings, 35 files) | ✅ Done | [#520](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/520) | ESLint's own `--fix` handled 34/37 `prefer-const` sites; the rest (a destructure mixing a reassigned and non-reassigned binding, 5 pass-through try/catch wrappers, 3 unnecessary regex escapes, 2 empty-interface→type-alias conversions, a `require()`→`import` swap in `tailwind.config.ts`, a `@ts-ignore`→`@ts-expect-error` swap in `vite.config.ts`, one deliberate `no-control-regex` suppression with a comment) were hand-verified. Baseline dropped 3,827→3,772 errors; `lint:ratchet` showed zero regressions across all 34 touched files; full frontend (298/298) and edge (254/254) suites pass; `npm run build:dev` confirmed the Tailwind config change. **Discovered mid-batch:** `src/integrations/supabase/previewAuthStorage.ts` is a second generated file (Lovable preview-auth brokering, marked "do not edit directly") carrying one `prefer-const` finding — left untouched; added to the exception policy and baseline in this same PR rather than hand-fixed. One `no-empty` finding in `ManageDocuments.tsx` (a file otherwise touched in this batch) was also left alone as out of this batch's declared scope. |
| Phase 2.5 PR 3 | Clear remaining non-`any` correctness findings needing per-site judgment: `no-case-declarations` (10), `no-empty` (6), `no-non-null-asserted-optional-chain` (8) | ✅ Done | [#522](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/522) | `no-case-declarations`: wrapped 4 switch-case bodies in block scopes after confirming each ends in `return` with no fallthrough. `no-empty`: added explanatory comments to 6 intentional best-effort catch blocks (ESLint treats a commented empty block as deliberate). `no-non-null-asserted-optional-chain`: replaced `profile?.field!` with an explicit throwing guard at 6 sites across 5 hooks, and a plain non-null assertion on the already-`enabled`-gate-protected parent object at 1 site — each of these could previously have silently written `undefined` into a required DB column instead of failing loudly. Baseline dropped 3,772→3,748 errors; zero ratchet regressions across 13 touched files; full frontend (298/298) and edge (254/254) suites pass. |
| Phase 2.5 PR 4 | `react-refresh/only-export-components` (38) — module-boundary cleanup, alongside affected component/module changes | ⏳ Not started (deliberately — the baseline's own guidance says this rule should ride alongside other component changes, not get a standalone sweep) | — | — |
| Phase 2.5 hooks batch 1 | `react-hooks/rules-of-hooks` (4) + `exhaustive-deps` (2), all in `BulkInvite.tsx` | ✅ Done | [#523](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/523) | Genuine bug, not just lint debt: two early returns sat before 4 hook calls, throwing React's "Rendered more hooks" error on every real Super Admin page load once `profile` resolved. Fixed by moving the guard after all hooks (each hook already no-ops internally via `isSuperAdmin`); verified live via Playwright (SuperAdmin persona, zero console errors, correct data). Baseline dropped 3,748→3,744 errors, 191→189 warnings. |
| Phase 2.5 hooks batch 2 | `react-hooks/exhaustive-deps` (10), 3 files, one feature area (SuperAdmin Academy admin pages: `AcademyCertificatesPage.tsx`, `AcademyTenantAccessPage.tsx`, `workforce-pdp.tsx`) | ✅ Done | [#528](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/528) | Each a genuine "memo doesn't actually memoize" bug: `now`/`thirtyDaysFromNow` were recreated every render (not memoized) in the first two files — fixed by snapshotting `now` once per mount via its own `useMemo`; `workforce-pdp.tsx`'s `rows = data ?? []` created a new array reference every render whenever `data` was falsy, defeating 4 downstream memos — wrapped in its own `useMemo` per ESLint's suggested fix. Verified live via Playwright: all 3 pages render correctly with real data and their interactive filters were exercised end-to-end, zero console errors. Baseline dropped 189→179 warnings (all-warnings batch, no errors). |
| Phase 2.5 hooks batch 3 | `react-hooks/exhaustive-deps` (6), 3 files, one feature area (audit creation/workspace: `AuditFormTab.tsx`, `SendEvidenceRequestDrawer.tsx`, `NewAuditModal.tsx`) | ✅ Done | [#530](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/530) | 3 of 4 `NewAuditModal.tsx` findings were the same guarded-default-setter pattern as batches 1/2; the 4th (`resetForm` missing from an effect gated on `!open`) was a real footgun — `resetForm` was a plain function recreated every render that reset state to new array/object literals, so blindly adding it to deps would have caused continuous re-renders while the modal was closed. Fixed by wrapping `resetForm` in `useCallback` first. `SendEvidenceRequestDrawer.tsx` reused the same documented "reset on open, not on every object-reference change" `eslint-disable` pattern from batch 1. Verified live via Playwright on `/audits`: opened/closed/reopened the New Audit modal, advanced through card selection and client selection (lead auditor correctly auto-defaulted), confirmed `resetForm` actually cleared state with no stale data or re-render symptoms. Baseline dropped 179→173 warnings. |
| Phase 2.5 hooks batch 4 | `react-hooks/exhaustive-deps` (8), 7 files, one feature area (EOS dialogs: meetings, rocks, QC — `ApplyTemplateDialog.tsx`, `MeetingScheduler.tsx`, `RockFormDialog.tsx`, `QCSectionCard.tsx`, `IDSDialog.tsx`, `MeetingCloseValidationDialog.tsx`, `SaveStatusIndicator.tsx`) | ✅ Done | [#531](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/531) | Deliberately excludes `AttendancePanel.tsx`/`LiveMeetingView.tsx` (the plan's own "active workflow" flagged files) for their own separate batch. 4 files safe to fix directly (self-guarding via ref/flag, or the effect only sets an idempotently-recomputed value); 3 files suppressed with a documented `eslint-disable` (reading `issue`, `validateClose`/`userRating`, or `state` only as a guard — adding them would wipe an in-progress edit, refire a real mutation API call every render, or cancel an idle-revert timer). Verified live via Playwright: edited two different Rocks (confirmed no data bleed between them), exercised `MeetingScheduler`'s auto-template-select on type change. `IDSDialog` only reachable from an active live meeting — deferred to the LiveMeetingView batch; reuses the already-verified id/status-narrowing pattern. Baseline dropped 173→165 warnings. Also surfaced (not fixed here): an unrelated pre-existing `main` typecheck regression, `AcademyAddCoursePage.tsx` duplicate identifier — confirmed via empty diff against `origin/main`, flagged separately. Remaining `react-hooks/exhaustive-deps` (127) findings are still queued as further small, one-file/feature-at-a-time batches. |
| Phase 2.5 hooks batch 5 | `react-hooks/exhaustive-deps` (4), `AttendancePanel.tsx` + `LiveMeetingView.tsx` — the plan's own "active workflow" flagged files, deferred from batch 4 | ✅ Done | [#533](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/533) | 3 of 4 safe to fix directly (ref-guarded mutation calls, same pattern as batch 4); the 4th (phrase-draft seeding) was a real trap where the obvious "just add the missing dep" fix would let a user's deliberate field-clear get silently undone — suppressed instead. **Live-verified without touching a real meeting**: created a throwaway `eos_meetings` row (clearly-labeled test title, minimal agenda, one participant) via direct SQL, drove it through the real UI — `AttendancePanel`'s auto-seed correctly seeded all 13 real Vivacity team members, the facilitator-confirm-then-start flow genuinely flipped the meeting to `in_progress`, and `LiveMeetingView`'s auto-add-current-user effect correctly marked the tester's own attendance row `attended` (confirmed via DB query) — zero console errors throughout. Deleted the test meeting afterward; cascade delete confirmed via query to leave zero orphaned rows in any child table. Baseline dropped 165→161 warnings. |
| Phase 2.5 hooks batch 6 | `react-hooks/exhaustive-deps` (9), 4 files, one feature area (admin pages: `CohortAccessSenderJob.tsx`, `RolePermissionsEditor.tsx`, `StaffEngagementDetail.tsx`, `RecentDraftsTable.tsx`) | ✅ Done | [#535](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/535) | 3 files were the familiar `x = data ?? []` pattern, wrapped in `useMemo`. `CohortAccessSenderJob.tsx` was a real risk if fixed naively — its mount effect's missing dep was a plain `refresh` function recreated every render that itself calls setters with fresh objects, so adding it directly would have created a fetch-render-fetch loop; wrapped in `useCallback` first. Verified live via Playwright: Role Permission Editor matrix + category filter, AI Drafting Insights real stats, a Staff Engagement checklist's real completion counts, and a cohort job detail page — plus a 3-second network-request watch confirming zero repeated fetches after the `refresh` fix. Baseline dropped 161→152 warnings. |
| Phase 2.5 hooks batch 7a | `react-hooks/exhaustive-deps` (14), 14 files, `src/components/client` sub-batch 1/2 | ✅ Done | [#536](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/536) | Two new risk patterns beyond prior batches: `PackageStagesManager.tsx`/`StartPackageDialog.tsx`/`TenantInviteDialog.tsx`/`ClientProfileForm.tsx` each had a plain function recreated every render feeding a mount effect or parent callback — wrapped in `useCallback`. `PackageDataManager.tsx`'s `DatePickerCell` computed `date = new Date(...)` fresh every render fed into `setDisplayMonth` — since `Date` objects never compare equal by reference, adding it directly as suggested would loop forever; memoized `date` on the actual `value` string first. `ClientAddressSection.tsx`'s `fetchAddresses`/`seedFromTga` mutually recurse, hitting a TDZ ordering problem if both were memoized — suppressed instead. Verified live via Playwright on a real tenant: stages/date-picker/diagnostics/start-package/integrations all rendered correctly with real data, zero console errors, page stayed responsive (no freeze) after opening the date picker. Baseline dropped 152→138 warnings. Remaining `src/components/client` findings (6) queued as batch 7b; `react-hooks/exhaustive-deps` total (100) still queued overall. |
| Phase 2.5 hooks batch 7b | `react-hooks/exhaustive-deps` (6), 2 files, `src/components/client` sub-batch 2/2 (final) — `ClientStructuredNotesTab.tsx`, `ClientTimeTab.tsx` | ✅ Done | [#537](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/537) | `ClientTimeTab.tsx`'s two findings were the familiar patterns: a conditional expression rebuilding an object every render (`effectiveWindow`) and a `data ?? []` array, both wrapped in `useMemo`. `ClientStructuredNotesTab.tsx` produced a genuine cascade: adding `scopeTag`/`toast` to a `useCallback` that already depended on `resetForm` surfaced a *new* finding (confirmed absent on `origin/main`) that `resetForm` itself — a plain function — needed memoizing; wrapping it in turn surfaced a second new finding on an *earlier* effect (the URL `initNote=true` auto-open handler) that also called `resetForm` and now needed it in its own deps. Since that earlier effect is declared before `resetForm`'s original position in the file, adding the dependency as flagged would have been a TDZ `ReferenceError` at runtime — fixed by relocating `resetForm`'s `useCallback` definition (unchanged, deps `[]`) to just before that effect rather than leaving it in place. Verified live via Playwright on Demo RTO's Notes tab: `?initNote=true` opened the Add Note dialog with all fields at their true reset defaults and no console errors, then the Time tab loaded cleanly. Baseline dropped 138→132 warnings. `src/components/client` cluster (20 findings across both sub-batches) is now fully clear; `react-hooks/exhaustive-deps` total is 94, still queued overall. |
| Phase 2.5 hooks batch 9 | `react-hooks/exhaustive-deps` (11), 7 files, `src/pages/Tenant*.tsx` sub-page cluster (`TenantDetail`, `TenantDocumentDetail`, `TenantDocuments`, `TenantLogins`, `TenantMembers`, `TenantNotes`, `TenantUsers`) | ✅ Done | [#538](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/538) | All followed the established "wrap fetch/filter function in `useCallback`, relocate it ahead of the effect that needs it" pattern. `TenantDetail.tsx` was the most involved: its mount-only package-list fetch effect is deliberately narrowed to `tenantId` (suppressed); its real-time document-subscription effect now depends on the memoized `fetchTenantData`, which actually fixes a latent stale-closure bug — switching the active package after the channel first subscribed used to keep re-fetching the old package's task count on later realtime events. **Found during verification, not previously known:** `TenantDetail.tsx` is dead code — nothing imports it (`App.tsx`'s own comment confirms its wrapper was already "removed — consolidated into ClientDetailWrapper"). Fixed anyway since the change is harmless and correct, but it could not be live-verified and is a candidate for future deletion, not done here to avoid scope creep. The other 6 files are all live-routed; verified live via Playwright on all of them with zero console errors, including switching to the ClickUp Task Comments note source (exercising `fetchClickupTasks`) and typing a search filter on `/admin/tenant-users` (confirmed correct row filtering via `applyFilters`). Note: PR #538 branched before #537 merged, so its own "138→127" delta is relative to pre-#537 `main`, not chained after batch 7b — the true post-both-merges total is tracked by the regenerated `lint-baseline.json`, not by summing these two deltas. Batch numbering (7b, 9) reflects creation order, not merge order — #538 merged first. |
| Phase 2.5 hooks batch 10 | `react-hooks/exhaustive-deps` (11), 8 files, `src/pages/Manage*.tsx` / `AdminManage*.tsx` admin cluster (`AdminManagePackages`, `AdminManageStages`, `ManageCategories`, `ManageDocuments`, `ManageInvites`, `ManageStages`, `ManageTenants`, `ManageUsers`) | ✅ Done | [#539](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/539) | All followed the established "wrap fetch/filter function in `useCallback`, relocate it ahead of the effect that needs it" pattern. `ManageDocuments.tsx` needed a two-level fix: memoizing `applyFiltersAndSort` surfaced a new missing-dependency finding for `applyNonFileStatusFilters` (a helper it calls internally), which needed memoizing in turn with its own accurate dependency list. **Found during verification, not previously known:** `AdminManagePackages.tsx` is dead code — nothing routes to it, same situation as `TenantDetail.tsx` in batch 9 (see [`dashboard-direct-layout-migration-plan-2026-09-01.md`](dashboard-direct-layout-migration-plan-2026-09-01.md) for that precedent). Fixed anyway since harmless and correct, but could not be live-verified. The other 7 files are live-routed; verified live via Playwright with zero console errors on each, including confirming correct filter narrowing on `/manage-documents` (20→18 rows), `/manage-tenants` (→1 row), and `/manage-users` (→4 rows, with only the filter effect re-firing on keystroke, not a duplicate fetch). Baseline dropped 121→110 warnings. Two files now confirmed dead code across this cluster of batches (`TenantDetail.tsx`, `AdminManagePackages.tsx`) — worth a dedicated dead-code sweep at some point, not done here to avoid scope creep. |
| Phase 2.5 hooks batch 11 | `react-hooks/exhaustive-deps` (11), 10 files, `src/hooks/*` shared-data-hooks cluster (`useAuditWorkspace`, `useEmailTemplates`, `useMissingMergeFields`, `useNotes`, `useOutlookCalendar`, `useSharePointBrowser`, `useStageDocuments`, `useStageEmails`, `useStageNotes`, `useTenantPacks`, `useTimeInbox`) — the last clustered group before remaining findings are scattered one-offs | ✅ Done | [#540](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/540) | Mostly the established `useCallback` pattern, plus three real finds: `useAuditWorkspace.ts`'s debounced score-save closed over a `useMutation()` result object (a fresh object every render) — destructured just the stable `.mutate` function instead of adding the whole object to deps, avoiding a refire-every-render trap. `useTimeInbox.tsx`'s `fetchDrafts` was missing `showOverdueOnly` — a genuine bug where toggling the "Overdue only" switch never refetched with the filter applied; confirmed fixed live via a fresh network request after toggling. `useNotes.tsx`'s `parentType` is deliberately tracked via its serialized `parentTypeKey` — suppressed rather than widened. `useStageNotes.ts`'s `packageId` is accepted but never used in its query — dropped from deps to match reality, after wrapping it in `useCallback` surfaced its own new "unnecessary dependency" finding. Verified live via Playwright: `useStageDocuments`/`useStageEmails`/`useStageNotes` via a real stage on Demo RTO (279 docs, 3 emails, 4 notes), `useAuditWorkspace` on a real completed audit, `useOutlookCalendar` on `/calendar/time-capture`, `useTimeInbox`'s specific fix via network-request confirmation, `useEmailTemplates`/`useMissingMergeFields` module-load checks. `useSharePointBrowser`/`useTenantPacks` are gated behind client-portal-only routes — verified via lint/typecheck/tests only (both fixes are purely mechanical, adding already-declared primitives to a deps array). Baseline dropped 110→98 warnings. |
| Phase 2.5 hooks batch 12 | `react-hooks/exhaustive-deps` (4), 3 files, `src/pages/client/*` cluster (`AcademyAssessmentPlayerPage`, `ClientTgaDetailsPage`, `SupportTicketsPortalPage`) | ✅ Done | [#541](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/541) | `AcademyAssessmentPlayerPage.tsx` reused the `useAuditWorkspace.ts` pattern from batch 11: `submitMutation` (a `useMutation()` result) is a fresh object every render, so `handleSubmit` destructures just the stable `.mutate` function before being safely added to the auto-submit timer effect's deps. **Known gap found during verification, NOT fixed here (out of scope, confirmed by direct instruction to file and continue rather than fold in an unrelated architectural fix):** `AcademyAssessmentPlayerPage` currently crashes to a full "Something went wrong" screen for every client attempting an academy assessment — it calls `useBlocker`, which requires a React Router data router (`createBrowserRouter`), but the app uses a plain `<BrowserRouter>` (`App.tsx:110`). Confirmed via `git diff origin/main` that the `useBlocker` line is untouched by this PR and identical on `main` — predates this program entirely. Needs a real fix (swap to a data router, or replace `useBlocker` with a different unsaved-changes guard) in a dedicated follow-up, not a lint-debt PR. Verified live as the real Demo RTO client persona (not SuperAdmin "View as Client"): `/client/support-tickets` and `/client/tga` both render correctly with zero console errors; the assessment page itself couldn't be interactively verified due to the crash above. Baseline dropped 98→94 warnings. |
| Phase 2.5 hooks batch 13 | `react-hooks/exhaustive-deps` (3), 3 files, `src/pages/Executive*.tsx` cluster (`ExecutiveClientCommitments`, `ExecutiveDecisionQueue`, `ExecutiveFinancialControls`) | ✅ Done | [#543](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/543) | Identical pattern across all three: `fetchRecords` only reads `toast`, wrapped in `useCallback` and relocated ahead of its mount effect. Verified live via Playwright (SuperAdmin persona) on all 3 routes, zero console errors. Baseline dropped 94→91 warnings. Remaining findings are now single/double-finding one-offs scattered across ~47 files; no more large clusters left. |
| Phase 2.5 hooks batch 14 | `react-hooks/exhaustive-deps` (4), 2 files, `src/pages/Team*.tsx` cluster (`TeamSettings`, `TeamUsers`) | ✅ Done | [#544](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/544) | Same established pattern. Verified live via Playwright (SuperAdmin persona), including confirming the search filter on `/admin/team-users` correctly narrowed 24→2 rows. Baseline dropped 91→87 warnings. |
| Phase 2.5 hooks batch 15 | `react-hooks/exhaustive-deps` (3), 3 files, `src/components/stage/*` cluster (`EmailAttachmentsManager`, `StageQualityIndicator`, `StageSimulationDialog`) | ✅ Done | [#545](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/545) | `EmailAttachmentsManager.tsx`: `data ?? []` pattern (`rows`), memoized. `StageQualityIndicator.tsx`/`StageSimulationDialog.tsx`: `useCallback` on the missing functions; the latter's `clearSimulation`/`fetchPackagesUsingStage`/`fetchTenantsForSimulation` (from `useStageSimulation`) were already individually stable inside that hook, safe to add directly. **Found during verification, unrelated, not fixed**: "Simulate Stage" logs a pre-existing 400 — `package_stages.package_id` has no FK to `packages.id` in the current schema, so PostgREST can't resolve the embed in `useStageSimulation.tsx` (confirmed untouched by this PR via `git diff`). The dialog handles it gracefully ("No packages found," no crash) — flagged as a separate minor bug, not fixed here. Verified live via Playwright: stages list quality indicators render, simulate dialog opens cleanly, email-attachments editor renders with zero console errors. Baseline dropped 87→84 warnings. |
| Phase 2.5 hooks batch 16 | `react-hooks/exhaustive-deps` (2), 2 files, `src/pages/Eos*.tsx` cluster (`EosQCSession`, `EosRocks`) | ✅ Done | [#546](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/546) | Both plain-boolean primitives missing from an already-self-guarded effect — safe to add directly, no relocation. Verified live via Playwright: `/eos/rocks`'s user filter correctly auto-defaults to the logged-in user; `/eos/qc/:id` loaded real data cleanly. Baseline dropped 84→82 warnings. |
| Phase 2.5 hooks batch 17 | `react-hooks/exhaustive-deps` (2), 2 files, `src/components/package-builder/*` cluster (`BulkGenerateDocumentsDialog`, `StagePreviewDialog`) | ✅ Done | [#547](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/547) | Established pattern. **Found during verification, unrelated, filed not fixed**: `StagePreviewDialog`'s usage-count queries 400 on every stage — `documents`/`package_client_tasks`/`package_stage_emails`/`package_staff_tasks` all lack an FK from `package_id` to `packages.id`, so the PostgREST embed fails and the dialog silently shows "0 configured" regardless of real content (confirmed with a stage that has 2 real linked documents showing 0). Same root cause as batch 15's `useStageSimulation.tsx` finding. Audit entry: `docs/audit-log/entries/2026-09-04-stage-preview-missing-package-id-fks.md`. Baseline dropped 82→80 warnings. |
| Phase 2.5 hooks batch 18 | `react-hooks/exhaustive-deps` (42), 38 files, all remaining scattered one-off findings — closes out the rule repo-wide | ✅ Done | [#550](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/550) | Per the workflow-efficiency-checkpoints practice, consolidated into one PR with 4 internal commits (18a dialogs/shared components, 18b messaging/profile/tenant, 18c invitation/audit/calendar/client pages, 18d remaining pages) instead of ~40 micro-PRs, using one reused worktree and parallelized typecheck/test:frontend/test:edge runs after each commit rather than a fresh install + sequential verification per file. Several real bugs beyond lint appeasement: a genuine stale-closure cycle in `celebration.tsx` (`dismiss` permanently captured the first render's `showCelebration`, so toggling reduced-motion mid-session while a celebration was queued kept using stale settings — fixed with a ref to break the circular `useCallback` dependency); a broken suppression in `KpiDeveloperTicketQueue.tsx` (`/* eslint-disable-next-line */` sat mid-statement on the same line as the effect it was meant to guard, disabling nothing — the fetch never re-ran when it should have); an unstable inline-array prop in `AddTimeDialog.tsx` (`activePackages` rebuilt as a new array literal every render) that would have made the receiving `NoteFormDialog` effect wipe user input on unrelated parent re-renders had it been fixed naively — memoized at the source instead; two more `data ?? []`-shaped unstable references (`useDocumentReadiness`'s `validateDocument`/`validateReleaseReadiness`, `MyOnboardingPage`'s `instances`); and `TasksManagement.tsx`'s real-time subscription effect, which would have re-subscribed to Postgres changes on every render had its called functions not been memoized before being added to deps. `PackageDetail.tsx` needed the most structural rework — three ~200-line fetch/filter functions were defined after the effects that called them, so the effects had to move below the functions rather than relocating the function bodies. Verified live via Playwright (SuperAdmin persona): Dashboard, Tasks Management (search filter), Package Detail (search filter, exercising the restructured fetch/effect chain), Settings + Team Profile tab — zero new console errors on any of them. Regenerated `lint-baseline.json`: 598 files with findings, 3744 errors, 38 warnings (all `react-refresh/only-export-components`) — **`react-hooks/exhaustive-deps` is now 0 repo-wide.** |
| Phase 2.5 any-batch 1 | `@typescript-eslint/no-explicit-any` (24), `src/types` EOS/audit shared-contract cluster (`eos.ts` 22, `audit.ts` 1, `eosAlerts.ts` 1) | ✅ Done | [#551](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/551) | First `any`-retirement batch, following the plan's own priority (shared contracts / high-risk boundaries first, not raw directory size). Every field checked against real usage before typing — not guessed: `EosMeetingSummary`'s array fields got real snapshot interfaces read directly off `generate_meeting_summary()`'s actual `jsonb_build_object` calls in the migration SQL (not inferred from partial frontend usage, which would have missed fields the UI doesn't render); VTO builder fields (`core_values`/`proven_process`/etc.) confirmed genuinely mixed-shape at runtime via existing `Array.isArray`/`typeof` guards in `VtoEditor`/`VtoViewer`, typed as the real generated `Json` union rather than a fabricated single shape; several fields (`EosVtoDraft.draft_json`, `EosAccountabilityChart.chart_data`, `EosMeeting.scorecard_data`/`rock_reviews`/`headlines`, both audit-log `details` fields) confirmed zero-consumer via grep before typing `Json`, zero behavioral risk. Fixing `eos.ts` surfaced 4 real consumer-side type errors (via `tsc`, not guessed) in `VtoEditor.tsx`/`VtoViewer.tsx`/`EosMeetingSummary.tsx`/`useEosAlerts.tsx`, each fixed with the minimal correct cast or (for `VtoViewer`) by applying a narrowing pattern the same file already used two sections above for a sibling field. Deliberately deferred `qc.ts`'s 1 finding — its `value_json` field has real consumers across 4 files that need their own narrowing, better scoped as its own QC-feature batch. Baseline dropped 3744→3720 (exactly the 24 targeted, no compensating increase). Verified live via Playwright (SuperAdmin persona): `/eos/vto` view and edit modes both render real production Core Values / "What Makes Us Different" data correctly, zero console errors. |
| Phase 2.5 any-batch 2 | `@typescript-eslint/no-explicit-any` (2), QC (quarterly conversations) `value_json` cluster — `src/types/qc.ts`'s `QCAnswer.value_json` (deferred from batch 1) + its sibling in `useQuarterlyConversations.tsx`'s upsert mutation | ✅ Done | [#552](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/552) | `value_json` is genuinely `{ value: X }` by write-side convention, but X's real type varies by prompt type (text/textarea/boolean/rating/list/checklist) — typed the real generated `Json` union rather than a guessed shape, matching batch 1's precedent for anything flowing into a Supabase RPC/insert. Typing it surfaced 6 real consumer-side errors across `QCSectionCard.tsx`/`EosQCSession.tsx`/`qcPdfExport.ts` (3 direct property-access sites plus 3 more downstream of `getOtherValue`'s changed inferred return type) — each fixed with a narrow `{ value?: string }` cast matching how that site already used the value. Baseline dropped 3720→3718 (exactly the 2 targeted). Verified live via Playwright (SuperAdmin persona) against a real completed Quarterly Conversation: Core Values answers/notes, the Summary & Sign-off prefill, and a full PDF export all rendered every real field correctly with zero console errors. |
| Phase 2.5 any-batch 3 | `@typescript-eslint/no-explicit-any` (15), `src/hooks/useEosConfigurations.tsx` | ✅ Done | [#555](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/555) | All 15 were `(supabase as any)` client-level casts, a different pattern from batches 1-2's JSON-field casts — used to bypass generated-schema checking on 4 tables (`eos_configurations`, `eos_configuration_segments`, `accountability_seats`, `accountability_seat_assignments`) that turned out to already exist in the generated types, so the casts were unnecessary legacy escape hatches, not a real gap. Removed all 11 client-level casts + 4 now-redundant map-callback annotations. Deliberately did not expand into sibling files `useEos.tsx`/`useEosOptions.ts` (73 findings across the wider EOS-hooks cluster) — those cast insert/update *payloads*, not the client, a different and more involved pattern needing per-table Insert/Update type verification; left as a clearly scoped future batch. Baseline dropped 3718→3703. Verified live via Playwright (SuperAdmin persona): all 4 EOS meeting-type configurations, a config's 7 real agenda segments, and the Facilitator Seat dropdown's real seat/holder-name join all render correctly, zero console errors. |
| Phase 2.5 any-batch 4 | `@typescript-eslint/no-explicit-any` (24), `src/hooks/useEos.tsx` (12) + `useEosOptions.ts` (12) — closes out the pair deferred from batch 3 | ✅ Done | [#556](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/556) | `useEosOptions.ts`'s 12 were unnecessary casts on 6 views/tables whose own code comments incorrectly claimed generated types were missing them — all 6 confirmed present, comments stale. `useEos.tsx`'s 12 required checking each table's real Insert/Update contract against the hand-written `EosX` interfaces: `createRock`/`createMeeting`/`createMetric` all destructured `tenant_id` out and never re-added it (the exact bug class already fixed once in this file for `createIssue`/`createTodo`, per a referenced prior audit entry) — confirmed via grep that none of the three affected mutations has any real caller (dead code, harmless today) before fixing anyway to match the established pattern. Removing the casts also surfaced two real type-accuracy bugs in `EosIssue`/`EosRock` (checked against real consumers, not guessed): a `priority` string-union arm no caller ever used (DB column is purely numeric), and a `progress` field with zero DB column or consumer at all (removed). Baseline dropped 3703→3679. Verified live via Playwright (SuperAdmin persona): `/eos/rocks` (134 real rocks, milestone progress bars), `/eos/risks-opportunities` (60 real items, priority badges from the now-numeric field), and the Add Item form's Category dropdown (8 real options) all render correctly, zero console errors. |
| Phase 2.5 any-batch 5 | `@typescript-eslint/no-explicit-any` (38), EOS Rocks/MeetingSummary component cluster — `MeetingSummaryCard.tsx` (14), `RockFormDialog.tsx` (14), `RockProgressControl.tsx` (1), `QuarterlyRocksSection.tsx` (8), `RockCard.tsx` (1) | ✅ Done | [#557](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/557) | Deliberately chosen to follow directly on from batch 1's `EosMeetingSummary` types and batch 4's `EosRock.milestones` fix while that schema context was still warm. Most findings were now-redundant casts droppable once the underlying types were correct; `RockProgressControl`'s `rock` prop was narrowed from the full `EosRock` to `Pick<EosRock, 'id'\|'status'\|'title'\|'description'\|'priority'>` (exactly what it reads) so a caller with a partial rock projection can pass one without a cast — this single change let two downstream files resolve their own findings without any casts. One real fallout bug caught by the compiler: `MeetingSummaryCard`'s cascade-message fallback used to silently render a whole raw object if `message` was falsy; now that `message` is a required string per the real type, that dead branch was removed rather than cast around. Baseline dropped 3679→3641. Verified live via Playwright: `/eos/rocks` (real status controls on every rock, a real rock's Edit dialog correctly loads its 3 real milestones), `/eos/flight-plan` (21 real rocks with working milestone sections), and a real meeting's summary page (real attendance/rocks-reviewed data) — zero console errors. |
| Phase 2.5 any-batch 6 | `@typescript-eslint/no-explicit-any` (71), 22 files — closes out the entire remaining EOS feature cluster (`LiveMeetingView.tsx` 10, `useEosHealth.tsx` 8, `useEosSegueShares.tsx` 7, `useEosAgendaTemplates.tsx` 6, `ApplyTemplateDialog.tsx` 5, `QCSectionCard.tsx` 4, `MeetingScheduler.tsx`/`useEosConfigMeetingActions.tsx`/`useEosReadiness.tsx` 3 each, `useEosDrafts.tsx`/`useEosRocksHierarchy.tsx`/`QCScheduler.tsx` 2 each, plus 10 files with 1 each) | ✅ Done | [#TBD] | Full sweep consolidating every remaining EOS `any` finding into one PR, reusing the compounding methodology from batches 1-5 (grep real consumers, check real DB/RPC shapes before typing, `Json`/`TablesInsert`/`TablesUpdate` for anything flowing into a Supabase call). Found a second live bug this phase (first was batch 4's dead-code tenant_id drop): `useEosHealth.tsx`'s `eos_rocks` query never selected `seat_id`, but `calculateRockDiscipline` reads `r.seat_id` to penalize the Health Score's Rocks dimension — every tenant's score was wrongly treating 100% of rocks as seat-less regardless of the real value, confirmed via direct SQL (4 of 42 real current-quarter rocks do have a seat). Fixed by adding `seat_id` to the select. Also found two stale-type gaps matching batch 4's pattern: `EosConfiguration.facilitator_seat_id`/`visionary_seat_id`/`integrator_seat_id` were hand-typed as `string \| undefined` when the real DB columns are nullable (`string \| null`) — widened to match, only consumers were a truthiness check and an `?? ''` fallback, both null-safe already; and a stale "types.ts not regenerated" code comment in `useEosMeetingSegments.tsx` next to `go_to_previous_segment`, which has in fact been in generated types for some time (same drift class as batch 4's `useEosOptions.ts` comments). `useEosReadiness.tsx`'s nested aliased `accountability_charts` embed needed 3 hand-written interfaces (`ReadinessFunction`/`ReadinessSeat`/`ReadinessSeatAssignment`) since the Supabase client can't infer types for aliased nested relationships. Deliberately deferred `ImportVideosPanel.tsx` (2 findings, `src/components/academy/builder/`) — out of EOS scope, candidate for a future academy-focused batch. Baseline dropped 3641→3570 (71 targeted, no compensating increase). |

**Phase 2.5 reconciliation checkpoint (2026-09-05, current `origin/main` `1641f418`):** the historical rows above were not kept current after subsequent any-retirement merges. A fresh baseline reports **267 files with findings, 1,176 errors, and 39 warnings**. Claude Code's next 11-file batch was independently verified in an isolated worktree: lint ratchet passed; frontend 298 passed/15 skipped; Edge 260 passed; build passed; authenticated SuperAdmin Playwright 4/4 passed; unauthenticated desktop/mobile Playwright 12/12 passed. Typecheck still reports only pre-existing defects in untouched `ClientLayout.tsx` and `useKpiSummary.tsx`; no changed file introduced a type error. The batch is ready for its own PR. Phase 2.5's final exit gate remains open until the remaining any-retirement queue is explicitly dispositioned and this checkpoint is merged.
**Post-merge correction (2026-09-05):** the 11-file batch and this reconciliation shipped in [PR #693](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/693), merged at `c897ef863` (15:16:12Z). The residual queue remains tracked separately; the historical Phase 2.5 any-batch-6 row above is retained as an archival description and must not be read as the current baseline.

**Profile cluster checkpoint (2026-09-05):** `TeamProfileFields.tsx` and `AdminActions.tsx` were independently typed against current generated contracts (10 `no-explicit-any` findings retired), with no runtime/schema/RLS/RPC changes. Lint ratchet, frontend/Edge tests, build, and authenticated SuperAdmin Playwright all pass; typecheck retains only the documented untouched `ClientLayout.tsx`/`useKpiSummary.tsx` baseline errors. Fresh baseline: **265 files, 1,166 errors, 39 warnings**.
| Phase 2.5 any-batch 7a | `@typescript-eslint/no-explicit-any` (39), `src/hooks/academy` sub-batch 1/2 — `useAcademyWorkbooks.ts`, `useEnrolCourse.ts`, `useAcademyTagManagement.ts`, `useMyEnrolledCourses.ts`, `useAcademyCourseResources.ts`, `useAcademyPackageRules.ts`, `useAdminAcademyCourses.ts` | ✅ Done | [#TBD] | First batch of the `src/hooks/academy` cluster (153 findings, 12 files total) — took the smaller/medium files first. Confirmed every table/RPC referenced by a removed cast exists in generated types before dropping it (`academy_course_resources`, `resource_library`, `fn_academy_rule_dashboard_stats`, `fn_academy_backfill_enrollments_for_rule`). New fallout pattern: Supabase's typed `.update()` builder rejects a variable whose *declared* type includes a property incompatible with the target even when no caller actually sets it — `AdminCourse.id: number` clashed with `academy_courses`' Update type declaring `id` as never-updatable; fixed by typing the payload as `Partial<Omit<AdminCourse, 'id' | 'module_count' | 'lesson_count' | 'enrollment_count'>>` instead of `Partial<AdminCourse>`. Verified live via Playwright (SuperAdmin): `/superadmin/academy/builder` loads all 115 real courses, and the search filter correctly narrows 115→3, zero console errors. Remaining 114 findings across 5 files (`useTenantAcademyAccess.ts`, `useAcademyCertificates.ts`, `useAcademyAssessmentBuilder.ts`, `useAcademyModulesLessons.ts`, `useAcademyEnrollments.ts`) queued as 7b. **Process correction this batch:** `lint-baseline.json` had not actually been regenerated/committed since batch 5 (#557) — batches 6 and the caching PR both landed without it, so the file's "3641→3641" narrative-tracked numbers in this table had silently drifted from the real committed baseline (confirmed via direct regeneration: batch 5 truly ended at 3634, not 3641 — a small, unexplained 7-count drift, likely an unrelated concurrent fix elsewhere in the repo during that window). Regenerated and committed the real baseline in this PR: 3634 (post-batch-5, confirmed) → 3530 (post-batch-7a, confirmed) — a real net drop of 104, more than this batch's own 39 because it also absorbs batch 6's un-recommitted -71 (plus whatever untracked drift happened in between). Going forward, regenerate `lint-baseline.json` and commit it in every batch's own PR — don't compute deltas by hand from the previous batch's narrative claim. |
| Phase 2.5 any-batch 7b | `@typescript-eslint/no-explicit-any` (114), `src/hooks/academy` sub-batch 2/2 (final) — `useTenantAcademyAccess.ts`, `useAcademyCertificates.ts`, `useAcademyAssessmentBuilder.ts`, `useAcademyModulesLessons.ts`, `useAcademyEnrollments.ts` — closes out the entire `src/hooks/academy` cluster (153 findings total across both sub-batches) | ✅ Done | [#TBD] | Same methodology throughout: `Json`/`Tables<>`/`TablesInsert<>` for anything flowing into or out of a Supabase call, `Partial<Omit<Interface, 'id'|...>> & {requiredField}` for update/create payloads matched against the file's own existing interface rather than a bare `Record<string, any>`. One real fallout fix outside the batch's own file scope: `useCreateLesson`'s new stricter type surfaced that `LessonEditorPanel.tsx` (a `.tsx` component, not part of this batch's target directory) built its update/insert payload as `Record<string, any>` — TS correctly rejected it since a bare `Record` type structurally can't prove a required `title` field is present. Fixed by giving that one local `payload` an explicit object type instead of expanding this batch into components. Baseline (now regenerated and committed every batch, see batch 7a's process fix) dropped 3530→3415 (115 = 114 targeted + 1 from the `LessonEditorPanel.tsx` fallout fix). Verified live via Playwright (SuperAdmin): `/superadmin/academy/builder/120`'s Structure and Assessment tabs both render correctly, a real "Create Assessment" click exercised the retyped `useCreateAssessment` mutation end-to-end (verified via direct SQL, then deleted — zero residue), and `/superadmin/academy/enrollments` renders all 660 real enrolments with correct stats (569 active, 91 completed, 448 auto-enrolled), zero console errors throughout. `src/hooks/academy` is now fully clear of `no-explicit-any` (0 findings, confirmed via `npx eslint`). |
| Phase 2.5 any-batch 8a | `@typescript-eslint/no-explicit-any` (42), `src/hooks/useStage*` sub-batch 1/? — `useStageCertification.tsx`, `useStageVersions.tsx`, `useStageEmails.ts`, `useStageActiveUsage.tsx`, `useStageAuditLink.ts`, `useStageAuditLog.tsx`, `useStageReviews.tsx`, `useStageDocuments.ts`, `useStageReleases.tsx` | ✅ Done | [#TBD] | First batch of the `src/hooks/useStage*` cluster (182 findings, 17 files) — the package/stage-builder feature area, chosen as the next natural cluster after `src/hooks/academy`. Took the 9 smallest files first. **Found and fixed a real live bug**: `useStageActiveUsage.tsx` queried `tenants.tenant_id`, but `tenants`' real primary key column is `id` — there is no `tenant_id` column on that table at all. The `(supabase as any)` cast had silently hidden this for as long as the hook existed; every call 400'd and was swallowed by the outer try/catch, so the "active clients using this stage" widget has always shown generic `Tenant #<id>` placeholders instead of real tenant names. Fixed by querying `id` instead. Also fixed a `Json`-typed `audit_events.details` column read with a small `StageAuditEventDetails` local interface (mirrors the `Json`-for-real-column-then-narrow-at-read-site pattern from EOS/academy batches) and cast it at its one `.tsx` call site (`AdminStageDetail.tsx`) rather than changing the public function signature. Baseline dropped 3415→3373 (42 targeted, no compensating increase, confirmed via regenerated + committed `lint-baseline.json`). Verified live via Playwright (SuperAdmin): `/admin/stages/5` (Mock Audit) — Documents tab shows real sync status (10/10 in sync, 5 template documents), Audit Log tab correctly renders the empty state — zero console errors throughout. |
| Phase 2.5 any-batch 8b | `@typescript-eslint/no-explicit-any` (140), `src/hooks/useStage*` sub-batch 2/2 (final) — `useStageTemplateContent.tsx` 34, `useStageAnalytics.tsx` 22, `useStageExportImport.tsx` 21, `useStageDuplication.tsx` 18, `useStageSimulation.tsx` 14, `useStageReplacement.tsx` 14, `useStageDependencies.tsx` 9, `useStageQualityCheck.tsx` 8 — closes out the entire `src/hooks/useStage*` cluster (182 findings total across both sub-batches) | ✅ Done | [#TBD] | **Found and fixed a real live bug**: `useStageAnalytics.tsx`'s "High-Risk Stages" query selected a nonexistent `stage_id` column from `package_builder_audit_log` — confirmed via direct schema read that the table instead uses `entity_id`/`entity_type` (same convention as `audit_events`), and via live SQL that `entity_type = 'stage'` rows store the stage id as a string in `entity_id`. Rewritten to `.eq('entity_type', 'stage').in('entity_id', ids.map(String))` with `parseInt(entity_id, 10)` at the consumer — this widget had silently returned zero data since it was built. **Found two real bugs, deliberately left unfixed and typed honestly instead of papered over with `any`:** (1) `useStageTemplateContent.tsx`'s `copyTemplateToOverrides` copies an `emails` row's `name`/`subject`/`content` into `package_stage_emails`, which has no such columns (it requires `email_template_id`/`trigger_type`/`recipient_type`, none derivable from the source row) — confirmed via schema read, cast via `as unknown as TablesInsert<'package_stage_emails'>[]` with an inline comment, since a real fix needs a product decision on how to map templates. (2) `useStageDuplication.tsx`'s "Duplicate Stage" and `useStageExportImport.tsx`'s "Import Stage" both insert into `stages` without an `id`, and `stages.id` has no default/sequence at the DB level (confirmed via `information_schema.columns` — `column_default: null`, `is_nullable: NO` — and `pg_get_serial_sequence` returning `null`) — both features have always failed with a NOT NULL violation. Cast via `as unknown as TablesInsert<'stages'>` in both files with matching explanatory comments; not fixed here since the real fix is a schema migration requiring its own audit entry per `AGENTS.md`'s guardrails, out of scope for a type-only batch. **New TS pattern this batch**: `useStageSimulation.tsx` hit "Type instantiation is excessively deep and possibly infinite" (TS2589) on a nested-embed `package_stages`→`packages` select — casting the query *result* doesn't help since the error occurs during the query builder's own generic inference before any downstream cast applies; fixed by passing the row type as an explicit second generic argument directly to `.select<QueryString, ResultType>(...)`, bypassing Supabase's automatic embed-inference algorithm entirely. Baseline dropped 3373→3233 (140 targeted, no compensating increase, confirmed via regenerated + committed `lint-baseline.json`). Verified live via Playwright (SuperAdmin): `/admin/stages` list, `/admin/stages/6` (ASQA Audit — Settings/Team Tasks/Documents/Audit Log/Usage tabs), and `/admin/stage-analytics`'s High Risk tab specifically — confirmed via the network log that the rewritten query hits `package_builder_audit_log` with `entity=eq.stage`/`entity_id` and returns 200 OK (not the pre-fix 400), rendering a correct empty state at both 30- and 90-day ranges; zero console errors across all pages/tabs (89 messages captured, 0 errors — the one pre-existing `unicorn-logo.svg` preload warning is unrelated and present repo-wide). `src/hooks/useStage*` is now fully clear of `no-explicit-any` (0 findings, confirmed via `npx eslint`). |
| Phase 2.5 any-batch 9a | `@typescript-eslint/no-explicit-any` (58), `src/components/client` sub-batch 1/? — 27 smallest files (1-4 findings each): `BulkUploadDialog.tsx`, `ClientEmailsTab.tsx`, `ClientTopbar.tsx`, `CreateActionDialog.tsx`, `LegacyDataDiagnostics.tsx`, `MembershipWeightsPanel.tsx`, `PeriodSelector.tsx`, `StageDocumentsSection.tsx`, `TaskNotesPopover.tsx` (1 each); `AuditProgressCard.tsx`, `ClientTimeSummaryCard.tsx`, `ComposeEmailDialog.tsx`, `ReassignConsultantDialog.tsx`, `StageDetailSection.tsx`, `TenantUsersPreviewCard.tsx`, `TimeLogDrawer.tsx` (2 each); `BulkReassignCscDialog.tsx`, `ClientActionItemsTab.tsx`, `ClientAuditReportsSection.tsx`, `ClientAuditsTab.tsx`, `PackageTimeSection.tsx`, `SharePointFolderDialog.tsx`, `TenantInviteDialog.tsx`, `TenantTimeTrackerBar.tsx`, `XeroCard.tsx` (3 each); `NewConversationDialog.tsx`, `StaffTaskActionMenu.tsx` (4 each) | ✅ Done | [#TBD] | First batch of a new directory cluster — a full `npx eslint 'src/**/*.{ts,tsx}'` ranking pass (now that `src/hooks/academy` and `src/hooks/useStage*` are both fully closed) surfaced `src/components/client` as by far the largest remaining concentration: 350 findings across 44 files, roughly 2x `useStage*`'s size. Took the 27 smallest files first (1-4 findings each), same smallest-first approach as every prior sub-batch-1. Overwhelmingly the same two mechanical patterns as prior batches: `catch (err: any)` → `catch (err)` with `instanceof Error` narrowing (by far the most common finding this batch), and dropping `(supabase as any)`/`'<table>' as any` casts on tables already present in generated types (`client_audits`, `package_instances`, `stage_instances`, `package_renewal_periods`, `tenant_messages`, `bulk_document_job_items`, `users` — all confirmed via generated types before removing). Two nested-embed findings hit the same TS2589 "excessively deep" error as batch 8b's `useStageSimulation.tsx` — `StageDocumentsSection.tsx`'s `bulk_document_job_items`→`bulk_document_jobs!inner(status)` join and `TenantUsersPreviewCard.tsx`'s `tenant_users`→`users:user_id(...)` join — both fixed with the same `.select<QueryString, ResultType>()` explicit-generic-argument pattern rather than a fresh `as any`, confirming that pattern generalizes beyond the one file it was discovered in. No new bugs found this batch (every removed cast covered a table/column/RPC that already existed and behaved correctly) — a difference from every `useStage*` batch, consistent with `src/components/client` being longer-established, more heavily-trafficked code. Baseline dropped 3233→3175 (58 targeted, no compensating increase, confirmed via regenerated + committed `lint-baseline.json`). Verified live via Playwright (SuperAdmin) against a real client (Australian College Pty Ltd, `/tenant/7512`) on the staff Client Detail page: Overview, Time, Action Items (including the Add Action and task-kebab dialogs), Audits, Emails, Users (including the Reassign Consultant dialog — **correction, 2026-09-07, Phase 2.6 P6-B**: this was almost certainly `BulkReassignCscDialog.tsx`, also typed in this same batch and genuinely live in `ManageTenants.tsx`; `ReassignConsultantDialog.tsx`'s only caller had already been removed 11 days earlier, 2026-08-27, `235c3a3ce` — it was retired as confirmed-dead code in P6-B, see the dead-code register's "Reassignment island" entry), Integrations (SharePoint + Xero cards), Packages (stage detail + documents sub-tabs), and Documents (bulk upload dialog — also since confirmed dead and retired in P6-B) — zero console errors across ~180 captured messages. **Coverage gap, honestly disclosed:** `ClientTopbar.tsx` is client-portal-side (the notification bell a client sees in their own portal, not the staff Client Detail page) and wasn't reached this pass — its one finding was a redundant `.map((n: any) =>` annotation on an already-correctly-typed array (`filteredClientNotifications`, itself derived from `useClientNotifications`'s already-typed `ClientNotification[]`), so risk is low, but it hasn't been live-verified; worth a quick client-portal check in a future batch. 293 findings remain across 17 files in `src/components/client` for subsequent sub-batches. |
| Phase 2.5 any-batch 9b | `@typescript-eslint/no-explicit-any` (85), `src/components/client` sub-batch 2/? — `AuditPreparationSection.tsx` 5, `ClientGovernanceDocumentsPage.tsx` 5, `ClientProfileForm.tsx` 6, `ClientUpcomingAuditSection.tsx` 6, `SharePointFolderConfig.tsx` 6, `ClientStructuredNotesTab.tsx` 10, `PackageStagesManager.tsx` 10, `ClientPackagesTab.tsx` 11, `EditTimeDialog.tsx` 12, `TenantUsersTab.tsx` 14 | ✅ Done | [#TBD] | Continues the mechanical/nested-embed pattern mix from 9a: most findings were `catch (err: any)` → `instanceof Error` narrowing and dropping `(supabase as any)`/`'<table>' as any` casts on real tables (`client_audits`, `package_instances`, `stage_instances`, `audit_appointments`, `users`, `dd_work_types`, `dd_work_sub_type`, `v_client_governance_documents`), all confirmed present in generated types before removing. One dynamic-table-name hook (`ClientProfileForm.tsx`'s `useLookupTable`) was typed with a literal union (`'dd_org_type' \| 'dd_sms' \| 'dd_lms' \| 'dd_accounting_system'`) instead of a bare `string`, since Supabase's `.from()` requires a real table-name literal. **Two more TS2589 "excessively deep" nested-embed errors** (now 4 total across the phase, all `tenant_users`→`users:user_id(...)`-shaped or similar), fixed with the same `.select<QueryString, ResultType>()` pattern from batches 8b/9a — `TenantUsersPreviewCard.tsx` in 9a and `EditTimeDialog.tsx` here both hit it on a `tenant_users` embed specifically, suggesting that particular join shape is the recurring trigger. **One more real fallout bug**: `ClientStructuredNotesTab.tsx`'s `ApiComment` interface (declared in this same file) was missing a `task_id` field that the code actually reads off `clickup_task_comments` rows — surfaced only when the mechanical `as any[]` → `as ApiComment[]` cast made TS check the real interface against real usage; fixed by extending the type at the one call site (`ApiComment & { task_id: string }`) rather than touching the shared interface's broader shape. No net-new runtime bugs found in this batch beyond that type gap — everything else was casts over already-correct tables/columns. Baseline dropped 3175→3090 (85 targeted, no compensating increase, confirmed via regenerated + committed `lint-baseline.json`). Verified live via Playwright (SuperAdmin) against a real client (HPA Training Pty Ltd, tenant 6278): Overview/Profile (SMS/LMS/Accounting dropdowns, Sync Contact Details menu), Packages (stage list, Manage dropdown), Notes (filters, ClickUp feed, Add Note dialog), Integrations (SharePoint Governance/Shared Folder sections), and Users (7-member list, Position Type combobox) all rendered with zero console errors. **One pre-existing bug found, deliberately left unfixed**: opening `EditTimeDialog.tsx`'s "Edit Time Entry" produces a 400 from `tenant_users?select=user_uuid,users:user_uuid(...)`, leaving the Person field showing a placeholder instead of the real assignee. Confirmed via generated-types schema read that `tenant_users.user_id` has no FK relationship to `public.users` at all (only to `dd_position_type`, `dd_relationship_role`, `tenants`) — the same root-cause class as the `conversation_participants.user_id`→`auth.users` FK gap AGENTS.md already documents, so PostgREST has never been able to resolve this embed. The query string is byte-identical before/after this batch's change (only a compile-time generic argument and an `as any` removal, neither alters the runtime request), and the surrounding code never checked `error` on this call either — so real time-log edits have silently shown the wrong/blank person since this dialog was written, unrelated to today's typing work. Left as-is (needs either a two-step fetch like `ClientEmailsTab.tsx`'s existing pattern, or an actual FK, both out of scope for a type-only batch) — worth a follow-up ticket. `AuditPreparationSection.tsx`/`ClientGovernanceDocumentsPage.tsx` (client-portal side) not covered this pass, same as `ClientTopbar.tsx` in 9a. **Multi-agent coordination note**: this batch ran under a newly-installed shared heavy-command lock (`UNICORN-PHASE-LOCK/with-heavy-lock.ps1`) coordinating with a concurrent Phase 2.6 dead-code investigation in a separate worktree — every eslint/typecheck/test invocation from `PackageStagesManager.tsx` onward went through the lock wrapper; see AGENTS.md or a future entry for whether this becomes standing practice. 208 findings remain across 12 files in `src/components/client`, largest: `ClientTimeTab.tsx` 71 (not yet fixed). |
| Phase 2.5 any-batch 9c | `@typescript-eslint/no-explicit-any` (136), `src/components/client` sub-batch 3/? — `StartPackageDialog.tsx` 16, `PackageDataManager.tsx` 18, `RenewalConfirmDialog.tsx` 24, `AddTimeDialog.tsx` 19, `ClientMessagesTab.tsx` 28, `ClientIntegrationsTab.tsx` 31 | ✅ Done | [#TBD] | **Liveness check before starting**: cross-checked all six files against the Phase 2.6 dead-code-feature-consolidation-investigation register on `origin/main` — none listed as retirement candidates; confirmed live import/route usage via grep (`ClientMessagesTab`/`ClientIntegrationsTab` render as `ClientDetail.tsx` tabs directly; the rest are imported by already-verified-live `ClientPackagesTab.tsx`). Noted for the record: two files fixed in batch 9a (`ReassignConsultantDialog.tsx`, `BulkUploadDialog.tsx`) **are** listed as Phase 2.6 second-wave candidates — harmless either way (type-only, listing isn't deletion approval), flagged rather than silently omitted. **First batch run fully under the shared heavy-command lock** (`UNICORN-PHASE-LOCK/with-heavy-lock.ps1`), coordinating with two concurrent Codex Phase 2.6 cohorts (admin-package retirement, then tenant-detail retirement + EOS-issues retirement) in separate sibling worktrees — real lock contention was observed once (Codex briefly held the lock while this session tried to start its own dev server) and resolved by waiting rather than forcing anything, per the coordination protocol. Mechanically similar to 9a/9b: `catch (err: any)` → `instanceof Error`, dropping casts on real tables, plus 2 more nested-embed TS2589 fallout fixes (`AddTimeDialog.tsx`, matching the now-established `.select<Query, Result>()` pattern). **One real bug fixed**: `AddTimeDialog.tsx`'s inline note-creation insert (fired when logging time with "+ New note") set the DB's legacy numeric `notes.user_id` with a string UUID and never set the required `created_by` — matched to `useNotes.tsx`'s canonical insert shape. **Two real bugs found, documented, left unfixed**: (1) that same insert also references `client_id` and `package_instance_id`, neither of which are real `notes` columns at all (confirmed via generated types — real columns are `package_id`/`parent_id`/`parent_type`) — this exact sub-flow has likely never succeeded; needs a product decision on the intended parent_type/parent_id mapping, out of scope for a type-only batch. (2) `useTgaRtoData.tsx`'s five `TGAQualification`/`TGASkillset`/`TGAUnit`/`TGACourse`/`TGATrainingPackage` interfaces were all missing a `scope_state` field (plus `usageRecommendation_raw`/`endDate_raw` on `TGAQualification`) that the hook's own sync mapper has always set at runtime — added the missing optional fields to those interfaces (a minimal, behavior-preserving type-only fix in a file outside this batch's own directory, required to remove the `any` casts honestly rather than re-adding them). `ClientIntegrationsTab.tsx`'s SuperAdmin TGA Debug Panel reads a loosely-shaped, versioned JSON blob with many optional camelCase/snake_case field pairs (written by different generations of the sync edge function) — typed as one explicit `TgaDebugPayload` interface covering every field actually read, rather than force-fitting a narrower shape. **A third pre-existing bug, found via this batch's own live verification, not fixed**: `AddTimeDialog.tsx`'s "Add Time" dialog 400s on open (`GET .../tenant_users?select=user_uuid,users:user_uuid(...)`) — confirmed via `git diff origin/main` that the query string is byte-identical before/after this batch's change (only the compile-time `any` cast/generic type argument changed, never the runtime request), so this is the exact same root-cause class as `EditTimeDialog.tsx`'s documented `tenant_users`→`users` FK gap from batch 9b, now found in a sibling file. The dialog itself still renders and a plain time entry (without the Notify recipient list, which the failed query was meant to populate) still works; only the Notify dropdown's data source is silently empty. Left unfixed, same reasoning as 9b: needs either a two-step fetch or an actual FK, out of scope for a type-only batch.

Baseline dropped 3090→2954 (136 targeted, no compensating increase, confirmed via regenerated + committed `lint-baseline.json`). Verified live via Playwright (SuperAdmin) across three separate verification passes this batch: the two Codex Phase 2.6 cohorts (tenant-detail retirement PR #571, EOS-issues retirement) both independently verified PASS with zero console errors as a favor during the coordination session, plus this batch's own Packages/Time/Messages/Integrations flows (5 of 6 flows clean, the 6th being the AddTimeDialog.tsx bug documented above) — see execution-efficiency-log.md for full detail. `src/components/client` cluster now fully clear of `no-explicit-any` except `ClientTimeTab.tsx` (71 findings, the single largest file in the cluster) — likely its own sub-batch 9d. |
| Phase 2.5 any-batch 9d | `@typescript-eslint/no-explicit-any` (71), `ClientTimeTab.tsx` — the single largest file in `src/components/client`, closing the cluster entirely (350 findings across 44 files, batches 9a-9d) | ✅ Done | [#575](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/575) | Same mechanical patterns as 9a-9c: dropping `(supabase as any)`/`'<table>' as any` casts on tables already covered by generated types (`package_renewal_periods`, `users`, `package_instances`, `packages`, `notes`, `time_entries`) and untyped-callback-parameter removal once the underlying query was properly typed. No TS2589 fallout this time — this file's embeds were already typed or absent. Two typecheck-fallout type-accuracy fixes, neither a behavior change: `PackageBurndownCards`' `row = burndownMap[instId] || {}` fallback collapsed the union to `{}` once the map's value type became concrete (TypeScript requires a property to exist on every union member for direct access) — replaced with optional chaining (`row?.used_minutes ?? 0`) instead of an object-literal fallback; and `linkedNoteMap`'s local type wrongly declared `id: number` when `notes.id` is actually `string` (UUID) per generated types — the one consumer already defensively wrapped it in `String(...)`, so this was a pure type-accuracy correction. No new runtime bugs found in this file — a contrast with 9b/9c, which each surfaced one. Baseline dropped 2954→2830 (71 targeted via this file's own `lint:ratchet` result of 71→0 with no regressions; the total also reflects three unrelated Codex Phase 2.6 dead-code-retirement merges landing on `origin/main` mid-batch — see the Phase 2.6 section below). Verified live via Playwright (SuperAdmin, subagent) against two tenants (7377 single-package, 7408 multi-package with 4 active packages, needed to exercise Move/Split/Reallocate which are conditionally rendered): all 10 checked flows passed — table/pagination/filters/summary strip, burndown cards, closed-package summaries, renewal history, Move/Split/Reallocate dialogs (cancelled cleanly), billable toggle (toggled and reversed, confirmed reversible), Log Time dialog. Two pre-existing console items observed, both confirmed unrelated via diff review (no dialog JSX changed in this batch): a Radix `DialogTitle` a11y warning on Move/Split/Reallocate, and the already-documented `tenant_users` 400 on Add Time. **`src/components/client` cluster now fully closed** — zero `no-explicit-any` findings remain in it. |
| Phase 2.5 any-batch 10 (10a/10b/10c) | `@typescript-eslint/no-explicit-any` (98), `src/components/audit` — a new cluster (staff-facing Audit workspace), closed entirely across three sub-batches: 10a (50, [#581](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/581)) `ReportTab.tsx`/`NewAuditModal.tsx`/`EvidencePanel.tsx`/`SendPreliminarySummaryDialog.tsx`; 10b (24, [#582](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/582)) `QuestionCard.tsx`/`AuditFormTab.tsx`/the three meeting-phase components/`AddFindingForm.tsx`; 10c (24, [#584](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/584)) `ActionsTab.tsx`/`OverviewTab.tsx`/`AuditSidebar.tsx`/`ActionDrawer.tsx`/`FindingCard.tsx`/`ActionCreateForm.tsx`/`DeleteAuditDialog.tsx`/`EvidenceRequestsSection.tsx`/`SendEvidenceRequestDrawer.tsx` | ✅ Done | [#581](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/581), [#582](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/582), [#584](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/584) | **Liveness check before starting**: excluded `AuditQuestionCard.tsx`/`AuditSectionNav.tsx` (4 findings) as exclusive to the fully-dead legacy `Audits.tsx`/`AuditWorkspace.tsx`/`AuditWorkspacePlaceholder.tsx` island; kept `FindingCard.tsx`/`ActionCreateForm.tsx` in scope since they're exclusive to the *reachable* (though legacy-contract) `/audits/:id/findings`/`/actions` routes the Phase 2.6 register explicitly says not to skip merely for looking stale. `ClientAudit` and `AuditResponse` (`src/types/clientAudits.ts`/`src/types/auditWorkspace.ts`) were both significantly stale versus their real DB schemas — extended with ~14 missing real columns across the two interfaces in one pass each, rather than patching field-by-field per file as each cast was found (same approach as batch 9c's `useTgaRtoData.tsx` fix). Defined two shared cross-file callback types (`UpsertResponseInput`, `AddFindingInput`) once and reused via `import type` across a 5-file call chain (`AuditFormTab.tsx` → the three meeting-phase components → `QuestionCard.tsx` → `AddFindingForm.tsx`). **One real fallout bug fixed**: `FindingsTab.tsx` (0 findings of its own, never directly touched) never explicitly set `audit_id` when creating a finding — invisible while the upstream callback was `any`-typed, a real type error once it wasn't; fixed with the same explicit `audit_id: auditId` pattern already needed at two other call sites, confirmed a true no-op via live Playwright. **One dead-field finding, documented not fixed**: `ReportTab.tsx`'s due-diligence "Purchaser" field read a `snapshot_purchaser_name` column that has never existed in `client_audits` — the UI already self-documented this ("See header for live purchaser name"), so the honest fix was a literal `—` plus a comment, not a schema change. Baseline dropped 2830→2709 across the three sub-batches (98 targeted total, each sub-batch's own `lint:ratchet` result independently confirmed 0 regressions; per the 9d-established practice, the repo-wide total isn't hand-reconciled against Codex's concurrent Phase 2.6 dead-code merges landing on `origin/main` throughout this batch). **Process note**: reusing one branch across 10a's and 10b's merged PRs left a stale merge-base that GitHub refused to auto-merge for 10b (a real `lint-baseline.json` conflict, resolved by merging `origin/main` in and regenerating) — 10c branched fresh off `origin/main` instead and merged cleanly; branching fresh per sub-batch PR is now the standing practice (see efficiency log). Verified live via Playwright (SuperAdmin, subagent) across all three sub-batches on 4 real audits, covering: New Audit modal (all 3 steps, RTO and Due Diligence types), Report tab (readiness/generation/AI-summary/editable fields/release), evidence linking and preliminary-summary composition, full phase-by-phase Form tab navigation, raising/editing/deleting a real finding end-to-end (including the `audit_id` fallout fix's call sites), Overview/Sidebar/Actions tabs, evidence requests, and the two still-live legacy pages (graceful empty states, no crash) — zero regressions found across all three passes. **`src/components/audit` cluster now fully closed** (94 of 98 findings — the 4 legacy-only-file findings remain, deliberately out of scope pending Phase 2.6's audit-route-convergence decision). As a coordination favor mid-batch, also ran an independent read-only Playwright characterization pass for Codex's Phase 2.6 "Audit route convergence" investigation ([#583](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/583)) — confirmed the three legacy `/audits/:id/{findings,actions,report}` routes `parseInt()` real UUID audit IDs and always render broken/empty legacy pages regardless of real data, while the same audits' in-page tabs on `AuditWorkspaceNew` work correctly; no security issue (no unauthorized content flash on invalid/malformed IDs); zero writes. |
| Phase 2.5 any-batch 11 (11a/11b/11c) | `@typescript-eslint/no-explicit-any` (156), `src/hooks/usePackageBuilder.tsx` + `src/components/package-builder/**` — a new cluster (the Package Builder admin feature), closed entirely across three sub-batches: 11a (79, [#590](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/590)) `usePackageBuilder.tsx` in full; 11b (35, [#592](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/592)) `StageDetailPanel.tsx`/`StagePreviewDialog.tsx`; 11c (42, [#594](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/594)) `PackageBuilderOverview.tsx`/`PackageBuilderEditor.tsx`/`StageDocumentsTab.tsx`/`BulkGenerateDocumentsDialog.tsx`/`PackagePhasesTab.tsx`/`CreatePackageDialog.tsx`/`StageLibraryDialog.tsx` | ✅ Done | [#590](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/590), [#592](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/592), [#594](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/594) | **Two real, previously-broken bugs fixed in 11a**: `createPackage()`/`createStage()` never supplied an `id` on insert — `packages.id`/`stages.id` have no DB default/identity/sequence (confirmed via `information_schema.columns` + `pg_trigger`), so "Create Package" and "Create New Stage" had never actually worked through the UI; fixed with the same `MAX(id)+1` pattern `duplicatePackage()` already used correctly. Verified live: created and deleted a real test package and a real test stage via the UI, then separately re-simulated both inserts via direct SQL and confirmed the resulting rows are structurally identical to the reference rows created by the 2026-08-07 `academy_seed_superhero_sidekick_packages` migration and the untracked 2026-06-26 "V2" package/stage batch (same slug auto-generation, same column defaults) — both test rows deleted immediately after, confirmed via `select count(*)`. **Two more real bugs fixed in 11c**: `PackageBuilderOverview.tsx`'s readiness query never selected `stage_type`/`stage_key`, so onboarding/offboarding/documentation readiness badges always evaluated against `undefined`; `PackageBuilderEditor.tsx`'s "propagate hours to active instances" filtered `package_instances` by a nonexistent `status` column instead of the real `is_active`/`is_complete` booleans, so the action had always silently matched zero rows. **Real bugs found and deliberately left unfixed** (schema/product decisions, out of scope for a type-only batch): `archivePackage()` sets `status: 'archived'`, but the `packages_status_check` CHECK constraint only allows `'active'`/`'inactive'` — confirmed live via a real `23514` violation on every Archive click; and `StagePreviewDialog.tsx`'s `doc_name`→`title` column fix (11b) was necessary but not sufficient — all four of its queries also embed `packages:package_id (name)`, and none of `documents`/`package_staff_tasks`/`package_client_tasks`/`package_stage_emails` actually has a foreign key from `package_id` to `packages` (confirmed via `pg_constraint`), so the dialog has never shown real data in any of its four sections. All four bugs, plus the corroborating `useStageExportImport.tsx` "Import Stage" bug from batch 8b, are written up in [`codebase-optimization/phase-2-6-stabilization/l10-real-bugs-found.md`](codebase-optimization/phase-2-6-stabilization/l10-real-bugs-found.md) for Carl's Monday L10 report — the user explicitly asked "how did this happen, we were able to create stages and packages recently," which the historical-migration investigation directly answers: every "recent" package/stage anyone remembers was created via raw SQL against the database, never through the app, and (for the 2026-06-26 V2 batch specifically) not even through a tracked migration. New TS pattern this cluster: 11c's `BulkGenerateDocumentsDialog.tsx` hit the same TS2589 "excessively deep" nested-embed error as batches 8b/9a/9b/9c on a `client_package_stage_state`→`tenants(id, name)` join — fixed with the same now-well-established `.select<QueryString, ResultType>()` explicit-generic pattern. Baseline dropped through the cluster (regenerated + committed `lint-baseline.json` each sub-batch, final count 2552 post-11c). Verified live via Playwright (SuperAdmin, subagent) across all three sub-batches, including 11c's own pass on PR #594: package list (45 packages, stat tiles), readiness badges (confirmed genuinely computed — most packages "Incomplete," two "Ready," not a blanket value), package editor (9-stage load, Phases tab), Stage Library dialog, Create Package dialog, and Stage Documents tab (0-doc and 9-doc cases) all rendered correctly with zero console errors attributable to this batch's files. **One more pre-existing bug found during this verification pass, documented not fixed** (added to [`codebase-optimization/phase-2-6-stabilization/l10-real-bugs-found.md`](codebase-optimization/phase-2-6-stabilization/l10-real-bugs-found.md)): `BulkGenerateDocumentsDialog.tsx`'s tenant-fetch query embeds `client_package_stage_state:tenant_id, tenants(id, name)`, but that table has no FK to `tenants` at all (confirmed via `pg_constraint`) — every real invocation 400s; confirmed via `git show` that the identical query string existed before this batch's change, so this is not a regression. Two harmless pre-existing console items also observed and confirmed unrelated via diff (a `useStageVersions.tsx` 406 on stages with zero published versions; the well-known `unicorn-logo.svg` preload warning). `src/hooks/usePackageBuilder.tsx` + `src/components/package-builder/**` cluster now fully closed (0 `no-explicit-any` findings, confirmed via `npx eslint`). **Phase 2.5 exit-checkpoint note**: this is not the final Phase 2.5 batch — a fresh repo-wide scan immediately after this batch (`npx eslint` for `@typescript-eslint/no-explicit-any` across `src/**` and `supabase/functions/**`) found 2545 findings remaining across 419 files, so Phase 2.5's exit gate (§8) has not been reached; see the execution-efficiency-log for the full deferred-findings ranking used to select the next batch. |
| Phase 2.5 any-batch 12 | `@typescript-eslint/no-explicit-any`, `src/pages/TeamCommunicationsPage.tsx` | ✅ Done | [#598](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/598) | Real bug found: `sendMessage`'s tenant_id could silently resolve to `undefined` on a bad selection path, sending a message with no tenant scope — fixed with an explicit guard. Verified live via Playwright, zero console errors. |
| Phase 2.5 any-batch 13 | `@typescript-eslint/no-explicit-any`, `src/hooks/useAuditWorkspace.ts` | ✅ Done | [#600](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/600) | Verified live via Playwright against a real audit workspace, zero console errors. |
| Phase 2.5 any-batch 14 | `@typescript-eslint/no-explicit-any`, `src/pages/AdminStageDetail.tsx` | ✅ Done | [#601](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/601) | Verified live via Playwright, zero console errors. |
| Phase 2.5 any-batch 15 | `@typescript-eslint/no-explicit-any`, `src/hooks/useClientCommunications.ts` | ✅ Done | [#602](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/602) | Verified live via Playwright, zero console errors. |
| Phase 2.5 any-batch 16 | `@typescript-eslint/no-explicit-any`, `src/pages/ManageDocuments.tsx` | ✅ Done | [#603](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/603) | Real bug found and fixed: the category-badge lookup compared a real string-slug category id against `parseInt(id)` (always `NaN`), surfaced when retyping the `categories` state from a guessed `id: number` to the real `id: string` shape (`dd_document_categories.value`). Verified live via Playwright, zero console errors. |
| Phase 2.5 any-batch 17 | `@typescript-eslint/no-explicit-any`, `src/pages/TasksManagement.tsx` | ✅ Done | [#604](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/604) | Two dead code paths found and removed (client→package auto-fill; Followers/Assign-To avatar rendering) rather than type-cast around, after confirming via grep they were unreachable. Verified live via Playwright, zero console errors. |
| Phase 2.5 any-batch 18 | `@typescript-eslint/no-explicit-any`, `src/components/kpi/MyKpiSignOffSection.tsx` | ✅ Done | [#605](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/605) | Added local row interfaces for 9 KPI views/tables (`v_kpi_csc_summary`, `kpi_email_log`, `v_kpi_cst_summary`, `kpi_tasks`, `users`, `v_kpi_dev_summary`, `eos_rocks`, `kpi_dev_milestones`, `kpi_reviews`/`kpi_review_signoffs`); two documented `as unknown as X` casts kept for genuine literal-union narrowing. Verified live via Playwright, zero console errors. |
| Phase 2.5 any-batch 19 | `@typescript-eslint/no-explicit-any`, `src/hooks/useClientPackageInstances.tsx` | ✅ Done | [#606](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/606) | **The most significant real-bug find of this stretch**: `fetchPackageStages()` selected `stage_instances.status_id`, a column removed in an earlier stage-status consolidation (confirmed via `information_schema.columns` — real column is `status`, text) — every call had always thrown, silently caught, leaving `ClientPackageDetail.tsx` (a staff-only admin page, `/admin/client-packages/:id`, despite its client-sounding name — confirmed via `ProtectedRoute`'s deny-by-default logic and corrected in this PR's own description after an initial mischaracterization) with zero stages/tasks/documents/emails for every package instance, regardless of real content. Fixed with a `STAGE_STATUS_TO_LEGACY_ID` map deriving the legacy numeric id the UI still expects from the real status string (verified real distinct values live: `not_started`, `in_progress`, `completed`, `na`, `core_complete`, `monitor`). Verified live against Demo RTO's package instance 15201 (9 real stages, confirmed via SQL) — all 9 now render with real Team Tasks/Documents content; the `stage_instances` request itself confirmed 200 (previously 400) via network inspection. |
| Phase 2.5 any-batch 20 | `@typescript-eslint/no-explicit-any`, `src/components/help-center/MessageTab.tsx` | ✅ Done | [#608](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/608) | Verified live via Playwright, zero console errors. |
| Phase 2.5 any-batch 21 | `@typescript-eslint/no-explicit-any` (34), `src/hooks/useClientManagement.tsx` | ✅ Done | [#609](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/609) | `useClientManagement`/`useClientProfile`/`useClientPackages` — `entitlements`/`package_instances` embeds typed with `.select<Query, Result>()`; `TablesUpdate<'tenants'>`/`TablesUpdate<'tenant_profile'>` for profile-save payloads; object-literal lookup maps replaced with `Map`. Fixed one real type gap: `package_instances.id` (DB `number`) was being assigned to `ClientPackage.id` (`string`) via a masking cast — added an explicit `.toString()`. Verified live via Playwright (SuperAdmin, Demo RTO tenant 7547): profile, packages tab, and the id-to-string fix's navigation round-trip (`/admin/package/:id/tenant/:id/instance/:id`) all confirmed correct, zero console errors. |
| Phase 2.5 any-batch 22 | `@typescript-eslint/no-explicit-any` (67), audit evidence-request/reference/scheduling cluster — `useAuditPrep.ts` (23), `useAuditReferences.ts` (22), `useAuditSchedule.ts` (22) | ✅ Done | [#610](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/610) | All five/eight referenced tables already existed in generated types — casts were dead weight. Two real findings: `useAuditPrep.ts`'s `useGenerateRequestFromQuestions` had zero consumers anywhere (dead code) and its own latent bug (ordered by a nonexistent `compliance_template_questions.display_order`; real column is `sort_order`) — removed rather than fixed. `useAuditSchedule.ts`'s calendar-event creation for opening/closing meetings has **never once succeeded in production**: the insert omits `calendar_id`/`provider_event_id`, both `NOT NULL` with no default, silently swallowed by a try/catch — confirmed live (0 of 9,611 `calendar_events` rows have `provider = 'internal'`); documented, not fixed (needs a product/schema decision on those columns' source). Verified live via Playwright (SuperAdmin, real audit "Initial CHC audit" + a Due Diligence audit): Audits dashboard, audit detail (both audit types), per-client Audits tab, and a real Evidence Request with items all rendered correctly, zero console errors. |
| Phase 2.5 any-batch 23 | `@typescript-eslint/no-explicit-any` (30), `src/hooks/useDashboardTriage.ts` | ✅ Done | [#611](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/611) | All twelve referenced tables/views already existed in generated types; no real bugs found in this hook itself. **Separate, pre-existing production issue surfaced during live verification, not caused by this PR** (confirmed via `git diff` — query text byte-identical): `v_dashboard_attention_ranked`/`v_dashboard_priority_inbox`/`v_dashboard_behavioural_prompts`/`v_dashboard_labour_efficiency` are currently returning HTTP 500 (`57014` statement timeout) on the main staff dashboard (`/triage-dashboard`) — reproduced twice live. Fast (~267ms) as `postgres` (bypasses RLS); `authenticated` role has an 8s `statement_timeout`; likely an RLS-induced query-plan regression, not conclusively diagnosed as a real authenticated user in this session. Flagged to Carl directly via push notification and recorded in a follow-up docs PR ([#612](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/612), left for Carl's review, not auto-merged since it's outside the batch-verification auto-merge scope) rather than folded into this PR. |
| Phase 2.5 any-batch 24 | `@typescript-eslint/no-explicit-any` (40), EOS Rocks/Weekly Review — `useRockAnalysis.tsx` (20), `useWeeklyReview.ts` (20) | ✅ Done | [#613](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/613) | `useRockAnalysis.tsx` had no unsafe `.from()` casts — every finding was a redundant callback annotation; one was masking a real (harmless) type mismatch where `quarterMap` was typed with the full `RockOutcome` shape but only ever held the partial select-result shape — retyped to match. `useWeeklyReview.ts`'s tables already existed in generated types. Verified live via Playwright (SuperAdmin): Rock Analysis (6-quarter trend, seat-level flags) and a real client's Weekly Review draft both rendered correctly, zero console errors. |
| Phase 2.5 any-batch 25 | `@typescript-eslint/no-explicit-any` (26), `src/components/task-notes/useNoteMutations.ts` | ✅ Done | [#614](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/614) | `user_daily_notes` already existed in generated types; its `items` column is `Json` against the app's narrower `ChecklistItem[]`, so each payload casts just that field via `as unknown as Json` rather than the whole payload. **Live-verified with a real create/toggle/add-item/edit/delete round trip** (a personal per-user feature, safe to exercise with the acting SuperAdmin's own account) — confirmed the test note was fully cleaned up afterward, zero console errors beyond one pre-existing, unrelated Radix `DialogTitle` a11y warning. |
| Phase 2.5 any-batch 26 | `@typescript-eslint/no-explicit-any` (18), `src/hooks/usePackageStageOverrides.tsx` | ✅ Done | [#615](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/615) | `package_stage_emails.id` is a real `number` column (unlike its sibling `package_stage_documents.id`, which is `string`) — four `itemId as any` casts were masking this; fixed with `Number(itemId)` per call site, confirmed functionally a no-op (PostgREST already coerced the string query param the same way at the wire level). `OverrideItem.packageData`/`templateData` typed as `{ name?: string; [key: string]: unknown }` to match their one real consumer. Verified live via Playwright (SuperAdmin): Stage Detail panel, diff view (11 packages using the stage, 0 with overrides — confirmed accurate via SQL), and Stage Impact panel all rendered correctly; live schema check confirmed `package_stage_emails.id` is `bigint`, matching the fix's rationale. |
| Phase 2.5 any-batch 27 | `@typescript-eslint/no-explicit-any` (12), `src/hooks/useCeoDashboard.ts` | ✅ Done | [#616](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/616) | `financial_controls`, `client_commitments`, `ceo_decision_queue`, `tenants.tier`/`.status`, and the `kpi_score_rolling` RPC all already existed in generated types. Verified live via Playwright (SuperAdmin, `/executive`): all 8 CEO Dashboard panels plus the KPI Score summary (real RPC data, 35% overall) rendered correctly, zero console errors. |
| Phase 2.5 any-batch 28 | `@typescript-eslint/no-explicit-any` (17), `src/hooks/useResources.tsx` | ✅ Done | [#617](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/617) | `resource_library`/`resource_usage`/`resource_favourites` already existed in generated types; every finding was a redundant callback annotation. Verified live via Playwright with **both personas** (SuperAdmin admin pages + the Demo RTO client persona on `ClientResourceHubPage.tsx`) — both correctly render an empty state, confirmed accurate via a live `count(*)` (0 rows in `resource_library` production-wide), zero console errors. |
| Phase 2.5 any-batch 29 | `@typescript-eslint/no-explicit-any` (24), audit lifecycle + evidence-request lifecycle — `useClientAudits.ts` (12), `useEvidenceRequests.tsx` (12) | ✅ Done | [#618](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/618) | `useEvidenceRequests.tsx`'s deep `evidence_requests`→`users`(requester/assignee)→`evidence_request_items`→`portal_documents` embed fixed with the established `.select<Query, Result>()` pattern. Verified live via Playwright (SuperAdmin): Audits dashboard (19 real audits), two different audit-type detail pages, a per-tenant Audits tab, and a real Evidence Request with items all rendered correctly, zero 4xx/5xx anywhere in the session. |
| Phase 2.5 any-batch 30 | `@typescript-eslint/no-explicit-any` (30), `useStaffTaskInstances.ts` (15) + `useTeamsMeetingMinutes.ts` (15) | ✅ Done | [#619](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/619) | `useTeamsMeetingMinutes.ts`'s raw-fetch REST helpers (predating `meeting_minutes` existing in generated types) were left as-is to avoid a behavior change outside this batch's type-only scope; the one genuinely-untyped case (`(supabase as any).supabaseUrl`/`.supabaseKey`, real runtime properties not in the public `SupabaseClient` type) is now one narrow documented cast instead of scattered blanket `any`. Verified live via Playwright (SuperAdmin): a real stage's 34-task staff checklist rendered correctly (read-only); `useTeamsMeetingMinutes.ts`'s two raw REST calls confirmed 200 directly (no meeting exists in production within the panel's -14/+90-day window, confirmed via SQL — a pre-existing data gap, not a defect). |

**Phase 2.5 synchronization checkpoint (2026-09-05, refreshed):** Claude's
rolling `no-explicit-any` lane has advanced through batch 49 and PR [#645](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/645).
Fresh measurement at `main@eba9833` reports **1,417 baseline findings across
379 files**; PR-local totals are not interchangeable with this merged-main
measurement. Phase 2.5 is still open because the final scan, full verification
contract, Playwright evidence, and explicit disposition of remaining findings
have not been recorded as one exit checkpoint. PR [#636](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/636)
has merged its safe RPC-argument fix, but its live tenant-less staff
notification-preferences defect remains unresolved: 72 of 626 users have
`tenant_id IS NULL` while `user_notification_prefs.tenant_id` is `NOT NULL`.
That requires a product/schema decision. PR [#612](https://github.com/vivacityrto/unicorn-cms-f09c59e5/pull/612)
remains the documentation record for dashboard 500 findings.

**Council recheck (2026-09-05):** three components newly orphaned by the
legacy Audit retirement (`AuditInspectionsTable.tsx` 324 LOC,
`AuditNavCards.tsx` 81 LOC, and `AuditTemplatesTable.tsx` 300 LOC) are now a
separate Phase 2.6 candidate cohort. Package/stage authoring is promoted to a
Phase 4 reliability slice with a later Phase 7 schema candidate because
browser-side `MAX(id)+1`, missing IDs, invalid archive status, and missing-FK
embeds are contract problems, not cosmetic refactors. Phase 2.6 is
non-blocking with respect to the entire lint backlog: its task-dialog slice
may proceed after parity fixtures and file exclusion are established while
Claude continues Phase 2.5 independently.

**Phase 2.5 strategic-orchestration checkpoint (2026-09-06):** the
`supabase/functions/strategic-orchestration/index.ts` cohort removed 13
explicit-`any` findings (13→0) after a fresh reachability sweep confirmed the
configured Edge Function, the `/admin/strategic-orchestration` route, the
`useStrategicOrchestration` importer, and its existing SuperAdmin gate. The
change only narrows advisory priority/RPC row shapes; advisory priority writes
and refresh RPCs are unchanged, and no schema, RLS, or auth contract changed.
Lint ratchet passed 13→0; Edge 260 passed; frontend 298 passed/15 skipped;
build passed; KB links 672/672; typecheck retained only the documented
baseline errors in `ClientLayout.tsx` and `useKpiSummary.tsx`; authenticated
SuperAdmin Playwright passed 4/4 with zero application console errors. This
cohort is ready for its implementation PR and merge; no new L10 item was
found.

> **Phase 2.5 governance-document cluster checkpoint (2026-09-06):** `GovernanceDocumentEditDialog.tsx` and `GovernancePackageAssignments.tsx` are live from `GovernanceDocumentDetail.tsx` on the Documents surface. Fresh importer and generated-schema checks confirmed the `documents`, `stages`, `stage_instances`, `package_instances`, and `packages` read/update contracts. Eight explicit-any findings were removed (4→0 in each file) using generated row types and keyed form-state updates; document editing and assignment grouping behavior is unchanged. Lint ratchet passed (4→0 per file), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retains only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated SuperAdmin Playwright passed 1/1 after a cold-start retry, with no application console errors on the passing run; temporary auth/junction/spec/config artifacts and the Vite process were removed. No new L10 bug was found.

### Phase 2.6 — verified retirement and bounded consolidation (historical preparation; current status linked above)

> The preparation wording below predates the Phase 2.5 closeout and the later
> retirement cohorts. Preserve it as design history; execute against the
> current stabilization plan, which records delivered retirements, the
> approximate LOC accounting, P1-C proof, and remaining P3-A/P4-D/P6-B/P7
> packets.

**Parallel preparation checkpoint (2026-09-04):** while Phase 2.5 remains
active, read-only characterization for the next boundary and reliability
phases is recorded in [`codebase-optimization/phase-3/parallel-preparation-packets.md`](codebase-optimization/phase-3/parallel-preparation-packets.md).
It covers the Lifecycle Checklists Phase 3 pilot, auth/permission seam
inventory, package/messaging/document reliability design gates, and the
authenticated Edge title-extraction clone candidate. These packets do not
authorize runtime, schema, RLS, RPC, trigger, grant, or public-contract
changes; they exist to keep the CPU-heavy implementation lane focused while
Claude completes Phase 2.5.

The three-seat council investigation is complete and recorded in [`codebase-optimization/cross-cutting/dead-code-feature-consolidation-investigation.md`](codebase-optimization/cross-cutting/dead-code-feature-consolidation-investigation.md). **Checkpoint refreshed 2026-09-05:** all six original highest-confidence cohorts are implemented and merged through #570, #571, #574, #577, #579, and #588; #586 supplied the required Audit UUID route convergence before #588 removed the legacy island. The remaining Phase 2.6 queue is bounded consolidation plus the three newly orphaned Audit components, not an unrestricted deletion sweep. Phase 2.5 remains active through #645; declare its exit only after the final baseline, full verification contract, and Playwright evidence are recorded. Each future implementation PR must still regenerate import-graph, route, and metrics evidence from its own fresh branch-cut SHA; the register is evidence, not standing deletion authority, and GitHub `main` continues to advance past any snapshot.

**Concrete post-Phase-2.5 execution order:** (1) Codex characterizes `AddClientTaskDialog`/`AddStaffTaskDialog` without editing; (2) after the Phase 2.5 exit checkpoint, add focused parity fixtures for create/edit, validation, date offsets, reset/close, failures, owner behavior, and permissions; (3) extract a shared form/controller with thin client/staff adapters while preserving separate `package_client_tasks` and `package_staff_tasks` contracts; (4) run the full lint/type/test/build/Playwright contract and open one PR; (5) only after that cohort merges, evaluate the title-extraction pair, then `useStageQualityCheck`, then the SeatCard presentation core. Any RBAC, tenant-scope, schema, RLS, RPC, trigger, grant, or Edge-contract change exits Phase 2.6 and requires its own aligned vertical slice.

**Measured current checkpoint (2026-09-05, `main@eba9833`, `scripts/architecture-metrics.mjs`):** 1,735 tracked product files; 492,817 physical lines; 419,353 lines excluding generated Supabase types; 408,643 product lines excluding generated types and tests; 118 files over 600 LOC (116,250 lines held); 33 files over 1,000 LOC; 7 wrapper files (116 LOC). Boundary indicators: 107/223/271 Supabase imports and 96/181/240 direct calls (pages/components/hooks); 12 Zod-adoption files; 161 `unicorn_role` files; 1,668 raw `any` keyword hits. These figures are the baseline for the next consolidation decision; do not compare them to older ad-hoc tables without noting the script's exclusions and detection method.

Later batches diff against `lint-baseline.json`'s `byFile`/`byRule` data, not against re-derived counts, so "no compensating increase elsewhere" is checkable per the plan's Phase 2.5 exit gate (§8).
> **Phase 2.5 stage-notes checkpoint (2026-09-06, merged PR #774):** `StageNotesTab.tsx` is live from `AuditTemplateBuilder.tsx` and `PackageDetail.tsx`; fresh importer sweep confirmed the unified notes hook and existing upload/notification paths. Three explicit-any catch paths were narrowed to `unknown` with safe error extraction, preserving CRUD, timer, assignee, and attachment behavior. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright on `/admin/client-packages/15201` passed 1/1 with zero page/console errors under the mutex; temporary artifacts were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 profile-header checkpoint (2026-09-06, merged PR #776):** `ProfileHeader.tsx` is live from `UserProfile.tsx`; fresh importer and operation-path review confirmed avatar upload/delete plus status/archive mutations remain unchanged. Four explicit-any catches were narrowed to `unknown` with `Error`-checked fallback messages (4→0). Lint ratchet, frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented baseline errors. Authenticated read-only Playwright on `/settings?tab=profile` passed 1/1 with zero page/console errors under the mutex; temporary artifacts/auth copy were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 meeting-series checkpoint (2026-09-06, merged PR #778):** `useMeetingSeries.tsx` is live from the EOS meeting-series surface; fresh importer and generated-schema review confirmed `agenda_snapshot` persists as the existing `Json` column type. One `Record<string, any>` annotation was replaced with generated `Json` (1→0), preserving all query, RPC, mutation, and UI behavior. Lint ratchet, frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented baseline errors. Authenticated read-only Playwright on `/eos/meetings` passed 1/1 with zero page/console errors under the mutex; temporary artifacts/auth copy were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 client-package status checkpoint (2026-09-06, merged PR #780):** `ClientPackageDetail.tsx` is live at `/admin/client-packages/:clientPackageId`; fresh hook/signature and route review confirmed the existing stage and staff-task status mutation contracts. Two explicit-any casts were replaced with runtime guards for the existing status maps (2→0), preserving valid selections and safely ignoring impossible values. Lint ratchet, typecheck (documented baseline only), frontend 298/15 skipped, Edge 260, and build passed under the mutex. Authenticated read-only Playwright on `/admin/client-packages/15201` passed 1/1 with zero page/console errors; temporary artifacts/auth copy were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 bulk-message history checkpoint (2026-09-06, merged PR #782):** `BulkMessageHistory.tsx` is live from `TeamCommunicationsPage.tsx` at `/communications`; fresh generated-table review confirmed the `broadcast_campaigns`, `broadcast_recipients`, and `tenants` read contracts. Four `supabase as any` escapes were removed (4→0), preserving campaign and recipient activity rendering; one unrelated pre-existing `no-unused-expressions` lint error remains unchanged. Lint ratchet, typecheck (documented baseline only), frontend 298/15 skipped, Edge 260, and build passed under the mutex. Authenticated read-only Playwright on `/communications` passed 1/1 with zero page/console errors; temporary artifacts/auth copy were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 settings profile checkpoint (2026-09-06, merged PR #785):** `Settings.tsx` is live at `/settings`; fresh route and `TeamProfileFields` contract review confirmed profile, team, calendar, and notification paths. One team-user state annotation and two error catches were narrowed (3→0) with runtime guards for persisted JSON fields; profile update behavior is unchanged. Lint ratchet, typecheck (documented baseline only), frontend 298/15 skipped, Edge 260, and build passed under the mutex. Authenticated read-only Playwright on `/settings?tab=profile` passed 1/1 with zero page/console errors; temporary artifacts/auth copy were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 evidence-readiness checkpoint (2026-09-06, merged PR #787):** `EvidenceReadinessWidget.tsx` is live from `ExecutiveDashboard.tsx`; fresh generated `evidence_gap_checks` schema and read-only metric review confirmed the dashboard contract. Four explicit-any JSON/row annotations were replaced with generated `Json` and unknown-safe mandatory-category counting (4→0), preserving the 30-day metrics. Lint ratchet, typecheck (documented baseline only), frontend 298/15 skipped, Edge 260, and build passed under the mutex. Authenticated read-only Playwright on `/executive` passed 1/1 with zero page/console errors; temporary artifacts/auth copy were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 manage-categories checkpoint (2026-09-06, merged PR #789):** `ManageCategories.tsx` is live at `/manage-categories`; fresh route and `dd_document_categories` CRUD review confirmed the existing create/edit/delete contract. Four explicit-any catches were narrowed to unknown-safe error messages (4→0), preserving all database writes and UI behavior. Lint ratchet, typecheck (documented baseline only), frontend 298/15 skipped, Edge 260, and build passed under the mutex. Authenticated read-only Playwright on `/manage-categories` passed 1/1 with zero page/console errors; temporary artifacts/auth copy were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 admin-invite dialog checkpoint (2026-09-06, merged PR #772):** `AdminInviteUserDialog.tsx` is live from `ManageUsers.tsx`; fresh importer/generated-row sweep confirmed tenant-user occupancy fields and the existing `invite-user` Edge contract. Three explicit-any findings were replaced with inferred occupancy rows and unknown-safe error handling, preserving role-slot validation and invitation behavior. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright on `/manage-users` passed 1/1 with zero page/console errors under the mutex; temporary artifacts were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 tenant-drawer checkpoint (2026-09-06, merged PR #770):** `TenantDrawer.tsx` is live from `Dashboard.tsx`; fresh importer and producer-interface sweep confirmed typed tenant drivers, communication previews, and dashboard-event metadata. Four explicit-any display/callback annotations were replaced with bounded preview interfaces and `Record<string, unknown>`, preserving navigation and rendering. Lint ratchet passed (4→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright on `/dashboard` passed 1/1 with zero page/console errors under the mutex; temporary artifacts were deleted and port 8080 was free after teardown. No new L10 bug found.

> **Phase 2.5 suggestion-dropdown hook checkpoint (2026-09-06, merged PR #764):** `useSuggestDropdowns.ts` is live from package-builder, suggestion-detail, and support-ticket surfaces; fresh importer and generated-table sweep confirmed all ten lookup tables and the dynamic query contract. Two explicit-any findings were removed with an explicit table-name union and unknown-safe row normalization, preserving filters and option mapping. Lint ratchet passed (2→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright against production-backed `/email-triage` passed 1/1 with zero page/console errors under the mutex; temporary artifacts were deleted and port 8080 was free after teardown. No new L10 bug found.

> **Phase 2.5 production Edge verification methodology (2026-09-06):** A green
> local Edge test run is not live production evidence. For each merged Edge
> cohort, first confirm the deployed function is `ACTIVE` and its version
> timestamp is later than the merge. Then map the function to its real caller
> (route, hook, cron, or protected action) and classify the caller as
> read-only, write-capable, or cron-only. Use the authenticated SuperAdmin
> storage state against the local Vite client (hosted production Supabase),
> with the shared heavy-job mutex and port-8080 preflight. Exercise only
> read-only callers; for write/cron functions, record a deliberate non-run
> and use deployment/source/log evidence instead. If UI navigation is too
> conditional to reach a safe caller reliably, a temporary Playwright
> `page.evaluate(fetch)` may call only an explicitly read-only function with
> the existing authenticated session and publishable key; never log tokens or
> response PII. Query production Edge logs for the exact function paths after
> the run. A 2xx response is a pass; 4xx/5xx is recorded with the response
> body and escalated unless it is a documented authorization/contract gate.
> Delete temporary specs/configs and all browser artifacts after the run. No
> Demo RTO seed or cleanup is required for read-only calls; any write-path
> verification needs an explicit disposable Demo RTO fixture and post-run
> zero-delta cleanup before it can count as evidence.

> **Phase 2.5 lifecycle-hooks checkpoint (2026-09-06, pre-merge):** `useLifecycleChecklists.ts` was confirmed live from `LifecycleChecklistsAdmin.tsx` and its three generated dropdown tables plus `lifecycle_checklist_templates`. Eight redundant `no-explicit-any` findings were removed using generated Supabase table types; lint ratchet passed (8→0), and the regenerated baseline is 1,139 errors across 261 files with 39 warnings. Frontend tests (298 passed/15 skipped), Edge tests, and production build passed. Typecheck retains only the established baseline errors in `ClientLayout.tsx` and `useKpiSummary.tsx`. No new L10 bug was found.
> **Phase 2.5 hooks-cluster checkpoint (2026-09-06, merged in PR #696):** `useProgressAnchors.ts`, `useWinBanner.ts`, and `usePackagePhases.ts` were reduced by 19 `@typescript-eslint/no-explicit-any` findings using generated Supabase row/insert/update types. Fresh lint ratchet passes (7/6/6 errors to 0); regenerated baseline was 1,147 errors across 262 files (39 warnings). Frontend tests passed (298/15 skipped), Edge tests passed, and production build passed. Typecheck reported only the established pre-existing errors in `ClientLayout.tsx` and `useKpiSummary.tsx`. Unauthenticated Playwright smoke passed 12/12 desktop/mobile cases; authenticated state was unavailable in that isolated worktree.

> **Phase 2.5 lifecycle-hook checkpoint (2026-09-06, merged in PR #698):** `useLifecycleChecklists.ts` was confirmed live from `LifecycleChecklistsAdmin.tsx` and its generated lifecycle tables. Eight redundant `no-explicit-any` findings were removed using generated Supabase table types; lint ratchet passed (8→0), frontend tests passed (298/15 skipped), Edge tests and build passed, and authenticated SuperAdmin Playwright passed 4/4. Typecheck retained only the established `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. No new L10 bug was found.

> **Phase 2.5 momentum-hooks checkpoint (2026-09-06, pre-merge):** `useCopilot.ts`, `useGWCTrends.tsx`, `useMomentumPanel.ts`, and `useMomentumState.ts` were confirmed live through their current dashboard, Copilot, and EOS/GWC consumers. Eight redundant `no-explicit-any` findings were removed without changing endpoint or query behavior; lint ratchet passed (2→0 in each file), and the regenerated baseline is 1,131 errors across 257 files with 39 warnings. Frontend tests passed (298/15 skipped), Edge tests passed, production build passed, and authenticated SuperAdmin Playwright passed 4/4. The Demo RTO storage state is not present in the shared auth directory, so client-persona verification remains a documented coverage gap. No new L10 bug was found.

> **Phase 2.5 retention-forecast hook checkpoint (2026-09-06, pre-merge):** `useRetentionForecast.ts` is live through `CommercialRiskWidget.tsx`. Five redundant `no-explicit-any` findings were replaced with generated Supabase row types; lint ratchet passed (5→0), and the regenerated baseline is 1,126 errors across 256 files with 39 warnings. Frontend tests passed (298/15 skipped), Edge tests passed, production build passed, and authenticated SuperAdmin Playwright passed (3 tests passed, dashboard passed on retry after a cold-start timeout). Typecheck retains only the established `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. No new L10 bug was found.
> **Phase 2.5 team-leader dialog checkpoint (2026-09-06, pending merge):** `TeamLeaderEmailDialog.tsx` is live from `NewStarterWizard.tsx`. Five redundant explicit-any findings were replaced with an explicit error payload/unknown-error path; email function contract and UI behavior are unchanged. Lint ratchet passed (5→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 code-table dialog checkpoint (2026-09-06, pending merge):** `CodeRowDialog.tsx` is live from `CodeTablesAdmin.tsx`. Six dynamic-form explicit-any findings were replaced with a bounded form-value type and typed save payload; code-table CRUD contract and UI behavior are unchanged. Lint ratchet passed (6→0), frontend (298/15 skipped), Edge (260), build, and KB checks passed; typecheck retains only established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 tenant-packs hook checkpoint (2026-09-06, pending merge):** `useTenantPacks.tsx` is live through `GeneratePackDialog.tsx`. Six raw-table explicit-any findings were replaced with unknown-safe response and mutation payload boundaries; deployed `generate-pack` and pack event contracts are unchanged. Lint ratchet passed (6→0), frontend (298/15 skipped), Edge (260), build, and KB checks passed; typecheck retains only established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 package-assignment checkpoint (2026-09-06, pending merge):** `PackageInstanceAssignment.tsx` is live from `ClickUpImport.tsx`. Six explicit-any findings were replaced with bounded task and RPC result types while preserving existing ClickUp matching and assignment contracts. Lint ratchet passed (6→0), frontend (298/15 skipped), Edge (260), build, and KB checks passed; typecheck retains only established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 tenant-logo checkpoint (2026-09-06, pending merge):** `TenantLogoUpload.tsx` is live from `TenantDetail.tsx` and `ClientDetail.tsx`. Six storage/update/audit explicit-any findings were replaced with typed tenant update payloads and unknown-safe error handling; storage, tenant update, and audit-log behavior are unchanged. Lint ratchet passed (6→0), frontend (298/15 skipped), Edge (260), build, and KB checks passed; typecheck retains only established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 ClickUp mapping checkpoint (2026-09-06, pending merge):** `ClickUpTenantMapping.tsx` is live on the protected `/admin/clickup-mapping` route and lazy-loaded from `App.tsx`. Six explicit-any findings were replaced with generated mapping/tenant row types and typed mutation errors; mapping CRUD behavior is unchanged. Lint ratchet passed (6→0), frontend (298/15 skipped), Edge (260), build, and KB checks passed; typecheck retains only established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 email-attachments hook checkpoint (2026-09-06, pending merge):** `useEmailAttachments.ts` is live through `EmailAttachmentChips.tsx`. Five explicit-any findings were replaced with generated document/link/file row types; attachment ordering and selection behavior are unchanged. Lint ratchet passed (5→0), frontend (298/15 skipped), Edge (260), build, and KB checks passed; typecheck retains only established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 workflow-optimisation hook checkpoint (2026-09-06, pending merge):** `useWorkflowOptimisation.ts` is live through `WorkflowOptimisation.tsx` and `WorkflowEfficiencyWidget.tsx`. Five explicit-any filter callbacks were replaced with the existing `WorkflowSignal` type; signal aggregation behavior is unchanged. Lint ratchet passed (5→0), frontend (298/15 skipped), Edge (260), build, and KB checks passed; typecheck retains only established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 portfolio-health checkpoint (2026-09-06, pending merge):** `PortfolioHealthWidget.tsx` is live through `ExecutiveDashboard.tsx`. Five explicit-any snapshot/filter findings were replaced with the generated `stage_health_snapshots` row type; portfolio aggregation behavior is unchanged. Lint ratchet passed (5→0), frontend (298/15 skipped), Edge (260), build, and KB checks passed; typecheck retains only established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 workboard-hook checkpoint (2026-09-06, pending merge):** `useClientWorkboard.tsx` is live through six workboard components. Eight redundant `no-explicit-any` findings were replaced with generated Supabase row/summary types while preserving the legacy optional `tags` fallback; lint ratchet passed (8→0), frontend (298/15 skipped), Edge, and build passed. Typecheck retains only the established `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors; no new L10 bug was found.
> **Phase 2.5 assistant-KPI checkpoint (2026-09-06, pending merge):** `AssistantKpiCards.tsx` is live from `KpiPage.tsx`. Five redundant explicit-any findings were replaced with generated `tasks_tenants`, `client_action_items`, and `ops_work_items` row types; query and aggregation behavior are unchanged. Lint ratchet passed (5→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only the established `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug was found.
> **Phase 2.5 sync-ClickUp cohort checkpoint (2026-09-06, pending merge):** `supabase/functions/sync-clickup-tasks/index.ts` is invoked by `ClickUpImport.tsx` and protected by the existing `admin.team_users.manage` caller gate. Eleven explicit-any findings were replaced with bounded ClickUp API task/custom-field shapes and a typed Supabase client boundary; external API parsing, tenant resolution, and upsert behavior are unchanged. Lint ratchet passed (11→0), frontend (298/15 skipped), Edge (260), build, and KB links (672/672) passed; typecheck retains only the established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug was found.
> **Phase 2.5 vector-index rebuild checkpoint (2026-09-06, pending merge):** `supabase/functions/vector-index-rebuild/index.ts` is configured as a protected SuperAdmin/Ask Viv Edge route and performs the existing tenant-scoped vector delete/upsert and index-log writes. Twelve explicit-any findings were replaced with bounded index records and source-row types; embedding generation, authorization, deletes, upserts, and logging are unchanged. Lint ratchet passed (12→0), frontend (298/15 skipped), Edge (260), build, and KB links (672/672) passed; typecheck retains only the established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug was found.
> **Phase 2.5 stage-health monitor checkpoint (2026-09-06, pending merge):** `supabase/functions/run-stage-health-monitor/index.ts` is the configured nightly cron job and writes tenant-scoped stage-health snapshots before refreshing the existing trends RPC. Sixteen explicit-any findings were replaced with bounded stage/task/risk/gap/time-entry/snapshot types and an explicit dynamic RPC call; cron authorization, health calculations, batch upserts, and refresh behavior are unchanged. Lint ratchet passed (16→0), frontend (298/15 skipped), Edge (260), build, and KB links (672/672) passed; typecheck retains only the established baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug was found.
> **Phase 2.5 risk-command-engine checkpoint (2026-09-06, pending merge):** `supabase/functions/risk-command-engine/index.ts` is an active cron-gated Edge function; fresh source/config reachability confirmed it is configured and its alert/playbook writes were preserved. Eleven explicit-any findings were replaced with bounded alert, query-row, and JSON category/trigger types; no SQL, RPC, RLS, trigger, authorization, or write behavior changed. Lint ratchet passed (11→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only the established `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug was found.
> **Phase 2.5 strategic-signals checkpoint (2026-09-06):** `run-strategic-signal-analysis/index.ts` is cron-gated and configured; fresh source reachability found no UI caller or schema/auth contract change. Nine redundant callback annotations were removed (9→0) while preserving signal inserts and materialized-view refreshes. Lint ratchet passed (9→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 risk-forecast checkpoint (2026-09-06):** `run-tenant-risk-forecast/index.ts` is the configured nightly cron function; its cron authorization, tenant-wide forecast writes, history inserts, and refresh RPC are unchanged. Six explicit-any findings were removed (6→0): the results array is now bounded and query callbacks use inferred row shapes. Lint ratchet passed (6→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 TGA preview checkpoint (2026-09-06):** `tga-rto-preview/index.ts` is live through `AddTenantDialog.tsx`, `lookupTargetRto.ts`, and `TgaRtoLookupRow.tsx`; its external TGA fetch, validation, hash, and response contract are unchanged. Four redundant explicit-any callback annotations were removed (4→0). Lint ratchet passed (4→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 predictive-risk checkpoint (2026-09-06):** `calculate-predictive-risk/index.ts` is configured as a cron-gated function with unchanged signal calculation, snapshot inserts, audit-event logging, and refresh behavior. Four redundant explicit-any filter callbacks were removed (4→0). Lint ratchet passed (4→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 ClickUp-comments checkpoint (2026-09-06):** `fetch-clickup-comments/index.ts` is live from `ClickUpImport.tsx` and `ClientStructuredNotesTab.tsx`, with its existing Vivacity-team gate and ClickUp fetch/upsert contract unchanged. Eight explicit-any findings were replaced with a bounded recursive comment shape and unknown-safe rich-text extraction. Lint ratchet passed (8→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 Xero invoice-list checkpoint (2026-09-06):** `xero-invoice-list/index.ts` is live through `useXeroInvoiceList.ts`/`XeroCard.tsx`, with the existing `staffXeroView` caller gate and money-field redaction preserved. Five explicit-any findings were replaced with bounded Xero invoice, line-item, and payment shapes; no redaction or pagination behavior changed. Lint ratchet passed (5→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 scorecard-refresh checkpoint (2026-09-06):** `scorecard-refresh/index.ts` is a cron-gated EOS automation function; its metric resolution, scorecard entry upsert, and audit logging behavior are unchanged. The final explicit-any helper boundary was replaced with `ReturnType<typeof createClient>` (1→0). Lint ratchet passed (1→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 Xero invoice-status checkpoint (2026-09-06):** `xero-invoice-status/index.ts` is live through the Xero client-status UI and protected by the existing `staffXeroView` caller gate. One explicit-any status-row callback was replaced with a bounded row type; refresh, non-actionable-status filtering, and the intentionally redacted response contract are unchanged. Lint ratchet passed (1→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 TGA scope checkpoint (2026-09-06):** `tga-fetch-scope/index.ts` is configured, authenticated, and has no current local UI caller; its TGA scope pagination, page-size clamp, and response contract are unchanged. The one explicit-any collection was narrowed to `unknown[]` (1→0). Lint ratchet passed (1→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 Unicorn-1 user search checkpoint (2026-09-06):** `search-unicorn1-users/index.ts` is live from `InviteUserDialog.tsx` behind the existing `adminUnicorn1` caller gate and RPC boundary. The catch path now narrows `unknown` errors explicitly (1→0); search inputs, RPC arguments, and response behavior are unchanged. Lint ratchet passed (1→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 transcript-minutes checkpoint (2026-09-06):** `generate-minutes-from-transcript/index.ts` is live behind the existing `staffMeetings` caller gate and writes the existing AI-minutes contract. The token-usage result was narrowed from explicit `any` to a nullable bounded usage shape (1→0); transcript parsing, AI extraction, and persistence behavior are unchanged. Lint ratchet passed (1→0), Edge 260, frontend 298/15 skipped, build, and KB links 672/672 passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero application console errors. No new L10 bug found.
> **Phase 2.5 assigned-conversations hook checkpoint (2026-09-06, merged PR #728):** `useMyAssignedConversationsCount.ts` is live through `DashboardLayout.tsx`; fresh reachability confirmed the generated `tenant_conversations` table/columns and the existing realtime subscription/query contract. Removed two redundant explicit-any escape hatches (2→0) without changing filters, count semantics, or channel behavior. Lint ratchet passed (2→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Existing authenticated SuperAdmin Playwright suite passed 4/4. No new L10 bug found.
> **Phase 2.5 merge-fields hook checkpoint (2026-09-06, merged PR #730):** `useMergeFields.tsx` is live through `MergeFieldTagsAdmin.tsx` and `MergeFieldsEditor.tsx`; fresh reachability confirmed the `dd_fields` table and `v_tenant_merge_fields` view contracts. Five explicit-any findings were replaced with generated insert/update/row types and unknown-safe error handling; merge-field CRUD and tenant merge-data behavior are unchanged. Lint ratchet passed (5→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass because this cohort changes typing only and no route behavior. No new L10 bug found.
> **Phase 2.5 risk-forecast widget checkpoint (2026-09-06, merged PR #732):** `ClientRiskForecastWidget.tsx` is live from `ExecutiveDashboard.tsx`; fresh reachability confirmed the read-only `tenant_risk_forecasts` query and existing dashboard route. Two explicit-any findings were replaced with the generated row type without changing deduplication or distribution behavior. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 mini-KPI summary checkpoint (2026-09-06, merged PR #734):** `MiniKpiSummary.tsx` is live from `MainDashboard.tsx`; fresh reachability confirmed the existing read-only KPI queries for consultant and assistant summaries. Three explicit-any findings were replaced with inferred Supabase row types without changing period filters, deduplication, or KPI calculations. Lint ratchet passed (5→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 email-templates hook checkpoint (2026-09-06, merged PR #736):** `useEmailTemplates.tsx` is live from `ManageEmailTemplates.tsx` and `EmailTemplateEditorDialog.tsx`; fresh reachability confirmed the generated `email_templates` CRUD contract and existing `send-stage-email` invocation. Three explicit-any findings were replaced with typed update payloads and unknown-safe errors without changing template CRUD or send behavior. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 triage-staff hook checkpoint (2026-09-06, merged PR #738):** `useTriageStaffOptions.ts` is live from `TriageSidePanel.tsx`, `TicketDetailPanel.tsx`, and `AllTicketsTab.tsx`; fresh reachability confirmed the read-only `users` query and role filters. Two explicit-any findings were replaced with generated row typing without changing the staff-option contract. Lint ratchet passed (2→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 suggestion-attachments hook checkpoint (2026-09-06, merged PR #740):** `useSuggestAttachments.ts` is live from `SuggestionDetail.tsx` and `SupportTicketPortalDetailPage.tsx`; fresh reachability confirmed the `suggest_attachments` read/upload contract and storage path behavior. Two explicit-any findings were replaced with generated insert typing and the typed table query without changing attachment retrieval or upload behavior. Lint ratchet passed (2→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 strategic-orchestration hook checkpoint (2026-09-06, merged PR #742):** `useStrategicOrchestration.ts` is live from `StrategicOrchestrationDashboard.tsx` and `StrategicOrchestrationWidget.tsx`; fresh reachability confirmed the existing priority/decision queries and mutation contracts. Two explicit-any JSON arrays were narrowed to `unknown[]` without changing orchestration behavior. Lint ratchet passed (2→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only the documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 membership-state hook checkpoint (2026-09-06, merged PR #744):** `useMembershipStateOptions.ts` is live from `ClientPackagesTab.tsx` and `MembershipCommandBar.tsx`; fresh reachability confirmed the generated `dd_membership_state` lookup contract and existing filters. Three explicit-any findings were replaced with generated row typing without changing option mapping or completion filtering. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 RTO-tips hook checkpoint (2026-09-06, merged PR #746):** `useRtoTips.tsx` is live from `RtoTips.tsx`; fresh reachability confirmed the generated `rto_tips` and `users` query contracts and existing CRUD behavior. Three explicit-any findings were replaced with generated row inference without changing creator mapping or mutations. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 email-ticket lookup checkpoint (2026-09-06, merged PR #748):** `useEmailTicketCategories.ts` is live from the email-triage panels; fresh reachability confirmed both lookup-table query contracts and existing active-row filtering. Four explicit-any findings were removed by using generated table inference without changing category/status behavior. Lint ratchet passed (4→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 calendar-shares hook checkpoint (2026-09-06, merged PR #750):** `useCalendarShares.tsx` is live from `CalendarTab.tsx`; fresh reachability confirmed the calendar-share/user query contracts and existing share/revoke/audit behavior. Three explicit-any findings were replaced with inferred query rows and unknown-safe error handling without changing mutations. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 add-existing-stage checkpoint (2026-09-06, merged PR #752):** `AddExistingStageDialog.tsx` is live from `PackageDetail.tsx`; fresh reachability confirmed the stages lookup and tenant `stage_ids` update path. Three explicit-any error catches were narrowed to `unknown` without changing stage assignment behavior. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 add-existing-document checkpoint (2026-09-06, merged PR #754):** `AddExistingDocumentDialog.tsx` is live from `PackageDetail.tsx`; fresh reachability confirmed the document lookup, tenant-stage update, and additional-stage-link paths. Four explicit-any findings were narrowed to typed values and unknown-safe errors without changing assignment behavior. Lint ratchet passed (4→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 CSC-assignment hook checkpoint (2026-09-06, merged PR #756):** `useCscAssignments.ts` is live from the bulk document, governance, contact-directory, activity-feed, tenant-management, and tenant-user surfaces; fresh reachability confirmed generated assignment/user query inference. Three explicit-any findings were removed without changing tenant filtering or assignment mapping. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 admin-help-messages checkpoint (2026-09-06, merged PR #758):** `useAdminHelpThreadMessages.ts` is live from `AdminHelpThreadDetail.tsx`; fresh reachability confirmed the `help_messages`/`users` read contracts and sender-name mapping. Three explicit-any findings were narrowed to `unknown` metadata and inferred rows without changing message rendering. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 client-notifications hook checkpoint (2026-09-06, merged PR #760):** `useClientNotifications.tsx` is live through the client inbox/topbar/banner and notifications query paths; fresh reachability confirmed the `user_notifications` read/update contract and realtime subscription. Three explicit-any escape hatches were removed without changing notification filtering, read-state mutations, or invalidation behavior. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated Playwright is covered by the consolidated Phase 2.5 pass. No new L10 bug found.
> **Phase 2.5 email-tickets hook checkpoint (2026-09-06, merged PR #762):** `useEmailTickets.ts` is live from the email-triage tabs and detail panels; a fresh importer sweep confirmed the `email_tickets` read/update contract. Four explicit-any escape hatches were removed with generated table typing and unknown-safe update handling, preserving filters, pagination, and mutation behavior. Lint ratchet passed (4→0), frontend 298/15 skipped, Edge 260, and build passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright against production-backed `/email-triage` passed (1/1, zero page/console errors); temporary harness/config/auth artifacts were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 task-dialog error checkpoint (2026-09-06, merged PR #766):** `AddClientTaskDialog.tsx` and `AddStaffTaskDialog.tsx` are live from `PackageDetail.tsx`; fresh importer and parity review confirmed separate `package_client_tasks`/`package_staff_tasks` contracts and unchanged validation, date-offset, reset, and owner behavior. Two explicit-any catches were narrowed to `unknown` with `Error` checks (1→0 per file). Lint ratchet, frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright on `/admin/client-packages/15201` passed 1/1 with zero page/console errors under the mutex; temporary artifacts were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 meeting-drawer checkpoint (2026-09-06, merged PR #768):** `MeetingDetailDrawer.tsx` is live from `WorkMeetings.tsx`; fresh importer/type-interface sweep confirmed `Meeting` already declares all three Microsoft sync fields. Three redundant `any` casts were replaced with direct typed access, preserving the artifacts display contract. Lint ratchet passed (3→0), frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright on `/work/meetings` passed 1/1 with zero page/console errors under the mutex; temporary artifacts were deleted and port 8080 was free after teardown. No new L10 bug found.
> **Phase 2.5 Login error-boundary checkpoint (2026-09-06, PR #801):** Four Login.tsx catch values were narrowed from any to unknown with behavior preserved. Static gates passed and unauthenticated Playwright passed 12/12 across desktop/mobile flows after restoring declared dependencies. No L10 bug found.
> **Phase 2.5 KPI team read checkpoint (2026-09-06, PR #802):** `KpiTeamSection.tsx` narrowed five explicit-any findings to typed directory/profile rows and an unknown-safe RPC boundary. Static gates passed; baseline is 839 explicit-any / 846 total errors / 39 warnings across 194 files. Focused authenticated SuperAdmin `/kpi` Playwright passed 1/1 with zero page/console errors; temporary artifacts were removed and port 8080 was free.
> **Phase 2.5 compliance-pack hook checkpoint (2026-09-06, PR #803):** `useCompliancePacks.tsx` narrowed five explicit-any findings to typed read rows and unknown-safe errors. The hook includes write-capable export creation/audit and Edge invocation paths; authenticated SuperAdmin Playwright exercised only the initial read-only listing route and passed 1/1 with zero page/console errors and no mutation requests. Baseline is 834 explicit-any / 841 total errors / 39 warnings across 193 files.
> **Phase 2.5 package-usage read checkpoint (2026-09-06, PR #805):** `usePackageUsageQuery.tsx` narrowed four explicit-any findings in the read-oriented package-instance mapping. Threshold/dismissal RPCs remain unchanged and uninvoked. Baseline is 830 explicit-any / 837 total errors / 39 warnings across 192 files. Authenticated SuperAdmin read-only `/tenant/7547` Playwright passed 1/1 with no page/console errors; one non-error GoTrue multiple-client warning was observed.
> **Phase 2.5 document-request hook checkpoint (2026-09-06, PR #806):** `useDocumentRequests.tsx` narrowed five explicit-any findings in typed request-list rows and mutation error paths. Request creation/cancellation and notification RPC paths were not invoked. Baseline is 825 explicit-any / 832 total errors / 39 warnings across 191 files. Authenticated SuperAdmin client-shell Playwright passed 1/1 with zero page/console errors; temporary artifacts removed and port 8080 free.
> **Phase 2.5 Login error-boundary checkpoint (2026-09-06, PR pending):** `src/pages/Login.tsx` narrowed four explicit-any catch values to `unknown` using a shared error-message helper; existing fallback text and navigation behavior are preserved. Lint ratchet 4→0, frontend 298/15 skipped, Edge 260, build, and KB links 672/672 passed; typecheck retains only the documented baseline errors. Unauthenticated Playwright could not run because `@playwright/test` is absent from the available worktrees; this cohort remains unverified for browser purposes and must not be merged until a clean browser pass is available.
> **Phase 2.5 document-stage links checkpoint (2026-09-06, PR #807):** `DocumentAdditionalStagesField.tsx` removed four explicit-any casts with typed stage-link rows. Immediate add/remove writes were not invoked; authenticated SuperAdmin `/manage-documents` Playwright passed 1/1 with zero page/console errors. Baseline is 821 explicit-any / 828 total errors / 39 warnings across 190 files.
> **Phase 2.5 KPI reporter ticket checkpoint (2026-09-06, PR pending):** `KpiReporterTicketView.tsx` removed four explicit-any findings in read-only ticket/comms/user queries and metadata rendering. Baseline is 817 explicit-any / 824 total errors / 39 warnings across 189 files; authenticated browser verification is pending.
> **Phase 2.5 KPI reporter ticket checkpoint (2026-09-06, PR #808):** `KpiReporterTicketView.tsx` removed four explicit-any findings in read-only ticket/comms/user queries and metadata rendering. Authenticated SuperAdmin `/my/kpi` Playwright passed 1/1 with zero page/console errors. Baseline is 817 explicit-any / 824 total errors / 39 warnings across 189 files.
> **Phase 2.5 audit-completion hook checkpoint (2026-09-06, PR #809):** `useAuditCompletion.ts` removed four explicit-any findings across query-cache event handling and read-only completion views. Authenticated SuperAdmin `/audits` Playwright passed 1/1 with zero page/console errors. Baseline is 813 explicit-any / 820 total errors / 39 warnings across 188 files.
> **Phase 2.5 KPI review checkpoint (2026-09-06, PR pending):** `useKpiReview.tsx` removed six explicit-any boundaries across read/preview and write/sign-off/lock methods without changing contracts. Browser must exercise only read/preview. Baseline is 807 explicit-any / 814 total errors / 39 warnings across 187 files.
> **Phase 2.5 email-ticket detail checkpoint (2026-09-06, PR pending):** `TicketDetailPanel.tsx` narrowed four explicit-any payload/error boundaries while preserving save/close mutations. Baseline is 803 explicit-any / 810 total errors / 39 warnings across 186 files; authenticated browser verification is pending.
> **Phase 2.5 email-ticket detail checkpoint (2026-09-06, PR #811):** `TicketDetailPanel.tsx` narrowed four explicit-any payload/error boundaries while preserving save/close mutations. Authenticated SuperAdmin `/email-triage` Playwright passed 1/1 with zero page/console errors; no mutation invoked. Baseline is 803 explicit-any / 810 total errors / 39 warnings across 186 files.
> **Phase 2.5 note-form lookup checkpoint (2026-09-06, PR #812 merged):** `NoteFormDialog.tsx` removed four explicit-any lookup casts while preserving its write/title-extraction paths. Baseline is 799 explicit-any / 806 total errors / 39 warnings across 185 files. Authenticated SuperAdmin Playwright on `/tenant/7547` passed 1/1 with zero page or console errors; no writes invoked.
> **Phase 2.5 code-table boundary checkpoint (2026-09-06, PR #813 merged):** Fresh importer sweep confirmed `codeTablesService`/`useCodeTables` are active through CodeTablesAdmin, admin dialogs/grid, and CreatableSelect. Nine explicit-any findings were replaced with bounded scalar row values and Json RPC boundaries; CRUD RPC contracts and mutation behavior are unchanged. Lint ratchet, frontend (298/15 skipped), Edge (260), build, and typecheck (documented baseline only) passed. Regenerated baseline is 790 explicit-any / 797 total errors / 39 warnings across 183 files. Authenticated SuperAdmin Playwright on `/admin/code-tables` passed 1/1 with real data, zero page or console errors, and no writes.
> **Phase 2.5 notification boundary checkpoint (2026-09-06, PR #815 merged):** Fresh importer sweep confirmed `useNotifications` is active in dashboard/client notification surfaces and `noteNotifications` is called by `ClientStructuredNotesTab`; read, optimistic-update, insert, and `emit_notification` contracts were preserved. Eight explicit-any findings were replaced with generated notification/user row types and typed realtime/update/insert boundaries. Lint ratchet, frontend (298/15 skipped), Edge (260), build passed; typecheck retains only documented baseline errors. Regenerated baseline is 782 explicit-any / 789 total errors / 39 warnings across 181 files. Authenticated SuperAdmin Playwright passed on `/dashboard` and `/client/inbox?tab=notifications`, with no writes. A pre-existing ClientRouteGuard render-time navigation warning was observed and logged separately for L10 follow-up.
> **Phase 2.5 bulk-message boundary checkpoint (2026-09-06, PR #817 merged):** Fresh importer sweep confirmed `BulkMessageDialog.tsx` is rendered by the protected `/communications` route. Five explicit-any Supabase/catch boundaries were replaced with generated RPC/table inference and unknown-safe error handling; read preview, campaign insert, queue RPC, storage upload, and Edge send contracts were not changed. Lint ratchet, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only documented baseline errors. Regenerated baseline is 777 explicit-any / 784 total errors / 39 warnings across 180 files. Authenticated SuperAdmin Playwright reached `/communications` and opened the dialog, but exposed a pre-existing Radix accessibility console error (missing DialogTitle); no writes were invoked. L10 follow-up recorded.
> **Phase 2.5 meeting-AI summary checkpoint (2026-09-06, PR #818 merged):** Fresh importer sweep confirmed `MeetingAiSummaryPanel.tsx` is rendered by `MeetingDetailDrawer`. Five explicit-any summary-item render/edit annotations were replaced with bounded string/object unions and inferred callback parameters; summary generation, approval, task creation, and Edge invocation contracts were unchanged. Lint ratchet, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only documented baseline errors. Regenerated baseline is 772 explicit-any / 779 total errors / 39 warnings across 179 files. Authenticated SuperAdmin Playwright passed 1/1 on `/eos/meetings` with zero page/console errors and no writes. No completed meetings were present, so the nested summary-panel click path was not reachable; this coverage gap is recorded.
> **Phase 2.5 support-thread query checkpoint (2026-09-06, in progress):** Fresh importer sweep confirmed `useAdminHelpThreads` is active in SupportTicketsPage and client/admin message panels. Five explicit-any query/result annotations were replaced with generated support-user/message/thread types; support-thread filtering and read contracts are unchanged. Lint ratchet, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only documented baseline errors. Regenerated baseline is 767 explicit-any / 774 total errors / 39 warnings across 178 files. Authenticated browser verification pending.
> **Phase 2.5 client-audit portal checkpoint (2026-09-06, PR #820 merged):** Fresh importer sweep confirmed `useClientAuditReports` and `useClientActionPlanEnhanced` are active through `ClientAuditReportsSection`/`ClientHomePage`. Four explicit-any Supabase table/view boundaries were replaced with generated relation inference; report visibility, action-plan filtering, and tenant scoping are unchanged. Lint ratchet, frontend (298/15 skipped), Edge (260), build, and typecheck (documented baseline only) passed. Regenerated baseline is 763 explicit-any / 770 total errors / 39 warnings across 177 files. Authenticated SuperAdmin Playwright on `/client/home` passed 1/1 with zero page/console errors and no writes. PR #820 auto-merged; temporary artifacts removed and port 8080 free.
> **Phase 2.5 staff-onboarding status checkpoint (2026-09-06, verification complete):** Fresh importer sweep confirmed `StaffOnboardingBanner` as the active consumer. Four explicit-any checklist/prompt row and upsert boundaries were replaced with generated Supabase types; first-login RPC, dismissal mutations, and query behavior are unchanged. Lint ratchet 4→0, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only documented baseline errors. Regenerated baseline is 759 explicit-any / 766 total errors / 39 warnings across 176 files. Authenticated SuperAdmin `/dashboard` Playwright passed 1/1 with zero page/console errors and no writes; port 8080 free after teardown. PR pending creation.
> **Phase 2.5 invitation-validation checkpoint (2026-09-06, verification complete):** Fresh reachability confirmed `AcceptInvitation` is the public `/accept-invitation` surface. Four explicit-any RPC/result and catch boundaries were narrowed to an explicit token-result shape and `unknown` error handling; invitation validation, account creation, and finalization contracts are unchanged. Lint ratchet 4→0, frontend (298/15 skipped), Edge (260), build passed; typecheck retains only documented baseline errors. Regenerated baseline is 755 explicit-any / 762 total errors / 39 warnings across 175 files. Unauthenticated Playwright passed 12/12 desktop/mobile flows; zero writes.
> **Phase 2.5 client-inbox checkpoint (2026-09-06, verification complete):** Fresh reachability confirmed `ClientInboxPage` is the nested `/client/inbox` surface. Four explicit-any attachment/send/error boundaries were narrowed to inferred mutation results and unknown-safe messages; message, attachment, and notification contracts are unchanged. Lint ratchet 4→0, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only documented baseline errors. Unauthenticated Playwright remained green (12/12); the authenticated SuperAdmin persona suite passed all 10 available checks, while four client-persona checks were unavailable because local `client-demo.json` storage state was absent. No writes.
> **Phase 2.5 strategic-command checkpoint (2026-09-06, verification complete):** Fresh reachability confirmed `useStrategicCommand` is consumed by `/admin/strategic-command`. Four explicit-any signal/RPC/forecast boundaries were narrowed with generated Json-safe types and unknown casts; read-only dashboard queries and RPC names are unchanged. Lint ratchet 4→0, frontend (298/15 skipped), Edge (260), build passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin `/admin/strategic-command` Playwright passed 1/1 with zero page/console errors and no writes.
> **Phase 2.5 invite-user checkpoint (2026-09-06, verification complete):** Fresh reachability confirmed `InviteUserDialog` is active in `/manage-users` and `/admin/team-users`. Three explicit-any error boundaries were narrowed to unknown-safe messages; the cross-schema `unicorn1` client cast remains an intentional reviewed exception, and invite/import Edge and mutation contracts are unchanged. Lint ratchet 4→1, frontend (298/15 skipped), Edge (260), build passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin `/manage-users` Playwright passed 1/1 with zero page/console errors and no writes.
> **Phase 2.5 app-settings form checkpoint (2026-09-06, verification complete):** Fresh reachability confirmed `AppSettingsForm` is rendered by `/admin/code-tables`. Three generic form-value boundaries were narrowed to the existing `CodeTableValue` union; settings save/add callbacks and code-table RPC contracts are unchanged. Lint ratchet 4→0, frontend (298/15 skipped), Edge (260), build passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin `/admin/code-tables` Playwright passed 1/1 with zero page/console errors and no writes.
> **Phase 2.5 PackageDetail error-boundary checkpoint (2026-09-06, verification complete):** Fresh reachability confirmed `PackageDetail` at `/admin/package/:id`. Nine catch/error boundaries were narrowed to unknown-safe messages; package, tenant, stage, task, and document mutation/query contracts were not changed. Lint ratchet 41→32, frontend (298/15 skipped), Edge (260), build passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin `/admin/package/1` Playwright passed 1/1 with zero page/console errors and no writes. Remaining 32 findings are deferred to dedicated typed data-shape cohorts.
> **Phase 2.5 email-template error-boundary checkpoint (2026-09-06, verification complete):** Fresh reachability confirmed `ManageEmailTemplates` at `/admin/email-templates`. Four catch/error boundaries were narrowed to unknown-safe messages; create/update/duplicate/archive/activate mutation contracts are unchanged. Lint ratchet 4→0, frontend (298/15 skipped), Edge (260), build passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin `/admin/email-templates` Playwright passed 1/1 with zero page/console errors and no writes.
> **Phase 2.5 exit-interview boundary checkpoint (2026-09-06, PR #834 merged):** Fresh reachability confirmed `MyExitInterview` is lazy-loaded at protected `/my-exit-interview`. Four explicit-any JSON/error boundaries were narrowed using the generated `engagement_exit_interviews` Insert contract and unknown-safe messages; autosave/submit fields and mutation behavior are unchanged. Lint ratchet 4→0, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated SuperAdmin read-only Playwright passed 1/1 with zero page/console errors; no writes. Regenerated baseline after merge: 723 explicit-any / 730 total errors / 39 warnings across 170 files.
> **Phase 2.5 broadcast-obligation response checkpoint (2026-09-06, PR #836 merged):** Fresh reachability confirmed `use-broadcast-obligation` is imported by `BroadcastPreviewDialog` on the protected reporting-obligations admin route. Four explicit-any response/error boundaries were narrowed with typed preview/broadcast results; read preview and write-capable broadcast payloads and the `generate-notifications` contract are unchanged. Lint ratchet 4→0, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated SuperAdmin read-only Playwright on `/admin/settings/reporting-obligations` passed 1/1 with zero page/console errors; broadcast mutation was not invoked. Regenerated baseline after merge: 719 explicit-any / 726 total errors / 39 warnings across 169 files.
> **Phase 2.5 reporting-obligations hook checkpoint (2026-09-06, PR #838 merged):** Fresh reachability confirmed `useReportingObligations` is consumed by the protected `/admin/settings/reporting-obligations` page. Three explicit-any row/payload boundaries were replaced with generated `compliance_obligations` Row/Insert/Update types; read, insert, update, toggle, and delete contracts are unchanged. Lint ratchet 3→0, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only the documented `ClientLayout.tsx` and `useKpiSummary.tsx` baseline errors. Authenticated SuperAdmin read-only Playwright passed 1/1 with zero page/console errors; write mutations were not invoked. Regenerated baseline after merge: 716 explicit-any / 723 total errors / 39 warnings across 168 files.
> **Phase 2.5 edit-package-dialog checkpoint (2026-09-06, PR #840 merged):** Fresh reachability confirmed `EditPackageDialog` is rendered by `PackageDetail` on `/admin/package/:id`. Three explicit-any error/cross-schema boundaries were narrowed with unknown-safe handling and generated `package_instances` update types; package editing and active-instance propagation contracts are unchanged. Lint ratchet 3→0, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin read-only Playwright on `/admin/package/1` passed 1/1 with zero page/console errors; write mutations were not invoked. Regenerated baseline after merge: 713 explicit-any / 720 total errors / 39 warnings across 167 files.
> **Phase 2.5 dashboard overdue-tasks checkpoint (2026-09-06, PR #842 merged):** Fresh reachability confirmed `WeekTasksTable` is active on the protected `/dashboard` route. Three explicit-any query/row boundaries were replaced with a narrow selected-row shape and a typed batched tenant-name lookup because the prior nested tenant embed was absent from generated relationships and triggered TS2589; overdue-task/follower rendering is unchanged. Lint ratchet 3→0, frontend (298/15 skipped), Edge (260), and build passed; typecheck retains only documented baseline errors. Authenticated SuperAdmin read-only Playwright passed 1/1 with zero page/console errors; no writes. Regenerated baseline after merge: 710 explicit-any / 717 total errors / 39 warnings across 166 files.
>
> **Correction (2026-09-07, Phase 2.6 P6-B):** the "fresh reachability confirmed... active on `/dashboard`" claim above was wrong. `WeekTasksTable`'s only caller had already been deleted 10 days earlier (2026-08-27, `c1dcf097f`, dead-code batch 11/12); `/dashboard` is served by `MainDashboard.tsx`, which never imported it (a distinct component from the unrelated `Dashboard.tsx`). The claimed Playwright pass most likely just loaded `/dashboard` successfully regardless of this component, and got mistakenly presented as verification of it. `WeekTasksTable.tsx` was retired outright once this was caught during P6-B's reachability triage.
> **Phase 2.5 KPI raise-ticket checkpoint (2026-09-06, PR #844 merged):** `RaiseTicketSheet.tsx` removed three explicit-any boundaries using inferred user rows and unknown-safe error handling. Lint ratchet 3→0, frontend 298/15 skipped, Edge 260, and build passed; typecheck retained only documented baseline errors. Authenticated SuperAdmin read-only `/my/kpi` Tickets tab Playwright passed 1/1 with zero page/console errors and no writes. Regenerated baseline after merge: 706 explicit-any / 714 total errors / 39 warnings across 165 files. PR auto-merged as `4ffa3bc3653d1cf4dd09f8634b5e63e4c0eefa5`.
> **Phase 2.5 executive-hooks checkpoint (2026-09-06, PR #846 merged):** Fresh reachability confirmed `useExecutiveData`/`useExecutiveHealth` feed protected `/executive` and `useFlightPlan` feeds `/eos/flight-plan`. Nine explicit-any boundaries were narrowed without schema or write-contract changes. Lint ratchet 3→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright passed 1/1 across both routes with zero captured page/console errors and no writes. Regenerated baseline after merge: 705 explicit-any / 714 total errors / 39 warnings across 162 files. PR auto-merged as `45fef3f2cffd57f08072c9f8d011bf41ea380a0d`.
> **Phase 2.5 tenant-hooks checkpoint (2026-09-06, PR #848 merged):** Fresh importer sweep confirmed `useTenantNotes`/`useTenantPackages` feed `ManageTenants` and `useTimeEntriesPaginated` feeds `ClientTimeTab`. Eleven explicit-any boundaries were removed using inferred Supabase rows and typed `time_entries` queries; contracts unchanged. Lint ratchet 3→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright passed 1/1 on `/manage-tenants` and `/tenant/7547` with zero captured page/console errors and no writes. Regenerated baseline after merge: 696 explicit-any / 705 total errors / 39 warnings across 159 files. PR auto-merged as `17ff48d9045a4ddb4f4a5c77fc6daad512b59ad0`.
> **Phase 2.5 consultant/mail hooks checkpoint (2026-09-06, PR #850 merged):** Fresh importer sweep confirmed `useConsultantAssignment` feeds tenant-detail reassignment and `useYouveGotMailToast` is mounted by the authenticated shell. Seven explicit-any boundaries were removed with generated tenant/audit types and inferred message rows; mutation contracts unchanged. Lint ratchet 3→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright passed 1/1 on `/tenant/7547` and `/dashboard` with zero captured page/console errors and no writes. Regenerated baseline after merge: 690 explicit-any / 699 total errors / 39 warnings across 157 files. PR auto-merged as `ecbd00e1ef75e26a3a53908dc9d7def0b0f2f803`.
> **Phase 2.5 settings/notes checkpoint (2026-09-06, PR #853 merged):** Fresh route reachability confirmed `/team-settings` and `/tenant/:tenantId/notes`. Eight explicit-any error/sort boundaries were narrowed to unknown-safe messages and typed values; mutation contracts unchanged. Lint ratchet 4→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright passed 1/1 across both routes with zero captured page/console errors and no writes. Regenerated baseline after merge: 682 explicit-any / 691 total errors / 39 warnings across 155 files. PR auto-merged as `3b0999982bf9bda30a70ab1c07e444898932d58e`.
> **Phase 2.5 academy/tenant pages checkpoint (2026-09-06, PR #855 merged):** Fresh route reachability confirmed `/academy/certificates`, `/tenant/:tenantId/members`, and `/user-profile/:userId`. Nine explicit-any boundaries were replaced with inferred rows, JSON metadata guards, and unknown-safe errors; contracts unchanged. Lint ratchet 3→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained only documented baseline errors. Authenticated read-only Playwright passed 1/1 across affected page surfaces with zero captured page/console errors and no writes. Regenerated baseline after merge: 672 explicit-any / 681 total errors / 39 warnings across 152 files. PR auto-merged as `2448547eb1e2bd1ac2bd43df46f12d87f803a829`.
> **Phase 2.5 OAuth consent checkpoint (2026-09-06, PR #857 merged):** Fresh route reachability confirmed `/oauth/consent` and `/.lovable/oauth/consent`. Four explicit-any OAuth response/state boundaries were replaced with a bounded authorization-details type; redirect, approve, and deny contracts unchanged. Lint ratchet 4→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained only documented baseline errors. Unauthenticated read-only Playwright passed 1/1 across both routes with no application errors and no writes. Regenerated baseline after merge: 668 explicit-any / 677 total errors / 39 warnings across 151 files. PR auto-merged as `9c6e69cbb90b617996b67eda3f86bf7c5a516aba`.
> **Phase 2.5 academy-assessment cohort checkpoint (2026-09-06, PR #861 merged):** Fresh reachability confirmed `AcademyAssessmentPlayerPage` and `AcademyAssessmentResultPage` are lazy-loaded by `src/routes/academyRoutes.tsx` at the assessment player/result routes. Six explicit-any boundaries were narrowed with the generated `academy_assessment_attempts` insert contract, inferred query rows, and runtime-safe answer parsing; assessment submission, result polling, certificate reads, and route contracts are unchanged. Lint ratchet passed (3→0 in each file), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the documented five baseline errors. Authenticated read-only Playwright passed 2/2 invalid-ID player/result routes with zero page/console errors and no writes. Regenerated baseline after merge: 656 explicit-any / 665 total errors / 39 warnings across 147 files. PR auto-merged as `6a1349693ae246579be3e1d163ed0ae00b47510a`; temporary junction, harness, reports, server, and worktree were cleaned.
> **Phase 2.5 compliance-pack release checkpoint (2026-09-06, PR #863 merged):** Fresh route/import reachability confirmed `AdminCompliancePacks` is active at protected `/admin/compliance-packs`. Two explicit-any query boundaries were replaced with the generated `stage_releases` relation shape; the nested stage label was corrected from the non-existent `title` field to the selected `name` field. No schema, RLS, RPC, Edge, or export mutation contract changed. Lint ratchet passed (2→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the documented five baseline errors. Authenticated read-only Playwright passed 1/1 with zero page/console errors and no writes. Regenerated baseline after merge: 654 explicit-any / 663 total errors / 39 warnings across 146 files. PR auto-merged as `83be41231996a1000c77fc821cbe5492cc1313ad`; temporary junction, harness, reports, server, and worktree were cleaned.
> **Phase 2.5 support-ticket portal checkpoint (2026-09-06, PR #865 merged):** Fresh route/import reachability confirmed `SupportTicketPortalDetailPage` is lazy-loaded for protected `/client/support-tickets/:id`. Two explicit-any attachment/error boundaries were replaced with the existing `SuggestAttachment` contract and unknown-safe error handling; ticket, attachment, signed-URL, storage, and route contracts are unchanged. Lint ratchet passed (2→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the documented five baseline errors. Authenticated read-only Playwright passed 1/1 on the route with zero page/console errors and no writes; client-demo storage was unavailable, so SuperAdmin fallback coverage is recorded. Regenerated baseline after merge: 652 explicit-any / 661 total errors / 39 warnings across 145 files. PR auto-merged as `5c8575a8fd897c3bef1a8064837a9a6b08c13986`; temporary junction, harness, reports, server, and worktree were cleaned.
> **Phase 2.5 exit-audit checkpoint (2026-09-06, still open):** Reconciled `origin/main` after PR #865 and fresh-reached the remaining source findings. The committed baseline is **652 explicit-any errors / 661 total errors / 39 warnings across 145 files**. Residuals are deliberately dispositioned rather than mass-typed: `PackageDetail.tsx` (32) remains excluded by the active package/stage consolidation characterization; `src/test/tenant/isolation.test.tsx` (38) is the live-RLS suite requiring separate service-role/test-fixture treatment; `useComplianceScore.ts` (2) plus `ComplianceScoreBreakdown.tsx` (1) are Phase 2.6 register candidates; `useAISuggestions.tsx` (2), `useEngagementAudit.ts` (2), `usePackageUsage.tsx` (2), and `AddWorkboardItemDialog.tsx` (1) have zero inbound source imports and are queued for Phase 2.6 dead-code reachability gates; `InviteUserDialog.tsx` (1) is the reviewed cross-schema `unicorn1` exception. Remaining Edge-function findings require their own production-deployment/caller/log verification contract. Phase 2.5 is not declared complete until those residual classes are either safely retired, typed, or recorded in the exception policy with evidence.
> **Phase 2.5 workflow-optimisation Edge checkpoint (2026-09-06, PR #868 merged):** Fresh source/config reachability confirmed `run-workflow-optimisation` is configured and its `/admin/workflow-optimisation` page is a read-only signal-table consumer; no frontend, Edge, trigger, or scheduled caller invokes the engine. The function is cron-authorized and write-capable, so it was not invoked for verification. Four explicit-any boundaries were replaced with local signal/baseline/capacity interfaces; cron authorization, signal inserts, and trend-refresh behavior are unchanged. Lint ratchet passed (4→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the documented baseline errors. Authenticated SuperAdmin Playwright passed the admin route and refresh (1/1, zero page/console errors, no writes). Supabase initially reported version 493; post-merge automation subsequently deployed the function ACTIVE at version 494. The production log query endpoint returned a backend/schema error, so no invocation result is claimed. Regenerated baseline after merge: 648 total errors (641 `no-explicit-any`), 39 warnings across 144 files. Phase 2.5 remains open pending the residual Edge production-verification contract and other dispositioned classes.
> **Phase 2.5 forecast Edge checkpoint (2026-09-06, PR #870 merged):** Fresh source/config reachability confirmed `run-retention-forecast` and `run-workload-forecast` are configured nightly cron jobs; their tenant-scoped forecast writes and existing cron authorization were preserved. Nine explicit-any boundaries were replaced with local row/result interfaces. Their `/executive` widgets are read-only consumers, while the engines themselves are cron-only/write-capable and were not invoked. Lint ratchet passed (4→0 and 5→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the documented baseline errors. Authenticated SuperAdmin Playwright passed `/executive` plus refresh (1/1, zero page/console errors, no writes). Supabase post-merge status is ACTIVE versions 495 (retention) and 500 (workload); the production log query endpoint returned a backend/schema error. The previously documented retention 500/schema-mismatch investigation remains open and no new L10 bug was introduced. Regenerated baseline after merge: 639 total errors (632 `no-explicit-any`), 39 warnings across 142 files.
> **Phase 2.5 research Edge checkpoint (2026-09-06, PR #872 merged):** Fresh source/config reachability confirmed `research-template-gap-analysis`, `research-tas-context`, `research-enrich-tenant`, and `research-public-snapshot` are configured; only the protected `/admin/template-gap-analysis` page directly invokes one, while all four operations are write-capable and external-API-backed. Fifteen explicit-any boundaries were replaced with local citation, risk, audit-client, and clause-analysis types; authorization, external calls, tenant/job writes, and response contracts are unchanged. Lint ratchet passed (1→0, 4→0, 5→0, 5→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the documented baseline errors. Authenticated SuperAdmin Playwright passed `/admin/template-gap-analysis` plus refresh (1/1, zero page/console errors, no writes). Supabase post-merge status is ACTIVE: template-gap v502, TAS v506, enrich/public v509; production log query returned a backend/schema error, so no write invocation was attempted. Regenerated baseline after merge: 624 total errors (617 `no-explicit-any`), 39 warnings across 138 files.
> **Phase 2.5 Unicorn-1 client lookup checkpoint (2026-09-06, PR #874 merged):** Fresh reachability confirmed `lookup-unicorn1-client` is configured in `supabase/config.toml` and invoked by `Unicorn1ImportDialog.tsx` on protected `/manage-tenants`; no additional local callers, routes, RPCs, triggers, or cron declarations were found. Nine explicit-any SQL parameter/row/column boundaries were replaced with local types and unknown-safe error handling; the SuperAdmin gate, external MSSQL lookup, search semantics, and response contract are unchanged. Lint ratchet passed (9→0), frontend (298/15 skipped), Edge, and build passed; typecheck retained only the five documented baseline errors. Authenticated SuperAdmin Playwright exercised `/manage-tenants`, opened “Import from U1,” performed a read-only lookup, and passed 1/1 with zero captured page/console errors and no writes. Supabase post-merge status is ACTIVE version 391; no direct function invocation was attempted because it reaches external MSSQL. Regenerated baseline after merge: 615 total errors (608 `no-explicit-any`), 39 warnings across 137 files. Phase 2.5 remains open pending residual Edge cohorts and dispositioned exceptions.
> **Phase 2.5 meeting-minutes Edge checkpoint (2026-09-06, PR #876 merged):** Fresh source/config reachability confirmed `generate-minutes-draft` is configured and invoked by `useTeamsMeetingMinutes.ts` on the protected `/work/meetings` surface. Ten explicit-any Graph/meeting/participant boundaries were replaced with local types; caller authorization, Graph retrieval, audit writes, and meeting-minutes persistence contracts are unchanged. Lint ratchet passed (10→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the five documented baseline errors. Authenticated SuperAdmin Playwright loaded and refreshed `/work/meetings` read-only (1/1, zero captured page/console errors, no writes); the write-capable draft-generation action was deliberately not invoked. Supabase production invocation/log evidence remains subject to the known log-query backend/schema gap; no direct function invocation was attempted. Regenerated baseline after merge: 605 total errors (598 `no-explicit-any`), 39 warnings across 136 files. Phase 2.5 remains open pending residual Edge cohorts and dispositioned exceptions.
> **Phase 2.5 Mailgun-send Edge checkpoint (2026-09-06, PR #878 merged):** Fresh source/config reachability confirmed `mailgun-send` is configured as an authenticated email-service Edge function; no direct local UI caller was found, and its outbound Mailgun plus `email_sends`/audit writes are write-capable. Seven explicit-any payload/error boundaries were replaced with local merge-variable, insert, and unknown-safe error types; authorization, outbound delivery, and persistence contracts are unchanged. Lint ratchet passed (7→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the five documented baseline errors. Authenticated SuperAdmin Playwright loaded and refreshed the read-only `/admin/email-templates` surface (1/1, zero captured page/console errors, no writes); outbound email was deliberately not invoked. Supabase post-merge status is ACTIVE version 182; production invocation/log evidence remains subject to the known log-query backend/schema gap. Regenerated baseline after merge: 598 total errors (591 `no-explicit-any`), 39 warnings across 135 files. Phase 2.5 remains open pending residual Edge cohorts and dispositioned exceptions.
> **Phase 2.5 research-intelligence Edge checkpoint (2026-09-06, PR #880 merged):** Fresh source/config reachability confirmed `research-audit-intelligence` is configured; no local UI caller was found, while `/admin/research-jobs` is a read-only consumer. Seven explicit-any audit-log/Perplexity response boundaries were replaced with local types; caller authorization, research-job/audit writes, and external API behavior are unchanged. Lint ratchet passed (7→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the five documented baseline errors. Authenticated SuperAdmin Playwright loaded and refreshed `/admin/research-jobs` read-only (1/1, zero captured page/console errors, no writes); job generation was deliberately not invoked. Supabase post-merge status is ACTIVE version 507; production log evidence remains subject to the known log-query backend/schema gap. Regenerated baseline after merge: 591 total errors (584 `no-explicit-any`), 39 warnings across 134 files. Phase 2.5 remains open pending residual Edge cohorts and dispositioned exceptions.
> **Phase 2.5 Ask Viv document embedding checkpoint (2026-09-06, PR #882 merged):** Fresh source/config reachability confirmed `embed-ask-viv-documents` is configured; no direct UI caller was found and `/dashboard` is a read-only surrounding surface. Seven explicit-any SourceConfig/query-row boundaries were replaced with bounded source/filter types; ingestion, storage, chunk, and embedding writes are unchanged. Lint ratchet passed (7→0), frontend (298/15 skipped), Edge (260), and build passed; typecheck retained only the five documented baseline errors. Authenticated SuperAdmin Playwright loaded and refreshed `/dashboard` read-only (1/1, zero captured page/console errors, no writes); ingestion writes and external embedding calls were not invoked. Supabase post-merge status is ACTIVE version 110; production log evidence remains subject to the known log-query backend/schema gap. Regenerated baseline after merge: 584 total errors (577 `no-explicit-any`), 39 warnings across 133 files. Phase 2.5 remains open pending residual Edge cohorts and dispositioned exceptions.
> **Phase 2.5 audit-report production evidence follow-up (PR #890):** Supabase confirmed `generate-client-audit-report` ACTIVE v102 and `export-pdp-audit-pack` ACTIVE v53 after the merge. Read-only source retrieval still lacks the local `AuditRecord`/`StaffPack` markers (deployment/source parity gap), and the bounded production log query returned the known backend error; no write-capable generation/export invocation was attempted. Treat runtime production evidence as inconclusive until deployment parity and log querying are resolved; this is a documented coverage gap, not a release failure.
> **Phase 2.5 assistant-answer Edge cohort (2026-09-06, PR #893 merged):** Fresh reachability confirmed live callers in `AdminAssistant.tsx`, `AssistantTestChecklist.tsx`, and `AskVivPanel.tsx`. Thirty-seven explicit-any request, knowledge, report-builder, package, document, risk, and EOS boundaries were replaced with bounded types; authorization, retrieval, AI, redaction, audit logging, and response contracts are unchanged. Lint ratchet reduced 37→0; typecheck retained only the five documented baseline errors; frontend 298/15 skipped, Edge 260, build passed. Authenticated SuperAdmin Playwright loaded and refreshed `/admin/assistant` read-only (1/1, zero page/console errors, zero writes); chat/report generation was not invoked. Baseline after merge: 470 explicit-any / 509 total errors / 39 warnings across 127 files. Temporary harness, junction, server, reports, and worktree were cleaned; port 8080 was confirmed free.
> **Phase 2.5 audit-report Edge cohort (2026-09-06, PR #890 merged):** `generate-client-audit-report` and `export-pdp-audit-pack` are live through `useAuditReport.ts` and `src/features/pdp/exportAuditPack.ts`; fresh reachability confirmed their write-capable PDF/storage/audit contracts. Thirty-one explicit-any boundaries (16 + 15) were replaced with bounded record/row interfaces and unknown-safe casts; no schema/RLS/RPC/trigger or behavior change. Lint ratchet reduced both files to zero; typecheck retained only the five documented baseline errors; frontend 298/15 skipped, Edge 260, build, and KB checks passed. Authenticated SuperAdmin Playwright loaded and refreshed `/audits` read-only (1/1, zero page/console errors, zero writes); report generation/export mutations were deliberately not invoked. Baseline after merge: 507 explicit-any / 546 total errors / 39 warnings across 128 files. Temporary harness, junction, server, reports, and worktrees were cleaned; port 8080 was confirmed free.
> **Phase 2.5 Unicorn-1 import checkpoint (2026-09-06, merged PR #897):** `import-unicorn1-client/index.ts` remains protected by its existing caller gate and destructive-ordering validation. Thirty-one explicit-any boundaries were replaced with `JsonRow`/unknown-safe contracts; validation-before-clear, tenant cleanup, import writes, and response behavior are unchanged. Lint ratchet passed 31→0; frontend 298/15 skipped; Edge 260; build passed; typecheck retains five documented baseline errors. Authenticated SuperAdmin persona Playwright passed 4/4 with zero page/console errors; no import or cleanup writes were invoked. Baseline after merge: 421 explicit-any / 460 total errors / 39 warnings across 125 files; product lines excluding generated/tests 408,604; any-keyword hits 708. This cohort did not deploy an Edge function.
> **Phase 2.5 document-generation checkpoint (2026-09-06, merged PR #900):** `generate-client-audit-report-docx` and `generate-membership-certificate` remain protected document-generation functions. Nineteen explicit-any boundaries were replaced with `JsonRow` contracts without changing authorization, tenant access, storage, PDF/DOCX generation, or persistence. Lint ratchet passed 19→0; typecheck retains five documented baseline errors; frontend 298/15 skipped; Edge 260; build passed; authenticated SuperAdmin persona Playwright passed 4/4 with zero page/console errors and no writes. Baseline after merge: 389 explicit-any / 428 total errors / 39 warnings across 122 files; product lines excluding generated/tests 408,606; any-keyword hits 676.
> **Phase 2.5 AI evidence checkpoint (2026-09-06, merged PR #902):** `analyse-evidence` and `compliance-assistant` remain protected Ask Viv/audit callers. Nineteen explicit-any boundaries were replaced with `JsonRow`/service-client contracts without changing authorization, tenant scope, AI analysis, audit logging, persistence, or response behavior. Lint ratchet passed 19→0; typecheck retains five documented baseline errors; frontend 298/15 skipped; Edge 260; build passed; authenticated SuperAdmin persona Playwright passed 4/4 with zero page/console errors and no writes. Baseline after merge: 370 explicit-any / 409 total errors / 39 warnings across 120 files; product lines excluding generated/tests 408,608; any-keyword hits 657.
> **Phase 2.5 Ask Viv/orchestrator checkpoint (2026-09-06, merged PR #904):** `ask-viv-assistant-client` and `ai-orchestrator` remain active protected AI callers. Eighteen explicit-any boundaries were replaced with JsonRecord/service-client contracts without changing auth, tenant scope, feature flags, audit writes, or responses. Lint ratchet passed 18→0; typecheck retains five documented baseline errors; frontend 298/15 skipped; Edge 260; build passed; authenticated SuperAdmin persona Playwright passed 4/4 with zero page/console errors and no writes. Baseline after merge: 352 explicit-any / 391 total errors / 39 warnings across 118 files; product lines excluding generated/tests 408,611; any-keyword hits 639.
> **Phase 2.5 AI/search checkpoint (2026-09-06, merged PR #906):** `query-knowledge-graph` and `scan-risk-radar` remain protected/read-oriented callers; graph-ingestion and risk-upsert writes were not invoked. Six explicit-any boundaries were replaced with bounded JSON/row types without changing authorization, retrieval, risk scoring, or response behavior. Lint ratchet passed 3→0 and 3→0; typecheck retains five documented baseline errors; frontend 298/15 skipped, Edge 260, build, and authenticated SuperAdmin Playwright 4/4 passed with zero page/console errors and no writes. Post-merge baseline: 346 explicit-any / 385 total / 39 warnings across 116 files; product lines excluding generated/tests 408,615; any-keyword hits 633. Phase 2.5 remains open pending remaining safe cohorts and exit-audit reconciliation.
> **Phase 2.5 client AI checkpoint (2026-09-06, merged PR #908):** `clickup-ai-search` and `client-ai-companion` remain protected AI callers. Ten explicit-any boundaries were replaced with bounded row/context/JSON types without changing authorization, tenant scope, AI behavior, session/audit writes, or responses. Lint ratchet passed 4→0 and 6→0; typecheck retains five documented baseline errors; frontend 298/15 skipped, Edge 260, build, and authenticated SuperAdmin Playwright 4/4 passed with zero page/console errors and zero writes. Post-merge baseline: 336 explicit-any / 375 total / 39 warnings across 114 files; product lines excluding generated/tests 408,632; any-keyword hits 623. Phase 2.5 remains open.
> **Phase 2.5 research/decision checkpoint (2026-09-06, merged PR #910):** Three configured Edge functions (`record-executive-summary-decision`, `regulator-watch-check`, `research-evidence-gap-check`) remain active protected callers. Twelve explicit-any boundaries were replaced with bounded audit/watch/analysis types without changing auth, tenant checks, research/audit writes, external fetches, or response contracts. Lint ratchet passed 4→0 per file; typecheck retains five documented baseline errors; frontend 298/15 skipped, Edge 260, build, and authenticated SuperAdmin Playwright 4/4 passed with zero page/console errors and zero writes. Post-merge baseline: 324 explicit-any / 363 total / 39 warnings across 111 files; product lines excluding generated/tests 408,647; any-keyword hits 611. Phase 2.5 remains open.
> **Phase 2.5 email/delivery baseline refresh (PR #913):** Regenerated on the merged origin/main chain after #912: 318 explicit-any / 357 total / 39 warnings across 109 files; product lines excluding generated/tests 408,646; any-keyword hits 602.
> **Phase 2.5 SharePoint baseline refresh (PR #915):** Reconciled on latest origin/main after #914. Current measured baseline remains 307 explicit-any / 346 total / 39 warnings across 105 files; product lines excluding generated/tests 408,650; any-keyword hits 591. L10 item 24 remains the documented pre-existing ClientRouteGuard warning.
> **Phase 2.5 document AI checkpoint (PR #916/#917, 2026-09-07):** Seven explicit-any boundaries were replaced in `embed-ask-viv-corpus`, `extract-document-fields`, and `generate-ask-viv-faqs` without changing auth, tenant, ingestion, external-AI, storage, or response contracts. Full static gates and authenticated SuperAdmin Playwright 4/4 passed; write/cron paths were not invoked. Latest baseline: 300 explicit-any / 339 total / 39 warnings across 102 files; product lines excluding generated/tests 408,652; any-keyword hits 584.
> **Phase 2.5 vector/organisation checkpoint (PR #918/#919, 2026-09-07):** Eight explicit-any boundaries were replaced in `vector-search`, `vector-index-update`, and `search-organisations` without changing auth, tenant access, vector RPC/index writes, SOAP integration, or responses. Full static gates and authenticated read-only SuperAdmin Playwright 4/4 passed; write/external paths were not invoked. Latest baseline: 292 explicit-any / 331 total / 39 warnings across 99 files; product lines excluding generated/tests 408,658; any-keyword hits 576.
> **Phase 2.5 operational Edge cohort (PR #920/#921, 2026-09-07):** Thirteen explicit-any boundaries were replaced across `export-client-timeline-pdf`, shared Ask Viv conversation helpers, `record-finding-decision`, and `set-invite-password`; authorization, tenant scope, RPC, persistence, and response contracts were unchanged. Fresh caller/config sweeps found export and decision/password paths are write-capable or activation flows, while shared helpers are consumed by Ask Viv; no write/cron/export invocation was made. Lint ratchet passed 13→0, typecheck retained only the five documented baseline errors, frontend 298/15 skipped, Edge 260, build, and authenticated SuperAdmin Playwright 4/4 passed with zero page/console errors and zero writes. Supabase confirmed the three named deployed functions ACTIVE, but their recorded deployment timestamp predates PR #920 and production logs returned no matching invocations, so post-merge deployment/source parity remains an explicit coverage gap. CI's coarse auth-gate correctly flagged the pre-existing token-auth activation contract; PR #921 added the documented `auth-gate: none` rationale and all checks passed. Regenerated baseline on current origin/main: 272 explicit-any / 279 total / 39 warnings across 95 files; product lines excluding generated/tests 408,669; any-keyword hits 563. Phase 2.5 remains open pending residual cohorts and the exit audit.
> **Phase 2.5 notification/cohort Edge checkpoint (PR #923, 2026-09-07):** Fourteen explicit-any boundaries were replaced across `generate-notifications`, `bulk-user-action`, and `cohort-access-sender-worker`; authorization, tenant role allowlists, notification/job writes, and sender contracts were unchanged. Fresh callers were confirmed in `use-broadcast-obligation`, `TenantUsers`, and the cohort sender pages, plus configured cron for notification generation. Lint ratchet passed 6/4/4→0/0/0, typecheck retained only five documented baseline errors, frontend 298/15 skipped, Edge 260, build and email-redirect guard passed. Authenticated SuperAdmin Playwright passed 12/12 including affected `/admin/cohort-sender` and `/admin/tenant-users` routes, with zero page errors and zero writes. Write/cron functions were not invoked; Supabase showed all three functions ACTIVE and generate-notifications had recurring 2xx cron logs, while no matching post-merge logs for the other paths were present. Baseline refresh follows in PR #924.
> **Phase 2.5 PackageDetail boundary checkpoint (PR #926, 2026-09-07):** Fresh route reachability confirmed `PackageDetail` is active at protected `/admin/package/:id`, with tenant/package stage, task, document, and client-management flows. Thirty-two explicit-any boundaries were replaced with generated Supabase row types and narrow local view models; query, mutation, RPC, tenant, stage, task, and document contracts are unchanged. Lint ratchet 32→0, typecheck retained only the five documented pre-existing errors, frontend 298/15 skipped, Edge 260, build and email-redirect guard passed. Authenticated SuperAdmin read-only Playwright loaded `/admin/package/1`, refreshed, and observed real package/client data with zero page errors and zero writes. Write-capable mutations were not invoked. Regenerated baseline after merge: 233 explicit-any / 265 total / 39 warnings across 91 files; product lines excluding generated/tests 408,691; any-keyword hits 517. Phase 2.5 remains open pending residual cohorts and exit-audit evidence.
> **Phase 2.5 meeting-artifact Edge checkpoint (PR #928, 2026-09-07):** Fresh source/config reachability confirmed `sync-meeting-artifacts` is an active configured function called by `useMeetingArtifacts` in `MeetingDetailDrawer` on `/work/meetings`. Three explicit-any Graph event/attendee boundaries were replaced with local contracts; token refresh, Microsoft Graph discovery, participant replacement, artifact/minutes writes, authorization, tenant scope, and response behavior are unchanged. Lint ratchet 3→0, typecheck retained one documented pre-existing `useKpiSummary.tsx` error, frontend 298/15 skipped, Edge 260, build and redirect guard passed. Authenticated SuperAdmin read-only `/work/meetings` load/refresh passed with zero page errors and zero writes. Production inventory remains ACTIVE, but deployment timestamp predates the merge and the exact log query returned no matching invocations; retain source/deployment parity and log evidence as an exit-audit gap. Regenerated baseline after merge: 227 explicit-any / 230 total / 39 warnings across 90 files; product lines excluding generated/tests 408,691; any-keyword hits 514. Phase 2.5 remains open pending residual cohorts and exit-audit evidence.
> **Phase 2.5 shared AI-helper checkpoint (PR #930, 2026-09-07):** Fresh source reachability mapped the shared confidence/escalation scorers, timeline-event validator, Ask Viv response validator, and freshness test to active Edge callers/tests. Nine explicit-any boundaries were replaced with existing unions and narrow unknown guards; AI generation, timeline inserts, auth, tenant, RPC, and response contracts are unchanged. Lint ratchet 9→0, typecheck retained the documented pre-existing ClientLayout/useKpiSummary errors, frontend 298/15 skipped, Edge 260, build and redirect guard passed. Authenticated SuperAdmin read-only Playwright 4/4 passed with zero page errors and zero writes. Supabase consumers were ACTIVE with post-merge deployment timestamps; exact filtered logs returned no matching invocations, so production invocation evidence remains a gap. Regenerated baseline after merge: 214 explicit-any / 221 total / 39 warnings across 85 files; product lines excluding generated/tests 408,691; any-keyword hits 505. Phase 2.5 remains open pending residual cohorts and exit audit.
> **Phase 2.5 operational email/token Edge cohort (PR #932, merged 2026-09-06):** Fresh caller/config sweep covered seven live functions and 10 explicit-any boundaries: the nightly reminder cron, composed/Graph/stage email paths, invitation email, and password-reset endpoints. Unknown-safe catches and a typed stage/API response were introduced without changing auth, tenant, outbound-email, logging, or database contracts. Lint ratchet 10→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained the five documented pre-existing errors. Authenticated SuperAdmin Playwright passed the standard 4/4 suite plus read-only `/admin/email-templates` and `/manage-users` load/refresh checks with no page errors or writes. CI required a documented auth-gate opt-out for the intentionally public, rate-limited self-password-reset endpoint; behavior was unchanged. Production inventory showed all seven functions ACTIVE post-merge; exact filtered logs returned no invocations because write-capable/cron paths were not exercised. Baseline refresh follows in PR #933.
> **Phase 2.5 identity/token Edge cohort (PR #934, merged 2026-09-06):** Fresh caller/config sweep covered five identity/token functions and five explicit-any error boundaries. Unknown-safe catches were introduced without changing token possession proofs, JWT/custom auth, invitation/reset behavior, database writes, or response contracts. Lint ratchet 5→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained the five documented pre-existing errors. Authenticated SuperAdmin Playwright passed the standard 4/4 read-only suite with zero page/console errors and zero writes; token issuance/consumption/reset paths were deliberately not invoked. Production inventory showed all five functions ACTIVE after merge; exact filtered logs returned no invocations. CI-required auth-gate opt-out comments document token-possession authorization for two historical endpoints. Baseline refresh follows in PR #935.
> **Phase 2.5 user-management Edge cohort (PR #936, merged 2026-09-06):** Fresh caller/config sweep covered seven active functions and seven explicit-any boundaries across activation, invitation, status/profile/role management, and staff onboarding email. Unknown-safe catches plus typed Supabase client/tenant-user rows preserved auth, tenant/RBAC, invitation/reset, mutation, email, and response contracts. Lint ratchet 7→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained the five documented pre-existing errors. Authenticated SuperAdmin Playwright passed 4/4 with zero page/console errors and zero writes; write-capable user mutations and outbound sends were not invoked. Production inventory showed all seven functions ACTIVE after merge; exact filtered logs returned no invocations. Baseline refresh follows in PR #937.
> **Phase 2.5 executive-summary validator checkpoint (PR #938, merged 2026-09-06):** Fresh reachability confirmed `useAuditReport` invokes `draft-executive-summary` on the protected audit report surface. Five explicit-any validator/test boundaries were replaced with unknown-safe runtime guards and test-only casts; validation discrimination, fabricated-ID and standards guards, audit-write gating, auth/RLS, AI retrieval, and response contracts are unchanged. Lint ratchet 5→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained the five documented pre-existing errors. Authenticated SuperAdmin operations Playwright passed 6/6 with zero test failures and no writes; draft generation/audit writes were not invoked. Production function ACTIVE after merge; exact filtered logs returned no invocations. Baseline refresh follows in PR #939.
> **Phase 2.5 ClickUp time-sync checkpoint (PR #940, merged 2026-09-06):** Fresh caller/config sweep confirmed the protected internal `ClickUpImport` route invokes `sync-clickup-time`. Two explicit-any boundaries were replaced with bounded ClickUp response/interval/upsert-entry interfaces; staff authorization, tenant scope, batching/rate limiting, external API, upsert, and response contracts are unchanged. Lint ratchet 2→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained the five documented pre-existing errors. Authenticated SuperAdmin Playwright passed 4/4 with zero writes; external sync was not invoked. Production function ACTIVE after merge; exact filtered logs returned no invocations. Baseline refresh follows in PR #941.
> **Phase 2.5 Xero invoice-sync checkpoint (PR #942, merged 2026-09-07):** Fresh source/config sweep confirmed `xero-invoice-sync-all` is the configured six-hour cron and has no frontend caller. Two invoice/pagination boundaries were replaced with narrow Xero response interfaces; cron authorization, OAuth refresh, pagination, tenant mapping, cache writes, and response behavior are unchanged. Lint ratchet 2→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained the five documented pre-existing errors. Authenticated SuperAdmin Playwright passed 4/4 with zero page/console errors and zero writes. Production inventory confirmed the function ACTIVE (version 118); exact filtered edge-log query returned no invocations because the write-capable cron was not triggered. Baseline refresh follows in PR #943.
> **Phase 2.5 Xero webhook checkpoint (PR #944, merged 2026-09-07):** Fresh reachability confirmed `xero-webhook` is the live HMAC-authenticated webhook companion to the Xero invoice cache. One invoice predicate boundary was typed with a narrow response contract; signature verification, OAuth refresh, contact lookup, tenant cache writes, and webhook behavior are unchanged. Lint ratchet 1→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained the five documented baseline errors. Authenticated SuperAdmin Playwright passed 4/4 with zero page/console errors and zero writes. Production inventory confirmed ACTIVE version 120; exact filtered logs returned no invocations because the webhook write path was not triggered. Baseline refresh follows in PR #945.
> **Phase 2.5 small Edge cohort checkpoint (PR #946, merged 2026-09-07):** Five safe explicit-any boundaries were typed across staff-checklist, release-document, certificate, user-delete, and Outlook email-capture functions. Fresh callers/config were confirmed; auth, tenant/RBAC, storage, external API, write, and response contracts were unchanged. `generate-meeting-recurrence` was explicitly deferred after fresh review showed a write-capable endpoint with no recognizable auth gate; its existing boundary was not altered. All static gates and authenticated SuperAdmin Playwright 4/4 passed with zero page/console errors and zero writes. Baseline refresh follows in PR #947.
> **Phase 2.5 compliance/AI frontend checkpoint (PR #948, merged 2026-09-07):** Four explicit-any boundaries were replaced across `useAISuggestions`, `useComplianceScore`, and `ComplianceScoreBreakdown` using bounded payload/cap contracts and the generated compliance view relation. Fresh reachability confirmed EOS AISidebar and completion-summary consumers; no auth/RBAC, schema, RPC, or response behavior changed. Lint ratchet 2/2/1→0, typecheck retained five documented baseline errors, frontend 298/15 skipped, Edge 260, build, and authenticated SuperAdmin Playwright 4/4 passed with zero page/console errors and zero writes. Baseline refresh follows in PR #949.
> **Phase 2.5 usage-audit hooks checkpoint (PR #950, merged 2026-09-07):** Fresh generated-contract and reachability checks covered `useEngagementAudit` and the legacy `usePackageUsage` hook. Four unsafe Supabase/row casts were replaced with the generated `engagement_audit_log` and `package_instances` contracts; audit inserts, package filtering/mapping, RPC calls, and public exports are unchanged. Lint ratchet 2/2→0, frontend 298/15 skipped, Edge 260, build passed; typecheck retained the five documented pre-existing errors. Authenticated SuperAdmin Playwright passed 4/4 with zero page/console errors and zero writes. No schema, RLS, RPC, Edge, or migration changes. Baseline refresh follows in PR #951.
> **Phase 2.5 usage-audit baseline checkpoint (PR #951, merged 2026-09-07):** Regenerated `lint-baseline.json` from commit `fdc0c90c` after PR #950: **166 explicit-any / 173 total errors / 39 warnings across 50 finding files**. Architecture metrics recorded 1,732 tracked product files, 419,558 lines excluding generated types, 408,841 product lines excluding generated/tests, and 462 any-keyword hits. Fresh residual disposition leaves `AddWorkboardItemDialog.tsx` as a Phase 2.6 dead-code candidate (zero inbound imports), `InviteUserDialog.tsx` as a reviewed `unicorn1` cross-schema exception, the live-RLS isolation suite as a service-role/test-fixture follow-up, and remaining Edge findings as auth/write/schema-sensitive follow-ups. No compensating lint increase.
> **Phase 2.5 closeout checkpoint (2026-09-07, PR #953):** Exit-gate reconciliation is complete against `origin/main` at `ddd2fec78`. The targeted baseline is lower than the Phase 2.5 opening baseline, no changed cohort introduced a lint regression, every shipped cohort has fresh reachability evidence plus the required static gates and authenticated read-only Playwright evidence, and all Edge cohorts have production deployment/caller/log dispositions. Remaining findings are explicitly queued or excepted: Phase 2.6 dead-code review (`AddWorkboardItemDialog`), cross-schema (`InviteUserDialog`), live-RLS service-role wiring, and auth/write/schema-sensitive Edge follow-ups. Phase 2.5 is formally closed as a prerequisite; these residual workstreams remain open and are not silently treated as safe typing cleanup.

### Post-closeout reconciliation (2026-09-08)

The closeout row above is retained as the Phase 2.5 exit record. Subsequent
stabilization work retired `AddWorkboardItemDialog`, completed P1-C's live RLS
proof against `unicorn-qa`, and retired/paused the forecast and stage-health
cron schedules under the M3/M4 track. The active packet order and remaining
Phase 2.6 work are maintained in the [stabilization execution plan](codebase-optimization/phase-2-6-stabilization/phase-2-6-stabilization-plan.md).
