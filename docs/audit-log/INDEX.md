# Audit Index

Chronological list of audits. Git tags are the authoritative reference
(`git tag --list "audit-*" "remix-*"`), this is the human-readable mirror.

Newest first.

---

## 2026

### August

- [2026-08-14 — Academy course-publish surfaced as a Client Activity timeline event — Carl asked for course publishing (an internal staff action) to show in the cross-tenant Client Activity dashboard; since it has no client tenant of its own, attributed it to `tenants.is_system_tenant = true` (Vivacity Coaching & Consulting) rather than hardcoding a tenant id; added event type `academy_course_published`, a new `academy_courses` publish trigger, and wired the type into the frontend icon/color/category maps; while rebuilding the event-type CHECK constraint found 4 live values (`action_item_comment`, `package_status_changed`, `audit_created`, `audit_completed`) missing from `src/types/timeline.ts` — pre-existing drift, preserved as-is, not reconciled; verified with a real (non-rollback) published-course insert, confirmed the resulting row and its live `/client-activity` rendering, then deleted both](entries/2026-08-14-academy-course-published-timeline-event.md) · author: Claude (session run by Carl)
- [2026-08-14 — Excel governance templates could never be published — Carl reported that a Manage Documents Excel file with no merge fields can't be published; found `handlePublish` in `import-sharepoint-template` gates on `document_template_mappings` having at least one row, but that table is only ever auto-populated by the import-time scan, which explicitly only runs for `.docx`/`.pptx` — every Excel draft was structurally guaranteed zero mappings regardless of whether the workbook had real placeholders, a de facto Excel-only bug rather than a real requirement, since merge-field mapping is inherently a Word/PowerPoint concept; fixed by skipping the mapping-count check when the version's file_name ends in `.xlsx`/`.xls`, leaving Word/PPT unaffected; deployed via `deploy_edge_function`, redeployed once more after an unrelated cosmetic doc-comment transcription slip in the shared `graph-app-client.ts` bundle, verified byte-for-byte against git afterward](entries/2026-08-14-excel-publish-merge-field-gate.md) · author: Claude (session run by Carl)
- [2026-08-14 — CSC and Integrator granted full parity with Team Leader on Academy Builder — Carl asked for CSC and Integrator to have access to the Academy pages; found the sidebar already claimed Team Leader/Integrator could see the Academy Builder section but all 11 routes were hard-gated `requireSuperAdmin` in `App.tsx` (latent bug, undetected because zero production users currently have `unicorn_role = 'Team Leader'`), and separately that a `role_permissions` table already gates create/edit/publish/manage actions inside those pages, where CSC/Integrator had `full` on view-only keys but `none` on every mutating key; added an `allowedRoles` option to `ProtectedRoute` and switched the routes to `["Team Leader","Integrator","CSC"]`, un-hid the sidebar section for CSC and stopped hiding items from Integrator, then ran a confirmed `UPDATE role_permissions` giving CSC and Integrator `full` on all 11 `academy.*` feature keys to match Team Leader exactly; BGT/CET untouched](entries/2026-08-14-csc-integrator-academy-parity.md) · author: Claude (session run by Carl)
- [2026-08-13 — Consolidate The Compliance Lab's 40 AI-generated segment-courses and stop Quick Add from creating one course per topic — diagnosed 6 source recordings exploded into 40 library cards (plus TAS Superhero, left alone) because Academy Quick Add wrote N `academy_courses` rows from `generate_workshop_segments` suggestions; added nullable `academy_lessons.segment_start/end_seconds`, taught the minutes trigger to use the segment span, pointed the lesson player at lesson bounds with a course-level fallback, and changed Quick Add workshop save to one course + N lessons; shipped `fn_consolidate_compliance_lab_segments(dry_run default true)` (service_role only) and ran it dry against prod — 5 parents / 28 archives / 3,027 enrollments → 713 keepers, Outcome Standards 83–94 reported only; **data apply gated on Angela confirming parent titles and the duplicate keep-list**; PR pending](entries/2026-08-13-compliance-lab-segment-consolidation.md) · author: Cursor (session run for Angela / Vivacity)
- [2026-08-13 — Facilitator names invisible to real clients — RLS gap on the users table read path — Carl noticed no facilitator name rendering on the Compliance Manager pathway logged in as the real client demo account; facilitator_id was correctly set in the DB but client-side users RLS only allows seeing self / own-tenant admins / assigned CSC, none of which covered Angela for the demo tenant, so the lookup silently returned nothing for most client tenants, not just the courses touched earlier; added SECURITY DEFINER function get_academy_facilitator_names_safe(uuid[]) scoped to only reveal names already public as a published course's facilitator_id, repointed useFacilitatorNames at it, granted EXECUTE to authenticated](entries/2026-08-13-facilitator-name-rls-gap.md) · author: Claude (session run by Carl)
- [2026-08-13 — Persist AI Assist transcript on academy_courses so "Generate quiz with AI" survives navigation — transcript was page-level session state on AcademyBuilderCourse, cleared on courseId change and never included in the Save Changes payload, so the Assessment tab button reset to disabled on every reload/revisit; added nullable academy_courses.transcript (migration add_transcript_to_academy_courses, applied via MCP to yxkgdalkbrriasiyyrwk), folded it into Structure-tab formState so it hydrates from the DB and persists on explicit Save Changes like every other AI-Assist field; write-path sweep found no RPC INSERT/UPDATE into academy_courses that would choke on the new nullable column; did not touch AiAssistPanel, AssessmentEditorTab, or the edge function](entries/2026-08-13-persist-academy-transcript.md) · author: Cursor (session run by Carl)
- [2026-08-13 — "Test CSC" facilitator corrected on 6 Trainers Edge courses, then re-corrected to no facilitator — Carl noticed "Test CSC" (carl+csc@vivacity.com.au, a test/demo account) showing as the facilitator on real published courses, right after facilitator names became visible to learners for the first time via the course-card work; found 56 of 62 published-with-facilitator courses correctly showed Angela Connell-Richards and exactly 6 showed "Test CSC" — the entire Trainers Edge series (ids 48, 50-54), all created in the same 8-minute window, the signature of someone batch-creating that series while logged in as their own test account; initially set facilitator_id on the 6 courses to Angela's user_uuid (only other facilitator in the catalog); same-day correction: Carl confirmed the real presenter was Samantha Holtham, not Angela — no users row exists for her yet, so reverted the 6 courses to facilitator_id = null pending her account being created; data-only, no code change](entries/2026-08-13-test-facilitator-trainers-edge.md) · author: Claude (session run by Carl)
- [2026-08-13 — Duplicate "Diversity Inclusion Cultural Safety" course archived — Carl spotted the same course title showing twice in the dashboard's Latest Recordings widget right after it was repointed to sort by delivery date; found two academy_courses rows for the same title, same delivery date, same source video, created 2 minutes 19 seconds apart — the classic signature of "Quick Add Recording" run twice on the same video by accident; swept the full catalog, confirmed this was the only duplicate pair; both courses had 178 enrollments each, and it was the exact same 178 real users auto-enrolled into both (confirmed via a join), with one completion on the older course and zero on the newer, no certificates issued on either; archived the newer, less-progressed duplicate (course 45) via the same status='archived' mechanism the app's own Archive action uses (permanent delete is blocked once a course has enrollments, and this matches a prior duplicate-cleanup's precedent) rather than migrating or deleting the enrollment rows, which stay in place as harmless residue; live-verified the duplicate is gone from Latest Recordings post-archive; data-only, no code change](entries/2026-08-13-diversity-inclusion-duplicate-course.md) · author: Claude (session run by Carl)
- [2026-08-13 — Academy tag cleanup + Tag Management tool — Carl asked for a breakdown of how Academy Builder's course tags surface as filter pills on the learner-facing pathway pages, then asked to clean up the tag data and build management tooling for it; found 190 distinct tags across 418 instances on 82 courses with no admin tooling at all (every edit happened one course at a time via the free-text course-editor chip input) and no bulk rename/merge RPC anywhere in the codebase; built a new Super-Admin-only `/superadmin/academy/tag-management` page (list all tags with live course counts, rename/merge — same operation, typing an existing tag's name collapses the two — or remove a tag from every course carrying it, with an expandable per-tag course list); proposed a full merge mapping in three confidence tiers before touching anything — mechanical (casing/hyphen/typo/plural), clear synonyms, and genuine taxonomy calls (the two biggest tags in the dataset, `compliance` (32) and `rto compliance` (29), were the same concept written two ways) — Carl reviewed and explicitly signed off on all three tiers; dry-run verified the mapping against all 82 affected rows before running, then applied via `execute_sql` directly to prod with Carl's explicit go-ahead (the bulk UPDATE itself, unlike the earlier UI testing, was correctly blocked by the auto-mode permission classifier pending that confirmation); result 190 → 162 distinct tags, 418 → 407 instances, verified post-hoc that no merged-away spelling remains and the new counts match expected arithmetic; deliberately left `vet compliance`/`vet standards` distinct from the RTO-specific tags (GTOs sit under a different Standards framework) and left tags that only looked similar (`self-assurance` vs `self-assessment`, `asqa`/`asqa audit`/`asqa standards`) untouched as genuinely different concepts; PR pending on unicorn-cms-f09c59e5](entries/2026-08-13-academy-tag-cleanup.md) · author: Claude (session run by Carl)
- [2026-08-12 — Added a "No Package" file status to Manage Documents, found a real package_stages data gap along the way — Carl wanted a status flagging documents that will never appear in any client package regardless of file readiness; presented two candidate definitions with real counts (16 vs 156 documents) since the codebase has two genuinely different "package assignment" signals (master-level `package_stages` vs. live-instance `document_instances`), Carl chose the broader one; Carl then spot-checked a specific document against the live `GovernancePackageAssignments` panel and found a contradiction — traced to `package_stages` missing a row for a stage ("GTO Documents - VIC") that had real provisioned usage, an isolated gap (swept the whole table, confirmed only one stage affected, but it touched 38 documents); fixed both the specific data gap (`apply_migration`, inserted the missing row) and the check's robustness (a stage now counts as "has package" if it's in `package_stages` **or** has real `stage_instances`, so a similar future gap can't silently break this feature again); live-verified the fix removed the specific document from the flagged list and the tab counts still summed exactly to 551; PR pending (same PR as the versioning work below) on unicorn-cms-f09c59e5](entries/2026-08-12-no-package-file-status.md) · author: Claude (session run by Carl)
- [2026-08-12 — Added YEAR.MAJOR.MINOR version labels (`display_version`) to document_versions — Carl wanted a human-readable version label shown wherever versions appear; review before building found zero documents had ever accumulated multiple versions (all 538 sat at v1), and that "re-linking a template creates a new version" didn't actually work for existing documents — the only live path to a real import was document-creation time, the button on an existing document's detail page just updated a URL pointer with no snapshot; also found 9 CRICOS documents already silently failing real generation (zero version rows) independent of this change; backfilled the new label from the legacy `documents.versiondate` field (a real historical date, not the 2026 bulk-import timestamp) for accuracy; added a Year/Major/Minor bump-type picker at import time, a "Current Version" selector reusing the existing publish/drift-check validation, a manual "Check for Drift" button, and wired the row-level SharePoint link icon in Manage Documents (a second, separate place doing the same old raw-URL-update) to the real import flow; explicitly declined a request to allow swapping the file on an already-published version, since that breaks the checksum/audit-trail integrity model; also removed the legacy, disconnected `documents.versionnumber` "Version Number" form field from Manage Documents and fixed an unrelated grid-cols-4-with-3-cards whitespace bug found along the way; made `display_version` itself inline-editable (a deliberate exception to the immutable-version-label rule, since correcting a label doesn't touch the checksum or delivery/audit trail) after Carl spotted 5 documents whose SharePoint filename embedded a real version (`...2026.03.00.docx`) that the backfill had missed, and used it to correct all 5 to `2026.03.00`; live-verified in a local dev server against prod Supabase before merge per Carl's request, including a real save-and-revert cycle on the new editable label; **caught post-hoc by Vercel's automated PR review bot** (not by this session's own testing): making `display_version` `NOT NULL` broke two Postgres RPCs that insert into `document_versions` without it — `bulk_create_documents_with_versions` (genuinely live, used by the bulk-upload dialog on `StageDocumentsPanel.tsx`, actively broken in prod for the gap between the two migrations) and `publish_document_version` (orphaned frontend caller, fixed anyway); root cause was that this session's safety sweeps only grepped frontend TypeScript, never server-side RPC bodies — fixed both RPCs to generate a label themselves, verified with a real throwaway RPC call against prod, and swept all `pg_proc` bodies referencing the table to confirm no third RPC was missed; PR pending on unicorn-cms-f09c59e5](entries/2026-08-12-document-versioning-labels.md) · author: Claude (session run by Carl)
- [2026-08-12 — Fixed RLS on document_fields (Required Merge Fields) — ported the orphaned `/document/:id` page's `MergeFieldsEditor` onto the live `GovernanceDocumentDetail` view so staff could actually define required merge fields for delivery-time completeness scoring; Carl noticed the panel rendered empty next to a populated "Merge Field Mappings" panel for the same document, then hit a visible RLS error on save; root cause: `document_fields`'s only two RLS policies were both broken (tenant-scoped SELECT never matches tenant-less master documents; the ALL policy depends on `is_superadmin()` → `users.global_role`, which is NULL for 7 of 9 real Super Admins) — the table had been silently unusable since inception because its only UI was unreachable via any live button; verified no data loss (delete-then-reinsert in the editor's save path was also silently blocked by the same RLS gap); fixed by adding four new policies mirroring the working `document_template_mappings` pattern (`users.is_vivacity_internal = true`), applied directly to prod via `apply_migration`; PR #252 on unicorn-cms-f09c59e5](entries/2026-08-12-document-fields-rls-fix.md) · author: Claude (session run by Carl)
- [2026-08-12 — Removed mismatched photo uploads from 4 documents — follow-up from the file-status readiness fix (#249); Carl asked to analyse the 12 "Ready"-but-no-SharePoint-link documents to see what actually made them ready; split into 8 genuinely ready (real files uploaded 2026-01-07, extension matching declared format) and 4 false positives where `format = 'docx'` but the actual uploaded file was an unrelated staff photo or company logo image (confirmed via `storage.objects` metadata as real, correctly-uploaded images — genuinely the wrong file, not a broken reference); one of the four (id 57) was the same document flagged during the original duplicate cleanup, showing the mismatched-photo pattern recurs across the Oct–Dec 2025 import batch rather than being a one-off; verified zero downstream references before removing, then cleared the legacy `uploaded_files`/`file_names` arrays and deleted the incorrect `document_versions` rows for all 4 so they correctly show "Needs Upload" again; deliberately did not delete the underlying image files from the `document-files` storage bucket, since removing `storage.objects` rows via raw SQL bypasses the Storage API's cleanup path; applied directly to prod via `apply_migration` with Carl's explicit approval; PR pending on unicorn-cms-f09c59e5](entries/2026-08-12-remove-mismatched-photo-uploads.md) · author: Claude (session run by Carl)

- [2026-08-12 — Atomic recording for governance document delivery — Carl asked why some documents show as "generated" with no matching Delivery History entry; found two unrelated causes — 89% of "generated" `document_instances` (23,156 of 26,119) were never actually generated at all, same stale-placeholder root cause as the "Needs Upload" badge bug; a genuine narrow bug affected 25 instances (all the same document, `Q3.D1-Trainers Handbook`, notably not even Governance-category, confirming `deliver-governance-document` is really a generic per-tenant document generation engine) that had a real SharePoint file but no delivery record at all, clustered in two 2026 windows (11 May, 5–8 Jun) with nothing since; root-caused to the edge function updating `document_instances` and inserting into `governance_document_deliveries` as two separate non-transactional calls, so a failure in the second after the first succeeded could leave a document looking fully generated with zero delivery trace; fixed by wrapping both writes (plus resolving open `document_generation_errors`) in one new plpgsql function (`record_governance_delivery_and_mark_generated`) that Postgres runs as a single implicit transaction, and updating the edge function to call it via one `rpc()` instead of two separate writes; along the way, confirmed a live-vs-git diff on the edge function and its `graph-app-client.ts` dependency was pure UTF-8 mojibake from the retrieval tool's transport, not real drift; did not backfill the 25 historical instances (would fabricate delivery metadata never actually captured); applied via `apply_migration` + `deploy_edge_function`; PR pending on unicorn-cms-f09c59e5](entries/2026-08-12-governance-delivery-atomic-recording.md) · author: Claude (session run by Carl)

- [2026-08-12 — `delete_document_cascade` live-vs-git drift reconciled — follow-up from the same-day Manage Documents duplicate cleanup, which surfaced that the live function no longer matches the only version committed in git; confirmed via `pg_get_functiondef` two real differences — the live version added an `is_vivacity_team_safe(auth.uid())` permission check (previously any role with EXECUTE could delete any document template), and fixed a bug where the committed version tried to `DELETE FROM documents_tenants WHERE document_id = ...` against a column that doesn't exist on that denormalized table, meaning the app's Manage Documents "Delete" button could never have actually worked while the old version was live; `list_migrations` shows the live version was applied 2026-07-15 as `harden_delete_document_cascade_c3`, part of an apparent security-hardening pass whose migration files were never committed (several sibling `l3_*`/`harden_*`/`*_taskNN` migrations from the same date show the same gap, parked as a broader follow-up, not reconciled here); this entry commits a migration matching live state, verified byte-for-byte identical before and after — a documentation fix, not a behavior change; applied via `apply_migration`; PR pending on unicorn-cms-f09c59e5](entries/2026-08-12-delete-document-cascade-drift-reconcile.md) · author: Claude (session run by Carl)

- [2026-08-12 — Manage Documents duplicate cleanup — Carl asked to clean up the "Duplicates" documents flagged in Manage Documents, using the heuristic that a document attached to a package stage is the "original"; found that heuristic needed refinement, since this system deliberately keeps a separate document row per package for shared templates (not a duplicate) — the real duplicate signal is multiple document rows linked to the *same* `package_stages.id`; found 7 genuine same-stage duplicate clusters (18 documents), the worst being 6 identical rows for "Q1.D3-RPL VET Student and Assessor Kit" all wired to one KS-RTO stage — a live client-facing bug, since each duplicate link generates its own `document_instances` row per client, so real active clients were each seeing the same requirement 6 times; verified safe before deleting anything: zero of the ~700 combined historical instances across all 18 candidates had ever been fulfilled (no `generated_file_url`), and zero rows existed in the non-cascading reference tables that could block or silently orphan on deletion; deleted 10 unambiguous duplicates automatically (lowest ID per cluster kept) plus one more (of a held-back pair with mismatched photo attachments) after Carl's explicit review; also surfaced separate pre-existing drift in `delete_document_cascade` (live-patched, never committed) as a parked follow-up, not fixed in this pass; second pass same day tackled the remaining 66 flagged docs — 24 confirmed legitimate cross-package copies (no action), 16 orphan-siblings-of-a-linked-original (all zero-usage, deleted after confirming none had a distinct SharePoint link — one, id 80, shared its kept twin's exact SharePoint source and was included), and 10 fully-orphaned with no original anywhere (deleted per Carl's explicit call, accepting the small risk one of those document types might need re-adding later); net result after two passes 92 → 24 flagged, 598 → 561 total documents; third pass same day investigated whether the remaining 24 (11 groups, all legitimate per pass two) could still be consolidated using a SharePoint-link heuristic Carl proposed — found only 2 of 11 groups had a link at all, and 3 of 11 ("GTO & Apprentice Induction Checklist", "GTO Host Employer Handbook", "GTO & Host Employer Induction Checklist") weren't duplicates at all but genuinely different VIC/NSW state-specific content sharing a generic title (flagged as a separate product question, not actioned); reviewed the other 8 clusters one at a time with Carl and merged each onto a single canonical document — first time this codebase deliberately shares one document row across multiple packages via multiple stage_documents links, done by repointing stage_documents and migrating document_instances (not deleting) since every cluster had real active client usage on both sides; overrode the default "latest id wins" rule twice where the latest id was actually the worse-maintained, unused copy; net result across all three passes 92 → 6 flagged (the 3 GTO groups), 598 → 551 total documents; all applied directly to prod via `apply_migration` with Carl reviewing and approving each cluster; PR pending on unicorn-cms-f09c59e5](entries/2026-08-12-manage-documents-duplicate-cleanup.md) · author: Claude (session run by Carl)

- [2026-08-11 — Audit feature correctness review (follow-up) — Carl asked for a full audit of the Audit feature after the two race fixes earlier today; combined a background code-review pass with a live test audit against Test RTO A; corrected a false positive first (a code-review agent read the stale, un-pulled shared checkout and wrongly flagged the morning's upsert fix as missing — verified against origin/main, it's genuinely there); found and fixed three compounding bugs stacked in the same two Timeline-event inserts (audit_created/audit_completed had never once succeeded, ever): a CHECK-constraint drift missing both event types (same root cause as the 10 Aug entry), both inserts missing the required client_id column, and both using an invalid source value ('internal', likely confused with the separate visibility column) — live-verified via a manual insert matching the exact corrected shape; also fixed two silent-failure UX bugs (a corrective-action sync failure on audit completion was indistinguishable from "nothing to sync", and the shared audit-update mutation had no onError at all) and one invalid-DOM-nesting bug (a Badge div inside a p, reproduced live by rating a real question); live-verified the morning's client_audit_responses fix by rating and adding notes to a real question — one row throughout, no duplication; verified findings creation and auto-derived risk rating work correctly; parked several lower-confidence items (a fully-dormant legacy scoring system, a tab deep-link gap, a shared-component a11y issue, unconfirmed RLS-only authorization on three edge functions); test audit fully deleted afterward (all FKs cascade or SET NULL cleanly); PR pending on unicorn-cms-f09c59e5](entries/2026-08-11-audit-feature-review.md) · author: Claude (session run by Carl)

- [2026-08-11 — `seat_rebalancing_recommendations` duplicate-recommendation race (follow-up sweep) — after fixing the client_audit_responses race, Carl asked to sweep the rest of the codebase for the same check-then-write anti-pattern; found one other confirmed-live instance in `useSeatHealth.tsx`'s `generateRecommendations` (checked for an active recommendation via `.maybeSingle()` with no error check, no constraint backing the key) — traced reachability and found its only caller (`SeatHealthWatchlist.tsx`'s Recalculate button) has zero importers anywhere in `src/`, so this was prevention rather than an active incident; fixed with a partial unique index (`WHERE status IN ('new','acknowledged')`, since a resolved recommendation legitimately recurring later isn't a duplicate) plus an atomic `INSERT ... ON CONFLICT ... DO NOTHING` SQL RPC rather than a plain upsert, since PostgREST can't target a partial-index conflict directly; three other same-shaped-but-already-constraint-protected spots (`useScorecardMetrics.tsx`, `useAcademyCertificates.ts`, `useClientImpact.tsx`) left as a parked follow-up; applied directly to prod Supabase with Carl's explicit approval; PR pending on unicorn-cms-f09c59e5](entries/2026-08-11-seat-health-recommendation-race.md) · author: Claude (session run by Carl)

- [2026-08-11 — `client_audit_responses` duplicate-row race on audit response save — Carl reported an auditor unable to save notes or ratings on Standard 1.2 of a mock audit, with progress counters incrementing anyway; root-caused to `upsertResponse`'s manual check-then-insert/update (no real atomic upsert) combined with no unique constraint on `(audit_id, question_id)` — two near-simultaneous saves 33ms apart both passed the "row doesn't exist" check and both inserted, and once ≥2 rows existed every later check's `.maybeSingle()` error was silently swallowed, permanently forcing every future save for that question to insert yet another row (63 duplicates accumulated over one session; confirmed via a full sweep that no other question in the database was affected); reconstructed and verified the intended final answer (Compliant, with the stable final notes sentence) with Carl from the row history, consolidated the 63 rows into one, added a `UNIQUE (audit_id, question_id)` constraint, and switched the mutation to a real `.upsert(..., { onConflict })`; applied directly to prod Supabase with Carl's explicit approval; worked from a git worktree since another tool held uncommitted changes in the shared checkout; PR pending on unicorn-cms-f09c59e5](entries/2026-08-11-audit-response-duplicate-race.md) · author: Claude (session run by Carl)

- [2026-08-11 — Xero invoice activity on client Timeline + Client Activity feed — Carl asked to check whether the Xero webhook (built 2026-08-05, blocked pending Nova's portal registration) was actually working; confirmed live via edge function logs and a lone tenant cache update matched to the second, outside the 6-hourly cron batch — addendum added to the 2026-08-05 entry, no code change; then asked for Xero invoice paid/due activity to reach the client Timeline and the portfolio-wide Client Activity feed, both of which read `client_timeline_events` generically, so a new DB trigger on `tenants` (firing only on a genuine paid/due-date change, not the 6-hourly no-op recheck) plus two new event types wired both surfaces at once; rebuilt the CHECK constraint from the live definition rather than an old migration file to avoid repeating the exact drift bug from a prior entry; caught a `to_char` padding bug (short month names) via a rolled-back dry-run before the first real apply; verified live via Playwright using Test RTO A (not a real client) for temporary test events, deleted after confirming rendering; PR pending on unicorn-cms-f09c59e5](entries/2026-08-11-xero-invoice-timeline-events.md) · author: Claude (session run by Carl)

- [2026-08-11 - Academy certificate quiz completion - Tiziana Russo's "Understanding the RTO Credential Policy Landscape" enrollment was 100% lesson-complete but had no certificate; found required-assessment gating was working, but the UI hid assessment entry points once the enrollment status became `completed`, and the certificate trigger could not re-fire when a required quiz was passed after lesson completion; hotfix shows assessment access for completed-but-uncertified enrollments, allows assessment submission against completed enrollments, repairs lesson-completion RPC drift from `status` to `is_completed`, and centralises certificate issuance so completion and assessment-pass paths both issue idempotently; PR pending on unicorn-cms-f09c59e5](entries/2026-08-11-academy-certificate-quiz-completion.md) · author: Codex (session run by Carl)

- [2026-08-10 — Audit Schedule backdating (log vs schedule) — Carl asked why the Schedule tab's date pickers (Evidence due, Opening, Closing) can't be set to a date before today, and what to do if a meeting already happened; found all three share one `AppointmentPanel` component with a hardcoded `disabled={(d) => d < today}` guard on its Calendar; lifting that alone wasn't enough, since the same mutation always creates a calendar event and sends a real Outlook meeting invite (`send_invites: true`) for opening/closing meetings — backdating unchanged would have sent an invite for a date already in the past; found the stage-task system already models a `'conducted'` milestone distinct from `'scheduled'` (per-stage task-ID mappings confirmed via `pg_get_functiondef`) that the frontend never called; on Carl's follow-up that labelling an already-concluded meeting "Schedule" doesn't make sense, changed the submit button to read "Log" (with an inline hint) for a past date, and wired backdated submissions to skip the calendar invite, use the `'conducted'` milestone, and land the appointment as `completed` immediately (stamped with the meeting's own date, not "now") rather than sitting in "scheduled" awaiting a separate manual step; confirmed live in the browser that past dates are selectable and the button relabels correctly, but did not submit an actual backdated entry against the real audit to avoid mutating live client data as a side effect of testing; PR pending on unicorn-cms-f09c59e5](entries/2026-08-10-schedule-backdate-log.md) · author: Claude (session run by Carl)

- [2026-08-10 — Audit report client-facing content fixes — Tanya's generated audit-report PDF had a raw finding UUID and internal jargon in "Risk Rating Rationale" with no way to edit it directly, a "Section Rollup" table showing "Not scored"/"—" despite real Findings counts, and Opening Meeting responses badged "NOT ANSWERED" despite visible answer text plus internal `[Context]`/`[Changes]` labels leaking through; found the live PDF generator (`generate-client-audit-report`) had never been checked into git at all — pulled its live source and committed it for the first time; root-caused NOT ANSWERED to a wrong rating key (`'na'` vs the real `'not_applicable'`) affecting every N/A response report-wide; root-caused Section Rollup to `client_audit_sections.score_total/score_max` never being written anywhere in the app (only an audit-wide total exists) — per Carl, dropped the dead Score column and derived Risk instead from findings' existing `priority` field; suppressed `[clause]` labels for Opening Meeting specifically via `audit_phase`; added direct inline-edit affordances for Executive Summary, Overall Finding, and Risk Rationale in `ReportTab.tsx` (previously only editable by discarding and regenerating an entire AI draft) and fixed the AI drafting prompt (plus a new validator check) to stop citing raw finding UUIDs and use client-appropriate plain language; diffed both edited-but-preexisting edge functions against their live source before redeploying to rule out unrelated drift; applied all three edge function updates to prod with Carl's explicit approval; merged](entries/2026-08-10-audit-report-client-facing-fixes.md) · author: Claude (session run by Carl)

- [2026-08-10 — `client_timeline_events` event-type constraint drift — "Finalise Package" failed with a check-constraint violation on `timeline_valid_event_type`; root-caused to the constraint being hand-rewritten from scratch by every migration that adds a new event type, so a 2026-08-05 migration (for an unrelated tenant-status feature) silently dropped `package_status_changed` — added the day before for package-instance state changes — by rebuilding from a stale base list; confirmed live that zero `package_instance_state_log` rows exist since 2026-08-04, meaning every membership-state transition (pause/resume/cancel/complete, not just Finalise) had been silently rolling back for 6 days; cross-checked all 24 triggers writing to the table against the live constraint and found one more instance of the same pattern (`action_item_comment`, never in the constraint since a month before it existed, 0 rows ever written) and fixed both in one migration with Carl's go-ahead; verified live via rolled-back dry-run transactions for both; also noted but did not fix a third stale copy of the allowed-types list in `emit-timeline-event.ts` (currently dormant, no live caller hits the gap); worked from a git worktree since Codex was active in the shared checkout; applied directly to prod with Carl's explicit approval; PR pending on unicorn-cms-f09c59e5](entries/2026-08-10-timeline-event-type-constraint-drift.md) · author: Claude (session run by Carl)

- [2026-08-10 — `eos_todos` null tenant_id on IDS-to-do creation — "Mark as Solved" with attached to-dos failed with a not-null constraint violation on `eos_todos.tenant_id`; root-caused to `useEos.tsx`'s `createIssue` never defaulting `tenant_id` (unlike its sibling mutations in the same file), leaving 31 of 60 `eos_issues` rows with `tenant_id = NULL`, which `create_todos_from_issue` then copied straight into the NOT NULL `eos_todos.tenant_id`; backfilled the 31 rows to `6372` (EOS is Vivacity-internal only — verified live, no other tenant ever present), added a `COALESCE(tenant_id, 6372)` fallback to the RPC, and defaulted `createIssue`'s `tenant_id` client-side; applied directly to prod with Carl's explicit approval; PR pending on unicorn-cms-f09c59e5](entries/2026-08-10-eos-todos-null-tenant-id.md) · author: Claude (session run by Carl)

- [2026-08-07 — invitation resend timeline deduplication — concurrent resend requests created duplicate timeline rows 76ms apart; serialized the invitation trigger and added a one-minute duplicate guard, with a narrow cleanup of sub-second duplicate rows](entries/2026-08-07-invitation-resend-dedupe.md) · author: Codex (session run by Carl)

- [2026-08-07 — User invitation lifecycle wired into the client Timeline — Carl asked for Manage Invites status updates (invited, pending, clicked, bounced, verified) to be logged on the client Timeline, having noticed no invitation activity appeared there at all; confirmed live that click/bounce/delivery tracking already existed and was live (via `mailgun-webhook` + `reconcile-invite-delivery-status`) but zero of it reached `client_timeline_events` despite 226 real invitations; added four new event types (`invitation_sent`, `invitation_clicked`, `invitation_bounced`, `invitation_accepted`) and five triggers on `public.user_invitations` covering every write path (`invite-user`'s INSERT, `resend-invite`'s UPDATE, `mailgun-webhook`'s two UPDATE branches, `accept_invitation_v2`'s UPDATE) — deliberately not reusing the existing `account_invited`/`account_activated` types since their only live writers are two unrelated flows (ghost-user bulk activation, account enable/disable toggle); backfilled full history (226/9/3/110 rows across the four types); dry-run tested in a rolled-back transaction before applying for real; grouped under the existing "Accounts" Timeline filter chip rather than adding a new one; verified live via Playwright against a real tenant (MCC Adelaide) showing correct rendering, icons, filter, deep link, and attribution; applied directly to prod Supabase with Carl's explicit approval; PR pending on unicorn-cms-f09c59e5](entries/2026-08-07-invitation-lifecycle-timeline-events.md) · author: Claude (session run by Carl)

- [2026-08-07 — Portfolio-wide "Client Activity" dashboard widget + feed page — Carl asked whether the per-client Timeline feature could surface on the main staff dashboard (`/dashboard`, `MainDashboard.tsx`) as a live, cross-client activity feed with a "View all" link to a full page; found `client_timeline_events` RLS was already portfolio-wide for all Vivacity staff roles (no new RPC/RLS needed) but discovered several existing hooks (e.g. `useRiskCommandCentre.ts`) subscribe to `postgres_changes` on tables never added to the `supabase_realtime` publication, so those realtime subscriptions silently never fire — fixed for `client_timeline_events` only (added to the publication) and paired with a 30s polling safety net so the new widget doesn't share that same latent failure mode; added `usePortfolioTimeline.tsx`, a "Client Activity" panel on `MainDashboard.tsx`, and a new `/client-activity` page (`ClientActivityFeed.tsx`) reusing the per-client Timeline's filter/icon/colour maps (exported for reuse rather than duplicated); also found and fixed a pre-existing, unrelated bug while touching `TimelineEventCard.tsx` — several timeline "View X" deep links pointed at a `/clients/:id` route that has never existed (real route is `/tenant/:id`), so those buttons have been dead links since they were written; verified live end-to-end via Playwright (real cross-client data including the same-session time-entry-timeline fixes, filter chips, row-click-through to the client's own Timeline tab); applied the Realtime-publication migration to prod with Carl's explicit approval; **same-day addendum**: redesigned the `/client-activity` filter bar (reused `TenantUsers.tsx`'s "Filter by Tenant" dialog for full status/CSC parity rather than a bespoke control) and reworked the main dashboard's 3-column grid layout per Carl's spec; while testing, Carl reported Client Health showing "No client data" on his own dev account — root-caused via `get_logs` to the portfolio-wide health query genuinely timing out under RLS (per-row `app.user_can_access_tenant` re-evaluation compounding with several expensive per-tenant joins), not an RLS-empty result as first assumed, surfacing as a swallowed REST 500; fixed with a new `rpc_portfolio_client_health()` SECURITY DEFINER RPC (row_security off, gated on `is_vivacity_team_safe`) computing just the aggregate counts server-side, verified live against Carl's own account; PR pending on unicorn-cms-f09c59e5](entries/2026-08-07-portfolio-client-activity-feed.md) · author: Claude (session run by Carl)

- [2026-08-07 — sync time_entry_allocations on package change — Dave reported MCC Adelaide M-GR Package Burndown over while monthly totals looked right after reallocating time to the previous package; root-caused to `fn_reallocate_time_entry` never refreshing allocations when `package_instance_id` changed (EditTimeDialog only updated the entry), while burndown reads allocations and monthly read the entry package; fixed the trigger to pin allocations to the chosen package on package change (and keep duration edits from snapping historical moves back to active memberships), repaired 334 stale single-alloc rows (~102 billable hours across many tenants), and made the Time-tab monthly/closed summaries allocation-aware; multi-alloc RTO/CRICOS splits left alone; applied to prod same session; PR pending on unicorn-cms-f09c59e5](entries/2026-08-07-sync-time-entry-allocations-on-package-change.md) · author: Composer (session run by Carl)

- [2026-08-07 — Manual/timer/reallocated time-entry timeline coverage + missing "Time" filter chip — Carl asked whether client-logged time entries show up on the client Timeline; found the existing `time_posted`/`time_ignored` event types and their trigger (`fn_time_entry_timeline_trigger`) only fire on `calendar_time_drafts` status transitions (calendar-draft-posted time), so manually logged time (`AddTimeDialog.tsx`) and timer-based time both produced zero timeline events — confirmed live via 0 `entity_type='time_entry'` rows despite 575 manual + 6 timer time_entries; added a new trigger scoped to `source IN ('manual','timer')` only (excluding already-covered `calendar`, bookkeeping-only `system` carry-over adjustments, and the separate `clickup` sync pipeline, parked as an open question), plus a 90-day/377-row backfill; separately confirmed a note linked to a time entry already correctly reaches the Timeline via the existing `public.notes` trigger, no fix needed there; also fixed a pre-existing gap where the Timeline UI had no "Time" filter chip at all despite the backing filter map already defining one; **same-day follow-up**: Carl asked whether time *reallocation* between packages (citing MCC Adelaide as a real example) was also considered — it wasn't, so added a new `time_reallocated` event type + trigger on `time_entries` `package_instance_id` UPDATEs (separate from the existing `fn_reallocate_time_entry` allocation-bookkeeping trigger, which a concurrent Cursor/Composer session fixed for a related-but-distinct bug — see the entry directly above — with no functional overlap since that trigger writes `time_entry_allocations` and this one writes `client_timeline_events`), confirmed live via `time_entry_audit_log` (113 all-time reallocations, 69 in the last 90 days) including the real rapid back-and-forth on tenant 6277 (MCC Adelaide Pty Ltd) that Dave's fix above was itself responding to; backfilled in full (113 rows, small enough unlike the notes table); applied directly to prod Supabase across two separate migrations, each requiring its own explicit Carl approval after the harness's permission layer blocked the first `apply_migration`/test-insert attempts; PR pending on unicorn-cms-f09c59e5](entries/2026-08-07-manual-time-entry-timeline.md) · author: Claude (session run by Carl)

- [2026-08-06 — user_invitations RESTRICTIVE policy outage — Carl reported Manage Invites and the Tenant Users tab broken in production; root-caused to the same-day `user_invitations_restrict_scope` RESTRICTIVE policy (from the Cursor-agent security migrations reconciliation) referencing `auth.users` directly in a plain RLS clause instead of a SECURITY DEFINER helper like its sibling checks — `authenticated` has no grant on `auth.users`, and because the policy is FOR ALL, Postgres permission-checked that table for every query against user_invitations including SELECT, breaking it outright for every caller, not just narrowing results; the same pattern had shipped safely before in a pre-existing UPDATE-only policy that never got permission-checked on a SELECT; fixed by reading the email from the JWT claim instead of the table; re-verified clean across Manage Invites, the Client Detail Users tab, Team Users, and Bulk Invite; verification gap acknowledged — the reconciliation session tested the invitee RPC path but never the staff direct-query path before applying to prod; PR pending on unicorn-cms-f09c59e5](entries/2026-08-06-user-invitations-restrictive-policy-outage.md) · author: Claude (session run by Carl)

- [2026-08-06 — primary/secondary contact swap + timeline role events — Carl reported Swap Primary Contact from Manage Tenants → Users was broken; root-caused to `confirmPrimarySwap` demoting the existing primary to secondary while the promotee still held secondary, which hit `uniq_tenant_one_secondary_contact` and aborted; rewrote `set_relationship_role` to free unique primary/secondary slots atomically (via internal `_apply_relationship_role_row`), emit internal-only `account_role_changed` timeline events for each final role change, simplify the frontend swap to a single promote RPC, and backfill ~28 historical role changes from `audit_eos_events`; when promoting to primary while a different user already holds secondary, outgoing primary demotes to User instead of Secondary; PR pending on unicorn-cms-f09c59e5](entries/2026-08-06-primary-secondary-swap-timeline.md) · author: Composer (session run by Carl)

- [2026-08-06 — Ask Viv client-portal assistant (replaces compliance-assistant-client) — Carl asked to bring the new agentic staff Ask Viv assistant to the client portal plus a portal/Vivacity Academy navigation capability; found a client-facing Ask Viv already existed and is live (`compliance-assistant-client`/`ClientAskVivPanel`, deterministic/Gemini-phrased) — Carl confirmed replacing it outright with a new tenant-locked sibling of the staff agentic assistant (direct-Anthropic Claude Sonnet, agentic tool-use) rather than dual-running; kept the original client-mode spec's Standards/regulatory exclusion; found `client_timeline_events` already has RLS-enforced `visibility='client'` scoping (notes/documents default to the hidden `'internal'` value), so using the RLS-scoped client rather than service-role makes the new account-context tool's timeline feed leak-proof by construction rather than by app-layer filtering alone; new `ask_viv_client_conversations`/`ask_viv_client_turns` tables + `app_settings` staged-rollout flags, and the new `ask-viv-assistant-client` edge function (5 tenant-locked tools: account context, document search, Academy progress, portal-page finder, deadlines) with `_shared/ask-viv-client-redaction.ts`/`_shared/ask-viv-client-navigation.ts` shared modules — both applied/deployed directly to prod Supabase mid-session once the MCP connection came up, with Carl's explicit approval, ahead of the PR merging (RLS-enabled and advisor-clean verified, function smoke-tested post-deploy); `ClientAskVivPanel.tsx` rewritten in place for the new free-text response contract; old function left deployed but unmounted pending a later decommission; **same-day follow-up round** added a token-based daily usage cap (new `ask_viv_client_assistant_usage` table + `app_settings` column, on top of the existing request-count cap), a sixth tool (`get_invite_status`, diagnoses expired/bounced/never-signed-in invites), and — after Carl flagged an "Academy Team Members" page from memory as not real — live Playwright verification confirmed it renders placeholder data unconnected to the real tenant, so it was removed from the navigation manifest; QA also found and fixed three real production bugs, plus two frontend UX gaps: `app_settings`' own RLS silently zero-rowing every client's rollout-flag/cap check (fixed via a service-role client for config reads only), the shared staff fact-builder module unconditionally rejecting every client role so `get_my_account_context` always failed (fixed via a new `caller_role_class` flag), and `get_invite_status` querying a view whose grant was revoked by an unrelated same-day security fix — a mid-session branch checkout by another AI tool (Cursor) in the same shared working directory reverted this round's then-uncommitted work from disk (prod was unaffected, redone via a git worktree — see `AGENTS.md`); rollout flag still defaults `false`, so nothing is client-visible yet; branch `feat/ask-viv-client-assistant`, merged](entries/2026-08-06-ask-viv-client-assistant.md) · tag: `audit-2026-08-06-ask-viv-client-assistant` · author: Claude (session run by Carl)
- [2026-08-06 — client-tenant-users RPC fix and cron.job deferral — direct follow-up to the same-day security migrations reconciliation; the deferred `v_client_tenant_users` fix turned out bigger than the auth.sessions grant issue already documented — anon/authenticated held a plain unscoped GRANT SELECT on the view with no tenant-membership check at all, so any caller (including unauthenticated anon) could read any tenant's full roster by changing the tenant_id filter client-side, and the originally-proposed security_invoker fix would also have broken the view's actual "team directory" product intent (every member, not just admins/contacts, is meant to see the roster); replaced with a tenant-scoped SECURITY DEFINER RPC gated on superadmin/staff/actual-membership, verified via rolled-back dry-runs against real accounts then smoke-tested end-to-end through the real /client/users page as both a primary_contact and an academy_user; separately, Carl decided to accept the blocked #171 cron.job PUBLIC SELECT grant as a deferred risk rather than filing a Supabase Support ticket, after confirming there's no self-service path (supabase_admin owns the table, Supabase's own docs point to Support for this class of issue) and that `cron` isn't in this project's PostgREST exposed-schema list so it's defense-in-depth only; PRs #185 (merged) and #186, both on unicorn-cms-f09c59e5](audit/2026-08-06-client-tenant-users-fix-and-cron-deferral.md) · tag: `audit-2026-08-06-client-tenant-users-fix-and-cron-deferral` (to be applied on merge) · author: Claude (session run by Carl)

- [2026-08-06 — Cursor-agent security migrations reconciliation — Carl asked to verify six Cursor-agent-authored security-hardening PRs (#166, #171–176) that Angela merged the previous day hadn't broken anything; found none of the five SQL migrations had actually reached prod despite merging to `main` (confirmed via `schema_migrations`, live extension/advisor state, and direct policy/grant checks) — root cause was `supabase db push` failing in CI since at least 2026-08-04 due to pre-existing migration-history drift between git and remote, unrelated to these PRs; the sixth change (#174, `APP_BASE_URL` fallback) had deployed independently via Lovable's own sync and was confirmed live and correct; before applying, found #172's `security_invoker` fix would have broken prod outright for `v_client_tenant_users` specifically (lateral-joins `auth.sessions`, which `authenticated`/`anon` have no grant on — reproduced live via a rolled-back `SET LOCAL ROLE authenticated` transaction, same failure shape as the 2026-07-22 invite-link incident) — applied #172 only to the two other views, deferred that one with a fix path noted; also found #171 (cron.job) and #173/#176 (pg_net relocation) are blocked at the `postgres` role's privilege level (grants/extension owned by `supabase_admin`, a real superuser `postgres` isn't a member of) and can't be applied via any SQL-based path available today; applied #166 and #175 as written after verifying them safe; corrected a mislabeling of which pg_net duplicate migration (#173 vs #176) had the more complete fallback logic and kept the right one; hand-applied hotfix reconciling migration files with what actually landed on prod, PR #184 on `unicorn-cms-f09c59e5`, not yet merged](audit/2026-08-06-cursor-security-migrations-reconciliation.md) · tag: `audit-2026-08-06-cursor-security-migrations-reconciliation` (to be applied on merge) · author: Claude (session run by Carl)

- [2026-08-06 — Unicorn 1 redirect override (`tenants.unicorn1_id`) — Carl asked for a way to fix the "Unicorn 1" redirect buttons on Client Detail and the Client Portal, which built `https://unicorn-cms.com.au/clients/{tenants.id}` directly and only worked for tenants imported 1:1 from Unicorn 1 (`import-unicorn1-client` sets `tenants.id = client_id`); Carl explained Unicorn 1's own auto-increment sequence has drifted 1 ahead, so organically-created Unicorn 2 tenants have no valid corresponding Unicorn 1 record at their own id; added nullable `tenants.unicorn1_id` plus a `BEFORE INSERT` trigger (`trg_set_default_unicorn1_id`) defaulting it to `id + 1` for organic tenant creation, while `import-unicorn1-client` was patched to explicitly set `unicorn1_id = client_id` on import so the drift-correction default doesn't misfire there (no offset needed for genuine imports); both redirect buttons (`TenantTimeTrackerBar`, `CompliancePulseBanner`) now use `unicorn1_id ?? tenant's own id`; added a new "Unicorn 1" panel to the Client Detail Integrations tab (`Unicorn1Card.tsx`, matching the existing ComplyHub/Xero card pattern) for manual override; hand-applied hotfix (direct git edit, not routed through Lovable), migration applied to prod Supabase same session with Carl's explicit approval; PR #181, merged](audit/2026-08-06-unicorn1-id-override.md) · tag: `audit-2026-08-06-unicorn1-id-override` · author: Claude (session run by Carl)

- [2026-08-05 — Xero invoice status cache, scheduled sync, Manage Tenants pill/filter, webhook — continuation of the same-day Xero OAuth work; Carl asked for a paid/unpaid pill on the client detail header and Manage Tenants, a matching filter, and mid-build asked whether Xero's webhook feature could give near-real-time updates instead of waiting on a schedule; Manage Tenants loads every tenant in one unpaginated query so a live Xero call per row would blow through rate limits — added `tenants.xero_invoice_paid`/`xero_invoice_due_date`/`xero_invoice_checked_at` (schema change, no RLS change) as a cache refreshed by a new 6-hourly `xero-invoice-sync-all` cron job (batches 25 comma-separated Xero `ContactIDs` per call, not one call per tenant) and by the existing manual "Check Xero" click; added `xero-webhook` for the fast path, which has to re-derive "most recent invoice" itself rather than trusting the notified invoice is current (webhook events only carry an invoice ID) — not yet activated, blocked on Nova completing the Xero portal webhook registration; found and fixed a real bug during live QA where `xero-invoice-status` was missing a `.trim()` fix already applied to the other three Xero functions, causing the connection's token refresh to fail with `invalid_client` once the access token actually expired mid-session — confirmed self-healed after the fix since the failed attempt never invalidated the stored refresh token; added a shared `isXeroInvoiceOverdue` helper after Carl noticed 2024-dated "Due" pills on cancelled tenants reading the same as due-next-week ones, now shown as red "Overdue since" instead; hit and recovered from a GitHub stacked-PR quirk where auto-retargeting a dependent PR after deleting its base branch silently closed it instead — recovered by re-opening the same commit directly against `main`; deliberately still exposes no invoice amounts/numbers/references anywhere in this feature, per explicit instruction; PRs #167, #168, #177 (re-opened after #169 was auto-closed), #170, all merged](audit/2026-08-05-xero-invoice-status-cache-and-sync.md) · tag: `audit-2026-08-05-xero-invoice-status-cache-and-sync` · author: Claude (session run by Carl)

- [2026-08-05 — Xero OAuth integration + invoice paid/unpaid lookup — Carl asked whether a Xero API integration was possible to check client invoice payment status in-app (previously only a manual `xero_contact_url`/`xero_repeating_invoice_url` deep-link into Xero's web UI existed); built the OAuth connection by extending the codebase's existing custom `outlook-auth` pattern rather than Supabase Auth's built-in providers (which don't fit a long-lived server-side org connection) — one shared connection to Vivacity's own Xero organisation (not per-client tenant, not per-staff-member), gated to Super Admin for connect/disconnect and any Vivacity staff for status, reusing the existing `oauth_tokens`/`oauth_states` tables with a new `provider_account_id` column (schema change, no RLS change) to cache the Xero connection `tenantId` required on the `Xero-tenant-id` header for all Accounting API calls; also built `xero-invoice-status`, which resolves each tenant's Xero `ContactID` directly from the GUID already embedded in its saved `xero_contact_url` (no separate Contact-matching exercise needed), handles Xero's rotating refresh tokens, and surfaces invoice status/amount/due-date in `XeroCard.tsx` via a "Check Xero" button; a `.env` hygiene gap (tracked in git, no `.gitignore` entry since the original Lovable remix commit) was found and fixed in the same session after the Xero Client Secret was briefly added there before being moved to Supabase Edge Function secrets — caught before any commit, so no rotation was required; PR #158, not yet merged, pending Nova Canto (confirmed `Standard` role + `Connect Apps` permission on the real Vivacity Xero org) adding the redirect URI and completing the one-time Xero authorisation](audit/2026-08-05-xero-oauth-provider-account-id.md) · tag: `audit-2026-08-05-xero-oauth-provider-account-id` (to be applied on merge) · author: Claude (session run by Carl)

- [2026-08-05 — Audit workspace section duplication fix (data repair) — surfaced mid-session during dark mode QA when Carl spotted visibly duplicated sections ("Opening Meeting" x4, "Outcome 1 — Training" x3+) on a live audit's Audit Form tab; root-caused to `useInitializeSections` having no server-side idempotency check, only a local React `initialized` `useState` flag that resets on remount — confirmed via SQL this hit one real, in-progress audit for RTO client AMR (`4a5fc12d`) on 4 separate days, taking 19 template sections to 76 rows and scattering 9 real auditor responses across duplicate rows for 3 sections; checked scope across all audits first and confirmed isolated to this one; checked question-by-question for conflicting answers across duplicates and found none, so remediation was a clean re-point rather than a merge-conflict decision; repaired directly via SQL with Carl's explicit approval (re-pointed the 9 responses to a canonical row per `template_section_id`, verified response count unchanged at 11 before deleting anything, deleted the 57 now-empty duplicates, verified 0 orphaned responses after); fix re-checks for existing sections inside the mutation itself before inserting, closing the hole regardless of how many times or from where the effect fires; same session also swept the whole Audit workspace (Overview, Audit Form, Findings, Actions, Schedule tabs, plus the shared `Badge` component) for the recurring "hardcoded light pastel colour, illegible or jarring against a dark canvas" pattern from the broader dark-mode rollout, confirmed via computed styles (not just visual inspection) on a real extreme-risk audit; PRs #156 (idempotency fix) and #157 (dark-mode sweep), both merged](audit/2026-08-05-audit-workspace-section-duplication-fix.md) · tag: `audit-2026-08-05-audit-workspace-section-duplication-fix` · author: Claude (session run by Carl)

- [2026-08-05 — Tenant status change timeline fix — Carl asked who changed Australian National Education College (tenant 7444) from On Hold back to Active, and asked for tenant status changes to show up in the client Timeline; found via `client_audit_log` that Carl Simpao set it to On Hold on 2026-07-28, then Dave Richards set it back to Active on 2026-08-04; separately found why status changes never reached the Timeline in the first place — `TenantStatusDropdown.tsx` called `rpc_create_client_note(p_note_type: 'status_change')` to auto-record it, but that RPC's `note_type` CHECK constraint never included `'status_change'`, so the call silently failed every time platform-wide (confirmed zero rows ever written, for any tenant); fixed with a new DB trigger (`fn_tenant_status_timeline_trigger`, AFTER UPDATE on `tenants.status`) so the event fires for any code path, not just this one component, plus a new internal-only `tenant_status_changed` Timeline event type, and a full backfill of all 61 historical status changes from `client_audit_log`; removed the dead `rpc_create_client_note` call and its now-unused `clientId` prop rather than fixing the RPC's validation, since the trigger already owns this and fixing both would have produced duplicate timeline entries; verified live against Test RTO A (toggle + restore) before shipping; PR #154, merged](audit/2026-08-05-tenant-status-timeline-fix.md) · tag: `audit-2026-08-05-tenant-status-timeline-fix` · author: Claude (session run by Carl)

- [2026-08-04 — Client Timeline expansion (notes, logins, messages, Academy, stage progression, page views, package status) — extended the existing (not greenfield — already a unified, `event_type`-discriminated `client_timeline_events` table with a mature UI) client Timeline into a comprehensive activity feed per Carl's request, across seven hand-applied hotfix phases, all feeding the Ask Viv Assistant RAG corpus for free since `client_timeline_events` was already a registered ingestion source: Phase A wired the previously-unlinked legacy structured-notes system (`documents_notes`) in, mirroring the existing `client_notes` trigger pattern; Phase B surfaced client-portal logins (internal-only) by extending the existing `handle_user_login()` trigger, resolving tenant ambiguity the same cautious way `ClientTenantContext.tsx` already does; Phase C surfaced client↔CSC messages (client-visible) and in the process found and fixed a real pre-existing production bug — a stale `CHECK (source IN ('system','user'))` constraint from the table's original creation was never dropped when a later migration widened the allowed set to include `unicorn`/`microsoft`, so Postgres's simultaneous-constraint-enforcement meant **no row with `source='unicorn'` or `'microsoft'` had ever been insertable in production**, silently blocking both this phase and the Microsoft integration's own timeline events the whole time; Phase D surfaced Vivacity Academy engagement (enrollment/lesson-completion/certificate, internal-only, confirmed via grep that no Academy engagement detail reaches any client-facing page today); Phase E, added mid-session on Carl's follow-up ask, surfaced stage-progression status changes on `stage_instances` (the actively-used, CSC-facing stage tracker — flagged in the PR that a second, parallel stage-state system, `client_package_stage_state`, exists by design but has no UI driving it, in case that was the intended target instead); Phase F built genuinely new client-portal page-view tracking (confirmed nothing like it existed before) — a raw per-page table written only via a `SECURITY DEFINER` RPC, and a daily `pg_cron` digest rolling each user's prior-day page views into one internal-only timeline event rather than flooding the feed, using the table's existing `dedupe_key` mechanism for idempotency; every phase verified end-to-end against live QA data (Test RTO A) before merging, with test data cleaned up and original state restored each time; Carl approved a stacked-PR-hold pattern initially then said mid-session to just merge to `main` directly, so all six merged immediately after each passed verification; **post-ship correction**: Carl reported a note wasn't appearing in the Timeline, which surfaced that Phase A had wired its trigger onto `public.documents_notes` — confirmed a completely dead table (zero rows, zero code references anywhere) — instead of `public.notes`, the table actually backing the "Structured Notes" tab; fixed by retiring the dead trigger and wiring the real one, then backfilling recent notes retroactively, which in turn surfaced that `public.notes` holds the full "Unicorn 1" legacy migration history (11,340 rows back to 2014 across 357 tenants) — an unscoped backfill was run first, found far larger than intended, rolled back cleanly, and redone scoped to the last 90 days (793 rows/69 tenants) per Carl's explicit direction; **Phase G**, added in a later same-day session prompted by a Governance Documents visibility investigation (Start Training Group), surfaced `package_instances.membership_state` transitions (cancel/hold/finalise/resume, internal-only) via a trigger mirroring Phase E's pattern, closing a real audit gap where the raw admin edit screen (`PackageDataManager.tsx`) could flip a package to `complete` with zero log/timeline trail — the underlying `v_client_governance_documents` view-visibility bug that prompted the investigation remains separately unfixed; PRs #133-#139, #149](audit/2026-08-04-client-timeline-expansion.md) · tag: `audit-2026-08-04-client-timeline-expansion` · author: Claude (session run by Carl)
- [2026-08-04 — Tenant primary CSC index fix — correcting Silverline College Pty Ltd's (tenant 7493) `lifecycle_status` to match its already-archived `access_status`/`archived_at` fired a churn-closure trigger that closed its CSC assignment stint without demoting `is_primary`, surfacing a pre-existing, already-documented-but-deferred bug: a legacy partial unique index (`idx_tenant_primary_csc`, no `ended_at` awareness) blocking CSC reassignment on any of 27 tenants with a closed-but-still-`is_primary=true` stint; fixed by dropping the stale index, fully superseded by the correct 2026-07-06 M4 index — no data backfill or function changes needed; verified via a rolled-back test insert before shipping; PR #145](audit/2026-08-04-tenant-csc-primary-index-fix.md) · tag: `audit-2026-08-04-tenant-csc-primary-index-fix` · author: Claude (session run by Carl)
- [2026-08-03 — Ask Viv redesign, Phases 3-7 (schema changes + safety-pipeline fix) — closes out the full Ask Viv redesign plan (`bright-crunching-meerkat.md`, Phases 0-7; Phases 0-2 audited in a prior session): Phase 3 wired real LLM generation (Lovable Gateway, `google/gemini-2.5-flash`) into Compliance mode for the first time — previously it built a full tiered prompt, logged it, then answered via hand-written keyword matching, the LLM path never existed; injected previously-dead `srto_corpus` Standards citations into the prompt; narrowed the unwired `intentClassifier.ts`'s hard-block list so `decision_request` ("does this meet the Standard?") reframes instead of blocking outright; wired the built-but-unused safety pipeline; two-write audit model (pre-flight fail-closed insert, post-response best-effort update); new `app_settings` LLM-generation rollout flags (now flipped on for all staff per Carl's explicit in-session instruction); a self-caused mid-session production incident (placeholder content sent in a manual MCP deploy call, taking the function down briefly) caught and fixed same-session, and the CLI-deploy-over-MCP-tool fix from a prior session reconfirmed as the default going forward; Phase 4 added four new fact sources (compliance audit register, notes/emails, tenant-user roster) to the fact builder, catching and fixing a "most recent audit by date picks an unfinished draft over the last completed one" bug during the author's own pre-ship verification; Phase 5 added `ask_viv_conversations`/`ask_viv_turns` (new tables, schema change) as a conversation-history layer distinct from the permanent `ai_interaction_logs` audit trail; Phase 6 added a portfolio-wide "All clients" scope reusing the existing compliance prompt/validator/safety pipeline via an extra-instructions injection rather than a parallel path; Phase 7 added a note-drafting UX reusing `NoteFormDialog`'s existing localStorage draft-restore mechanism with zero new save path, split the single Flag button into a quality flag and a genuine "Escalate to my Team Leader" (`users.manager_uuid` + real `user_notifications`, RLS-verified before shipping), and deleted confirmed-dead code — while also verifying two other plan-named cleanup targets were **not** actually dead (the plan's assumptions there were stale) and leaving both alone; a live Playwright verification pass against a real client tenant (not just SQL dry-runs/curl smoke tests, which is as far as Phases 4-7 had been checked before this point) then found the phrase filter blocking real LLM narration on 2/2 test questions, root-caused to the filter being unable to distinguish an accurate quote of a real audit finding's own wording ("non-compliant") from the model asserting a new determination — fixed with a one-repair-retry addition to the safety pipeline (mirroring the existing repair shape already used for structural validation failures) after a prompt-only attempt was tried and confirmed insufficient first; PRs #114-#120](audit/2026-08-03-ask-viv-redesign-phases-3-7.md) · tag: `audit-2026-08-03-ask-viv-redesign-phases-3-7` · author: Claude (session run by Carl)

### July

- [2026-07-31 — Ask Viv role-gating unification & fact-source correction (Phases 0-1) — started as a 400-error investigation from a screenshot, expanded once Carl asked for a plan to make Ask Viv genuinely useful for CSC staff and research showed the feature was far more broken than its UI let on; Phase 0 unified role-gating — `assistant-answer`'s stale inline 3-role list, a duplicate stale role constant in the fact-builder enforced live inside `validateInput()` (was 500ing every Compliance-mode call from CSC/Integrator/BGT/CET), `assistant_threads`/`assistant_messages` RLS requiring super-admin only, `profile.status`→`state` (a field that doesn't exist), and `AskVivPanel`'s gate bypassing the shared RBAC helper; Phase 1 corrected the fact-builder's data sources — `tasks`/`stages` were reading a raw ClickUp mirror and a global stage-template registry (neither has a `tenant_id` column) instead of the real per-client `tasks_tenants`/`client_package_stage_state`, hours were read from a confirmed-empty legacy `consult_logs` table instead of `time_entries`, `package_hours` never actually populated `used_hours` so "hours remaining" always silently read 100%, `client_action_items` was never queried at all, and freshness derivation queried a nonexistent `tenant_id` column on two tables and had silently never worked; a second production mistake mid-session (a role-list "fix" based on an incomplete migration-history grep, actually a regression against an already-correct consolidated function) was caught via live introspection and corrected same-session, plus a Bugbot-driven follow-up fix for a policy-coverage gap the correction itself introduced, and 5 further Bugbot/Vercel-Agent-Review findings fixed before merge (duplicate stage IDs across packages, wrong-ID-space record links, already-overdue items counted as upcoming, unlimited-package hours-exceeded false positive, missing scope filter); the CI/CD deploy pipeline was confirmed broken pre-existing since ~2026-07-27 (unrelated to this work), so the 3 touched edge functions were manually deployed to prod via the Supabase MCP tool (bundling each function's full transitive dependency tree by hand) with Carl's explicit approval, and verified live post-deploy via Playwright against both a staff Super Admin session and a client-tenant login — confirming the fixes work and separately reconfirming the still-open tenant-context-resolution bug (planned as Phase 2, not yet fixed); PRs #111/#112/#113 merged](audit/2026-07-31-ask-viv-role-gating-and-fact-source-correction.md) · tag: `audit-2026-07-31-ask-viv-role-gating-and-fact-source-correction` · author: Claude (session run by Carl)

- [2026-07-30 — Hours-used pre-term fix (direct migration) — direct follow-up to the same day's package-burndown-view-fix audit: while explaining to Carl why SHCS Academy's package showed "47.75/63 hours used" only weeks after starting, found `fn_package_used_minutes()` (the function `package_instances.hours_used` syncs from, and the same function the burndown fix had used as its ground truth) had no date floor — `allocate_time_entry()` always resolves to "whichever RTO/CRICOS instance is active right now" with no time-awareness, and a one-time bulk reallocation event (all activity clustered within a single 13-hour window, 2026-02-23 20:11 to 2026-02-24 09:19, confirmed via `time_entry_allocations.created_at` — no similar clustering anywhere else in the table, no corresponding migration file, no UI bulk-action found) had re-run that logic against historical time entries back to 2022, permanently re-pointing prior-term work onto whichever instance was active that day; first blast-radius estimate (26 instances/440h) was wrong and self-corrected before writing any fix — a >100% pre-term-to-total ratio on some instances exposed a missing filter in the diagnostic query itself, corrected estimate is 8 active instances/5 tenants/139.6h, with a fuller dry-run across all 1047 instances (including completed ones, for the History tab) finding 21 change, all downward, none negative; ruled out two adjacent concerns first — a stale doc-comment warning about `time_entries.package_id` value-space checked and found not a live risk, and suspected double-counting across combo RTO+CRICOS memberships checked and found to be a correctly-conserved 50/50 split, not a duplication bug; fix adds a `start_date` floor to the calculation only, doesn't touch the underlying allocation rows or `allocate_time_entry()`'s resolution logic; direct hand-written migration, applied via Supabase MCP with Carl's explicit approval, verified against dry-run numbers both in SQL and live in the Client Detail Packages tab (SHCS: 47.75/63 → 9.42/63); PR #90, merged to `main`](audit/2026-07-30-hours-used-pre-term-fix.md) · tag: `audit-2026-07-30-hours-used-pre-term-fix` · author: Claude (session run by Carl)

- [2026-07-30 — Package burndown view fix (direct migration) — following a full Playwright + Supabase audit of the Client page and Client Detail (all 13 tabs, package stage progression, burndown), found `v_package_burndown` (backing the Time tab's burndown cards) never joined `time_entry_allocations`, so any time entry reallocated across packages was invisible to it — verified live on SHCS Academy showing 0:00/63:00 used against an authoritative `fn_package_used_minutes()` of 47.75h; sized the blast radius first (28 of 91 active package instances affected, ~16.3h understated on average) and confirmed `package_instances.hours_used` itself is correct (a red herring ruled out before writing the fix) before touching anything; fix mirrors `fn_package_used_minutes()`'s own allocation-first-then-raw-fallback pattern, dry-run verified via CTE against prod data before writing the migration; direct hand-written migration (not Lovable), applied via Supabase MCP with Carl's explicit approval after an auto-mode permission classifier blocked the first attempt; a larger bundle of unrelated frontend-only Client Detail bugs from the same audit (stage status resolver race, package-refresh wiring, duplicate-package expand-key collision, 3 broken PostgREST embeds, N+1 query batching, tab URL sync, `core_complete` progress counting, locale/icon fixes) shipped in the same PR but touched no schema/data, not detailed here; PR #89, open awaiting Carl's merge](audit/2026-07-30-package-burndown-view-fix.md) · tag: `audit-2026-07-30-package-burndown-view-fix` · author: Claude (session run by Carl)

- [2026-07-29 — FK relationships and TGA status fix (direct migration) — following a full-app read-only Playwright audit (SuperAdmin/Executive/Academy Builder + Internal Staff sections, `unicorn-kb` PR #59), fixed 4 confirmed DB bugs plus 2 more discovered mid-fix: `tga_sync_status()` referenced an unassigned PL/pgSQL `record` (`v_last_job`) whenever no TGA sync had ever run, raising "record not assigned yet" on every load of `/admin/integrations/tga` (distinct from the already-known `l3_gate_tga_sync_cluster` outage in the sync RPC); `compliance_pack_exports.tenant_id` and `stages.created_by` both had the column but no FK constraint at all, so PostgREST's embed syntax 400'd on `/admin/compliance-packs` and `/manage-stages` respectively; `stage_release_reviews.reviewer_user_id` had a FK, but to `auth.users` instead of `public.users` (the table with the columns the frontend actually embeds), so a second FK was added and the frontend's embed-hint constraint name updated to match; fixing that exposed two more broken layers of the same `/admin/reviews` query — `stage_releases.stage_id` pointed at the **deprecated** `documents_stages` table instead of the authoritative `stages` table, and `stage_releases.tenant_id` had no FK at all — both zero-risk since `stage_releases` is completely empty in prod (confirmed via read-only count before each fix); every FK addition checked for orphaned rows against prod data first (0 found in all cases); direct hand-written migration per Carl's explicit instruction (not routed through Lovable), applied via Supabase MCP with per-step approval after an auto-mode permission classifier blocked the first attempts; all 4 originally-broken pages verified live with 0 console errors post-migration; 3 further findings from the same audit (RLS 403s on 6 executive/strategic views, `get_user_audit`/`list_code_tables` errors, `handle_staff_first_login` 400) investigated read-only but root cause not fully isolated, parked; legacy "Parent"/"Child" terminology on `/admin/tenant-users` investigated and found to be a genuine data-model mismatch (not a simple relabel) — parked for a product decision rather than fixed; 8 separate frontend-only hotfix PRs (#79–86) covering the rest of the audit's fixable findings also shipped and merged the same session, no DB changes in any of those](audit/2026-07-29-fk-relationships-and-tga-status-fix.md) · tag: `audit-2026-07-29-fk-relationships-and-tga-status-fix` · author: Claude (session run by Carl)

- [2026-07-28 — Branding rollback flag — following a UI/design-system research pass (tables, chips/badges, layout) and a hand-applied hotfix fixing table horizontal-scroll clipping bugs, a Badge shape override, and a Purple-vs-Aqua `--primary` contradiction between `index.css` and both `docs/DESIGN_SYSTEM.md` and `button.tsx`'s own comment (plus wiring up the previously-unconfigured Anton/Binate/Calibri brand fonts), Carl asked for a way to revert the color/font change without a redeploy in case Angela doesn't like it; found the existing `app_settings` single-row feature-flag table is staff/admin-only via RLS, which doesn't fit a flag that must be readable by client-portal roles too — rather than loosen `app_settings`' RLS (it also holds SharePoint URLs and AI settings), created a new narrow, single-purpose `public.public_branding_config` table (one boolean, `legacy_branding_enabled`, default `false`) with authenticated-read/super-admin-write RLS, a `useLegacyBrandingFlag` hook mirroring the existing flag-hook pattern (`useEosConfigV2Flag`), and a `BrandingFlagGuard` in `App.tsx` toggling a `legacy-branding` CSS class; verified end-to-end against a local dev server — flipped the flag via Supabase MCP, confirmed computed `--primary` and `h1` font-family both reverted, flipped back, confirmed restored; migration applied directly to production with Carl's explicit approval after an auto-mode permission classifier blocked the first attempt; follow-up live review found the flag's CSS-class override couldn't reach two headers that set Anton via inline `style` (inline styles always beat class selectors regardless of specificity) — fixed by moving those to the `font-display` utility class instead; hand-applied hotfix, PR #72 merged to `main`](audit/2026-07-28-branding-rollback-flag.md) · tag: `audit-2026-07-28-branding-rollback-flag` · author: Carl

- [2026-07-28 — Integrator QC schedule permission — Carl asked to check on three reported issues (Clients nav unread indicator, support-ticket-resolution notification, "Schedule QC" unavailable for Integrator) then to hotfix all three; only the Integrator fix touched production data, covered here. Root cause: `role_permissions` seeded `eos.qc.create = 'none'` for the `Integrator` unicorn_role (migration `20260609052540`), contradicting the older hardcoded `useRBAC.tsx` map which already granted `qc:schedule` to Integrator — `PermissionTooltip` read the legacy source while the button's disabled state read the DB table, so the two systems silently disagreed and Integrators saw a disabled button with no explanatory tooltip; per Carl's confirmed decision, updated `role_permissions` to `'full'` for Integrator (matching Team Leader) via a new migration, logged before/after in `permission_change_log` (apparently its first real row — nothing else in the app currently writes to that table), applied directly to production (project `yxkgdalkbrriasiyyrwk`) via Supabase MCP with Carl's explicit approval; caught and fixed a silent em-dash-to-NULL data-loss quirk on the `reason` column mid-session; flagged but did not action a related gap where `QCScheduler.tsx`'s facilitator picker still excludes Integrator from being selectable as a QC's manager; two sibling hotfixes (nav indicator, ticket-resolution notification) shipped in the same session but touched no schema/data; all three hand-applied hotfixes under the workspace's standing hotfix path, PR #55/#56/#57 not yet merged](audit/2026-07-28-integrator-qc-schedule-permission.md) · tag: `audit-2026-07-28-integrator-qc-schedule-permission` · author: Carl

- [2026-07-27 — Tenant user Position Type — added a structured, extensible Position Type classification (CEO/Owner, Compliance, Administration, Finance, Manager, Trainer/Assessor) for tenant users, replacing reliance on the free-text, person-global `users.job_title` for this purpose; new `dd_position_type` lookup table cloning the existing `dd_relationship_role` convention (id/value/label/sort_order/is_active, authenticated-read-only RLS) plus `tenant_users.position_type` FK column (single value per tenant-user membership, confirmed with Carl), migration applied directly to production (project `yxkgdalkbrriasiyyrwk`) after merge with pre/post-flight verification; unlike the `RELATIONSHIP_ROLE_OPTIONS` precedent, the dropdown fetches lookup values live rather than hardcoding them, so future position types need only a DB insert; shipped across three PRs because tenant users are surfaced by two independently-querying UIs (`/client/users` reads `tenant_users` directly, PR #52; `/admin/tenant-users` reads `public.users` and needed the value joined in separately, PR #53) — Carl asked for inline-editability on the admin page after seeing it render read-only, added along with a tenant multiselect filter reusing the existing `MultiSelect` component (PR #54); all three hand-applied hotfixes under explicit in-session override, all merged](audit/2026-07-27-tenant-user-position-type.md) · tag: `audit-2026-07-27-tenant-user-position-type` · author: Carl

- [2026-07-27 — Message preview word-boundary truncation — Carl reported message bubble text cutting off mid-word on the Team Communications page; traced to `ConversationPanel` combining `overflow-wrap:anywhere` with `word-break:break-all`, which forces breaks between any two characters even when the whole word would fit on the next line (e.g. "bounced" → "bo"/"unced"); fixed by dropping `break-all` (PR #50). Follow-up investigation on the dashboard surfaced a second, related bug: the "Client Messages" panel and Team Communications thread list both read `tenant_conversations.last_message_preview`, populated by the `fn_tm_on_message_insert` trigger via a hard `left(NEW.body, 200)` cut with no word-boundary awareness and no ellipsis (e.g. "...At this s"); confirmed live production function matched git migration exactly (no drift) via `pg_get_functiondef`, then fixed with a new `fn_truncate_preview()` helper that trims to the last whole word and appends an ellipsis; forward-looking only, no backfill of already-truncated rows; both hand-applied hotfixes under explicit in-session override, PR #50 and PR #51 not yet merged](audit/2026-07-27-message-preview-word-boundary-truncation.md) · tag: `audit-2026-07-27-message-preview-word-boundary-truncation` · author: Carl

- [2026-07-27 — Action Item reminder dedupe now due-date-aware — Carl asked what happens to the notify email when a due date changes; traced the actual cron logic and found ordinary edits already worked correctly (days-until-due is recomputed fresh every night from whatever due_date currently sits on the row), but the dedupe log (`client_action_item_reminder_log`) was keyed only on `(action_item_id, offset_days, recipient_user_id)` with no `due_date` — so a due date that bounced around and later crossed the same offset threshold a second time for a genuinely different date would be silently suppressed as "already sent"; fixed by adding `due_date` to the table and its unique constraint (table was empty, no backfill needed), applied directly to production and smoke-tested; PR #49 merged](audit/2026-07-27-action-item-reminder-dedupe-due-date.md) · tag: `audit-2026-07-27-action-item-reminder-dedupe-due-date` · author: Carl

- [2026-07-27 — Action Item notify edit-mode support — follow-up to the same-day notify feature (PR #47): editing an existing action item loaded the notify dialog without its saved recipient lists/reminder offsets and never persisted changes made there — toggling the boxes during an edit was a silent no-op, caught because Carl asked directly whether it would work; fixed by extending the `ActionItem` type with the three `notify_*` fields (data was already there via `select('*')`, just untyped), loading them on edit-open, and persisting on save; only recipients newly added since the edit was opened get notified (diffed against a snapshot) so unrelated edits (e.g. changing priority) don't re-spam existing recipients; also fixed an incidental bug where an already-selected-but-no-longer-reachable reminder offset rendered disabled and unremovable; no DB changes needed — the nightly cron already reads live columns regardless of how they were set; PR #48 merged](audit/2026-07-27-action-item-notify-edit-support.md) · tag: `audit-2026-07-27-action-item-notify-edit-support` · author: Carl

- [2026-07-27 — Action Item notify + due-date reminders — built a new feature replacing the old single "Notify Client (Primary Contact)" checkbox on Create Action Item with two proper recipient lists (internal Vivacity staff, all tenant portal users) plus configurable due-date reminders (15/7/3/1 days before, only offsets still reachable given the entered due date selectable); previously no notify choice was ever persisted — pure ephemeral UI state used once at creation; added `notify_staff_user_ids`/`notify_tenant_user_ids`/`notify_offset_days` to `client_action_items`, a new `client_action_item_reminder_log` dedupe table, and a new nightly cron (`send-action-item-due-reminders`, mirroring the existing `verify_jwt=false` cron-function pattern) that emails every configured recipient once per (item, offset); immediate "created" emails reuse the existing authorized `send-composed-email` rather than a new endpoint; corrected a mid-session assumption that no Mailgun-hosted templates exist in this codebase (`send-invitation-email` actually uses one) before choosing the inline-HTML-in-code approach instead, confirmed with the user as requiring no Mailgun dashboard setup; migration + edge function applied directly to production and smoke-tested; hand-applied hotfix under explicit in-session override, PR #47 not yet merged](audit/2026-07-27-action-item-notify-reminders.md) · tag: `audit-2026-07-27-action-item-notify-reminders` · author: Carl

- [2026-07-27 — CSC assignment silent failure — reassigning a client's CSC via the quick-assign dropdown on the client detail page appeared to succeed (toast + dialog close) but silently reverted; root cause traced to `useTenantCSCAssignment.tsx` only checking the Postgrest transport `error`, never the JSON payload `admin_set_tenant_csc_assignment`/`admin_remove_tenant_csc_assignment` return — both RPCs reject invalid requests (non-SuperAdmin caller, or target user not flagged `is_csc`/`client_success`) with a soft `{success:false, error:...}` rather than a SQL exception, so `onSuccess` fired regardless and the row was never touched; fixed by checking `data.success` and throwing so the existing error toast surfaces the real reason; follow-up investigation confirmed a second, live drift bug — `tenants.assigned_consultant_user_id` (sole source for the Triage Dashboard, Executive Dashboard, and bulk-reassignment-tool capacity figures) was never synced by this RPC, unlike the bulk/auto-assign paths, causing 7/413 tenants to show the wrong CSC/consultant on those dashboards; fixed with the same sync pattern plus a one-time backfill, applied directly to production (verified 0 remaining mismatches) then committed to git; both hand-applied hotfixes under explicit in-session override, PR #46 not yet merged](audit/2026-07-27-csc-assignment-silent-failure.md) · tag: `audit-2026-07-27-csc-assignment-silent-failure` · author: Carl

- [2026-07-24 — EOS meeting overhaul — full EOS meeting-feature overhaul for Vivacity's internal EOS practice (tenant 6372): schema/RPC/RLS migrations M1-M20, three frontend stages (Configuration UI, simplified scheduling, live-meeting control model), `eos_config_v2` flag flip, live testing against production via a seeded isolated test meeting; ~16+ rounds of Cursor Bugbot findings on PR #39 cross-checked against the plan doc before applying, one real gap found (M16, required-seats fallback refined to check actual row count); M19 closed a post-apply security-advisor RLS gap on 3 backup tables; found and fixed two genuine production bugs via live Playwright testing — a broken `eos_meeting_participants → users` PostgREST embed referencing a non-existent FK (silently HTTP 400 on every load since before this overhaul, made load-bearing by Stage 3's tightened facilitator-only control model, PR #41), and a Supabase Realtime registration-layer failure where `postgres_changes` subscriptions never register server-side for the EOS tables despite working Presence on the same channel (confirmed via `realtime.subscription`/`pg_stat_activity` inspection on the live DB; worked around with a broadcast-based sync fallback, PR #42); M20 closed a separate `eos_todos` realtime-publication gap found along the way; the plan doc itself had never been committed to this repo until this session's wrap-up](audit/2026-07-24-eos-meeting-overhaul.md) · tag: `audit-2026-07-24-eos-meeting-overhaul` · author: Carl

- [2026-07-23 — 18 July batch full reconciliation — resolved the remaining 17 un-audited PRs from the 18 Jul security batch that never deployed due to broken CI: #19/#23 applied as pure defense-in-depth (zero real gap); #28/#38 (anon-EXECUTE revokes on 102 + 49 trigger functions) had phantom function names stripped before applying (would have aborted the transaction otherwise), closing the previously-flagged `set_user_organisation` gap as a side effect; #29 (cohort-send RLS tightening to `admin.cohort.send`) confirmed zero real impact (7/7 historical jobs by Super Admin, route already `requireSuperAdmin`-gated) and applied; #31 (restrict `users` UPDATE to admin-only) required a full redesign after disproving its own stated rationale — traced 3 real non-admin-gated flows (CSC toggle, colleague-disable, client job_title/phone edit) that would have broken, replaced with a RESTRICTIVE backstop permitting only those 4 safe fields for non-admin staff via a jsonb-diff helper; a follow-up bug in that fix (`anon` retaining EXECUTE on the new helper function despite an explicit REVOKE FROM PUBLIC — a default-privilege quirk, not a code bug) caught and closed same-session; edge-function-only PRs #22/#26/#27/#30/#32/#33 confirmed zero drift between deployed code and `origin/main`; `tga_scope_items` cross-tenant SELECT finding investigated a third time (table confirmed empty, proposed auto-fix found buggy) and deliberately left parked with a corrected fix documented for later](audit/2026-07-23-18jul-batch-full-reconciliation.md) · tag: `audit-2026-07-23-18jul-batch-full-reconciliation` · author: Carl

- [2026-07-22 — Git-drift reconciliation (12–13 Jul + 14–16 Jul security remediation) — reconciled two classes of git/production divergence: migrations applied live but never committed (33 total: 27 from the 14–16 Jul window backfilled verbatim, plus 6 findings from an earlier 12–13 Jul pass — C2 privilege-escalation block, H1 world-readable emails policy drop, H3 internal-onboarding admin-only scoping, M2 five-table RESTRICTIVE backstops, M3 anon-EXECUTE revoke on 15 functions re-derived from live truth not the unsubstantiated historical "92" figure, L1/L3 stale-table drop + cron.job_run_details lockdown — all blast-radius checked before applying, git sync briefly stalled then resolved); and the opposite direction — an 18 Jul PR batch merged to git but never deployed due to a silently-failing `deploy-supabase.yml`, surfacing `admin.broadcast.send`'s Team Leader/Team Member access as a real-but-already-decided change (left as-is) and `cron.job`'s PUBLIC SELECT grant as a platform-level constraint no project migration can fix (confirmed non-exploitable — `cron` schema isn't exposed via PostgREST at all — left as a documented gap rather than pursuing Supabase support); `tga_scope_items` world-readable policy also surfaced and parked (real gap, zero live callers via authenticated client)](audit/2026-07-22-git-drift-reconciliation.md) · tag: `audit-2026-07-22-git-drift-reconciliation` · author: Carl

- [2026-07-22 — Invitation role-ceiling trigger — closed a live, currently-exploitable privilege-escalation gap on `user_invitations`: RLS let a tenant Admin bypass `invite-user`'s app-level role ceiling via direct REST and self-escalate to a VIVACITY-tier internal role (Team Leader/CSC/BGT/Integrator/Super Admin/etc.); the DB trigger the code already assumed existed (`enforce_invitation_role_ceiling`) had been correctly authored and merged 18 Jul but never deployed due to the same broken CI as the git-drift reconciliation above; deployed under a fresh timestamp with an added `REVOKE EXECUTE` hardening line, verified end-to-end against live DB state and `origin/main`; live smoke tests skipped at user's request in favour of object-level verification](audit/2026-07-22-invitation-role-ceiling-trigger.md) · tag: `audit-2026-07-22-invitation-role-ceiling-trigger` · author: Carl

- [2026-07-22 — TGA gate and blast-radius fixes — follow-up to the invite-validation anon-grant fix; closed the remaining findings from that session's blast-radius sweep of the 12–21 Jul security remediation: TGA sync gate cluster (`tga_swap_scope_from_staging`, `tga_start_staged_sync`, `upsert_excel_template_bindings`, `stall_bulk_document_job`, `persist_tga_scope_items`) fixed with a service-role passthrough pattern (`auth.uid() IS NOT NULL AND NOT is_vivacity_team_safe(...)`), verified end-to-end against the originally-reporting client (98 real scope rows landed); a secondary bug found in the same function (`p_triggered_by := auth.uid()` silently discarding a correctly-verified caller id) fixed alongside it; two of three reported `users.select('*')` hard-fails confirmed and fixed (`TenantDetail.tsx`, `TenantMembers.tsx`), the third (`ManagePackages.tsx`) turned out to be a phantom finding from a stale local repo checkout (file deleted 14 Jul, unrelated); `internal-onboarding` storage bucket upload-policy asymmetry investigated and found unreachable in practice (real caller bypasses RLS via service-role and enforces its own uniform check) but tightened anyway as zero-risk hygiene](audit/2026-07-22-tga-gate-and-blast-radius-fixes.md) · tag: `audit-2026-07-22-tga-gate-and-blast-radius-fixes` · author: Carl

- [2026-07-22 — Restore invite-validation anon grant — investigating an unrelated client TGA-sync complaint surfaced a week-long (12–21 Jul) untracked security-hardening effort on prod; `validate_invitation_token` had lost its `anon` EXECUTE grant in a bulk "revoke anon from SECURITY DEFINER functions not used in RLS" sweep, silently blocking every new-user invitation acceptance since ~14 Jul (weekly acceptance rate 40–75% → ~17–18%, invites showing `delivered` but stuck `pending`); fixed via a single scoped migration (`GRANT EXECUTE ... TO anon`, search_path hardened) — the first of that week's ~46 migrations to actually land in git; verified independently (grant query, git diff vs `origin/main`, live logged-out browser test) rather than trusting the applying tool's own report; TGA sync gate (`tga_swap_scope_from_staging`/`tga_start_staged_sync`), 3 `users` `select('*')` hard-fails, and 3 other gated functions from the same remediation remain broken and unfixed](audit/2026-07-22-restore-invite-validation-anon-grant.md) · tag: `audit-2026-07-22-restore-invite-validation-anon-grant` · author: Carl

- [2026-07-06 — Churn Timestamp & CSC Stint Consistency fix — Client Retention KPI on `/kpi` (CSC role) was structurally stuck at ~100% retention because `tenants.churned_at` was never written (referenced an invalid `lifecycle_status='churned'` value that never existed in `dd_lifecycle_status`); `sync_tenant_lifecycle_status()` extended to stamp/clear `churned_at` on lifecycle transitions and atomically close the tenant's open `tenant_csc_assignments` stint (`ended_at`) so churned tenants don't silently revert to "retained" in later KPI periods; `EXECUTE` revoked from `authenticated` on the now-`SECURITY DEFINER` trigger function (lint 0029); 20 legacy churned tenants (of 343 candidates, narrowed to only those with an open CSC stint) backfilled to floor date `2026-02-17` (not `updated_at`, rejected as unreliable); `chk_tenants_churned_at_consistency` CHECK constraint + `uq_tenant_csc_assignments_one_active_per_tenant` partial unique index added to prevent regression; index predicate required tracing the actual `admin_set_tenant_csc_assignment`/`bulk_reassign_primary_csc` bodies (`ended_at IS NULL`, not `superseded_at IS NULL`) to avoid breaking reactivation; two flawed Lovable migration drafts (wrong timestamp source, then hallucinated column names + widened JOIN) caught and corrected before applying; verified live against `kpi_csc_retention_rows`/`kpi_csc_tasks_rows` for a real CSC portfolio; 5 migrations (M1/M1b/M2/M3/M4); `kpi_csc_communication_rows` fix and reassignment-function hardening deferred](audit/2026-07-06-churn-timestamp-csc-stint-fix.md) · tag: `audit-2026-07-06-churn-timestamp-csc-stint-fix` · author: Carl

- [2026-07-02 — Task completion trigger null-package fix — marking a task complete in Tasks Management failed with a generic "Failed to update task status" toast for any task with `package_id = NULL`; root cause: `log_task_completion()` (AFTER UPDATE trigger) unconditionally inserted into `package_workflow_logs`, whose `package_id` column is NOT NULL, rolling back the whole status-update transaction for package-less tasks; confirmed via Postgres logs and reproduced directly against the reported row; fixed with an `AND NEW.package_id IS NOT NULL` short-circuit, preserving log semantics for package-linked tasks; front-end code confirmed not at fault (both call sites correctly surface Postgres errors, just generically); applied as a direct Supabase migration rather than a Lovable prompt (bug fix in existing trigger, not new schema/RLS/data authorship); verified via `BEGIN...ROLLBACK` dry run before reporting fixed; two unrelated recurring Postgres errors spotted in the same log window, parked for follow-up](audit/2026-07-02-task-completion-trigger-null-package.md) · tag: `audit-2026-07-02-task-completion-trigger-null-package` · author: Carl

### June

- [2026-06-30 — email-linked-viewer-access — RLS broadened on `email_messages` + `email_message_attachments` + `email-attachments` storage (owner-only → team-scoped); `body_html text` column added to `email_messages`; `EmailViewDialog` reads from DB first (skips Graph for non-linker viewers); `addin-email-capture` latent `body_content` ghost-column bug fixed; 3 RLS changes applied directly via SQL editor, 1 migration + 6 code files via Lovable](audit/2026-06-30-email-linked-viewer-access.md) · tag: `audit-2026-06-30-email-linked-viewer-access` · author: Carl

- [2026-06-30 — PDP duration backfill — Staff PDPs client portal page showed 0.0 actual hours / 0% complete for every staff member on every tenant; root cause traced through `training_videos.duration_seconds` (NULL for all 703 videos, never populated on add) → `academy_lessons.estimated_minutes` → `pdp_evidence_items.duration_minutes` → `v_pdp_cycle_summary.actual_pd_hours`; Vimeo API only covered 5/703 videos (separate domain-restricted account for the rest, 404s/403s on every direct/oEmbed approach); workaround loaded the Vimeo Player SDK inside a Playwright session on the whitelisted `unicorn-cms.au` domain to resolve `getDuration()` for all restricted videos; 701/703 backfilled (2 confirmed genuinely deleted from Vimeo), 26 existing `pdp_evidence_items` corrected; new `trg_pdp_evidence_fill_academy_duration` trigger (INSERT-only, preserves manual edits) + matching edge-function/frontend precedence fix prevent recurrence; incidental RLS tightening on `email_attachments` (overly permissive ALL policy) and `user_invitations_accept` (accepted regardless of email match); "View as Client" redirect bug and new-video duration automation parked; temporary `backfill-vimeo-durations` edge function flagged for deletion](audit/2026-06-30-pdp-duration-backfill.md) · tag: `audit-2026-06-30-pdp-duration-backfill` · author: Carl

- [2026-06-23 — KPI Module (Phases 1–7) — 10 `dd_kpi_*` lookup tables, 7 core KPI tables (`kpi_email_log`, `kpi_tasks`, `kpi_tickets`, `kpi_ticket_comms`, `kpi_reviews`, `kpi_review_signoffs`, `kpi_dev_milestones`), 3 SECURITY INVOKER summary views, 2 RPCs (`compute_kpi_overall_status`, `upsert_kpi_review`), `is_kpi_reviewer_safe()` helper, `users.kpi_role` + `tenants.churned_at` columns; Outlook on-demand email sync edge function + `OutlookInboxBrowser` folder prop; 3-way sign-off (subject / reviewer / superadmin); reviewer board at `/admin/kpi-overview`; 11 staff role assignments applied via Supabase MCP; post-verification correction: `is_kpi_reviewer_safe` rewritten to use `users.kpi_role` instead of `user_roles`; zero existing RLS policies, FKs, or features modified](audit/2026-06-23-kpi-module.md) · tag: `audit-2026-06-23-kpi-module` · author: Carl

- [2026-06-22 — Staff Onboarding & Offboarding feature + post-build bug fixes — (1) `user_invitations_unicorn_role_check` constraint widened to include CSC, Integrator, BGT, CET (was missing — caused 500 on all non-Team-Member Vivacity invites); (2) `accept_invitation_v2` patched: `tenant_id = 6372` branch sets `relationship_role = NULL` instead of `'user'`, fixing routing to Academy instead of admin dashboard; `ELSE NULL` added to CASE; `v_tu_role/secondary/access_scope` defaults added to internal fallback block to prevent NOT NULL INSERT failure; (3) `SuggestionRegister.tsx` access guard extended to CSC/Integrator/BGT/CET (Lovable artefact from March 2026); 1 migration `20260622032700`; 2 frontend commits; 2 test users created and deleted](audit/2026-06-22-staff-onboarding-offboarding.md) · tag: `audit-2026-06-22-staff-onboarding-offboarding` · author: Khian / Brian

- [2026-06-18 — Impersonation `acting_user_id` column — `acting_user_id uuid NULL REFERENCES auth.users(id) ON DELETE SET NULL` added to `audit_client_impersonation`; `ClientPreviewContext.startPreview()` reordered so options load before the audit INSERT; `acting_user_id` populated at session start only (mid-session switches not written back by design); 433 legacy rows stay NULL; FK and column confirmed in production via Supabase MCP; `endPreview()` and `setActingUserId()` untouched; 1 migration `20260618021155`; 1 code file](audit/2026-06-18-impersonation-acting-user-id.md) · tag: `audit-2026-06-18-impersonation-acting-user-id` · author: Carl

- [2026-06-18 — Reporting obligation notifications — new reporting-obligation notification system: `compliance_obligations` migrated off raw `audience`/`recurrence` text+CHECK columns onto `dd_obligation_audience`/`dd_obligation_recurrence` lookup tables (no application code read the legacy columns directly, so the drop was clean); in-app-only notifications (`user_notifications`, type `reporting_obligation_due`) at lead times [30,14,7,1] days, tenant/user filtering via `relationship_role`+`access_scope`+test-tenant exclusion+per-user prefs; two-step manual broadcast (Preview then Send, Send disabled until Preview fetched); cron job 17 `generate-notifications-reporting-obligations` at `15 0 * * *` UTC; Super Admin CRUD page under Settings → Configuration; Phase 6 frontend shipped (7 new files); GTO audience deferred (no current tenants); redundant non-partial unique index on `user_notifications.dedupe_key` flagged for optional cleanup; functional preview→broadcast→client-inbox QA not yet run against prod](audit/2026-06-18-reporting-obligation-notifications.md) · tag: `audit-2026-06-18-reporting-obligation-notifications` · author: Carl

- [2026-06-17 — User management roles — (1) `TenantUsersTab.tsx`: `relationship_role` added to `tenant_users` SELECT — fixes admin dropdown showing "User" for all non-primary/secondary users including `academy_user`; (2) client portal invite extended: `user` (Full access) added as third invite option alongside Academy only and Secondary contact; `invite-user` allowed array updated; full chain verified `access_scope='full'`; (3) `ClientUsersPage.tsx`: inline `RoleSwitcher` for `academy_user ↔ user` — per-row pending state, `set_relationship_role` RPC, cache invalidation on success; no migrations](audit/2026-06-17-user-management-roles.md) · tag: `audit-2026-06-17-user-management-roles` · author: Carl

- [2026-06-16 — Per-membership user capacity limits — `packages.user_limit` column seeded (Gold=5, Ruby=10, Sapphire=15, Diamond=NULL, KS/default=5); `get_tenant_user_capacity(bigint)` RPC (MAX-with-NULL-beats-all, counts tenant_users + pending/sent invites, excludes primary/secondary); `invite-user` edge function gated at both write paths with Vivacity staff bypass; `CapacityPill` + `useUserCapacity` hook; `TenantUsersTab`, `ClientUsersPage`, `InviteUserDialog` updated; D/E smoke test pending against tenant 6372; flag: M-AM defaulted to 5 — Vivacity's own tenant is 1 over cap](audit/2026-06-16-user-capacity-limits.md) · tag: `audit-2026-06-16-user-capacity-limits` · author: Carl

- [2026-06-16 — Tasks overhaul (Phases 1–4) — unified `client_action_items` model: `published_action_item_id` FK on CTI, RLS split on CAI + comments (staff full CRUD / portal scoped), column-guard trigger, dashboard double-count fix, `rpc_publish_stage_tasks` SECURITY DEFINER RPC, unified portal Tasks view (My/All tabs, inline status), Publish button in `PackageStagesManager`; 7 migrations; Phase 5 (legacy deprecation) deferred](audit/2026-06-16-tasks-overhaul.md) · tag: `audit-2026-06-16-tasks-overhaul` · author: Carl

- [2026-06-11 — `last_sign_in_at` insert sync — new `trg_sync_last_sign_in_on_insert` (`AFTER INSERT ON public.users`, `SECURITY DEFINER`) syncs `last_sign_in_at` from `auth.users` at row creation; fixes race condition where first-time activation creates `public.users` AFTER auth sign-in sets the timestamp; 26 users backfilled; `handle_user_login` (UPDATE path) unchanged; 1 migration `20260611062316`](audit/2026-06-11-last-sign-in-insert-sync.md) · tag: `audit-2026-06-11-last-sign-in-insert-sync` · author: Carl

- [2026-06-11 — 0-minute time entries — `AddTimeDialog.tsx:401` + `EditTimeDialog.tsx:255` guards changed `<= 0` → `< 0`; `v_client_package_hours_recent` updated to remove `duration_minutes > 0` filter so 0-minute activity records surface in Recent Work; 10 pre-existing 0-minute entries now visible; all hour-aggregation surfaces unaffected; 1 migration `20260611060223`, 2 frontend files](audit/2026-06-11-zero-minute-time-entries.md) · tag: `audit-2026-06-11-zero-minute-time-entries` · author: Carl

- [2026-06-11 — `rpc_get_package_usage` billable fix + `package_instances.hours_used` trigger — (1) `rpc_get_package_usage` rewritten: `v_used_minutes`, trailing-30d, and source breakdowns all filtered to `is_billable=true`; `v_billable_total`/`v_non_billable_total` kept in separate unfiltered SELECT for badge; `search_path=''` hardened; EXECUTE restricted to `authenticated`; (2) `tg_recalc_package_hours_used` trigger on `time_entries` now maintains `package_instances.hours_used` (billable-only, child→parent rollup) on INSERT/UPDATE/DELETE; backfill set all 1,036 rows (70 with usage > 0); no frontend changes; 1 migration `20260611054310`](audit/2026-06-11-package-hours-rpc-trigger-fix.md) · tag: `audit-2026-06-11-package-hours-rpc-trigger-fix` · author: Carl

- [2026-06-11 — Package billable hours filter — `AND te.is_billable = true` added to all 12 package hour aggregation surfaces (1 RPC + 11 views); non-billable goodwill time no longer consumed from clients' hour allocations; `v_client_package_hours_recent` gains `is_billable` column + "Included" badge in `PackageRecentWork.tsx`; `v_dashboard_tenant_portfolio` `security_invoker` absence preserved; numeric sanity confirmed (package 15127: 1.25 billable vs 43.50 total hrs); 1 migration `20260611041127`, 3 frontend files](audit/2026-06-11-package-billable-hours-filter.md) · tag: `audit-2026-06-11-package-billable-hours-filter` · author: Carl

- [2026-06-11 — `v_client_tenant_users` display name fix — `display_name` COALESCE rewritten to derive from `first_name + last_name` only; `full_name` ("Full Legal Name") removed from display path; root cause: trigger only populates `full_name` when null/empty, so admin last_name changes left stale surnames showing in the portal; 0 other views affected; 1 migration `20260611004758`; no app code or RLS changes](audit/2026-06-11-v-client-tenant-users-display-name-fix.md) · tag: `audit-2026-06-11-v-client-tenant-users-display-name-fix` · author: Carl

- [2026-06-10 — tenant-registry-links RLS fix — `tenant_registry_links_tenant_select_own` replaced: old `users.tenant_id` check swapped for `has_tenant_access_safe()`; 2 new INSERT/UPDATE policies added gated by `has_tenant_admin_safe()`; CSCs/tenant members now see their own TGA link status; tenant Admins can upsert; Super Admin / Team Leader unchanged; 1 migration `20260610025642`; no app code changes](audit/2026-06-10-tenant-registry-links-rls-fix.md) · tag: `audit-2026-06-10-tenant-registry-links-rls-fix` · author: Carl

- [2026-06-08 — Invitation & auth flow fixes — 5 bugs resolved: (1) 45 client users backfilled with null `users.tenant_id` (RLS lockout); (2) `set_relationship_role` missing `tenant_id = COALESCE(tenant_id, p_tenant_id)` — recurrence source plugged + silent audit exception replaced with `RAISE WARNING`; (3) scanner-burn in password reset emails — server-side `/activate` URL transform in `send-password-reset` + `send-self-password-reset`; (4) misleading "welcome email could not be sent" toast — `invite_sent` → `email_sent` field rename in `activate-ghost-user` response; (5) `ghost_activation: true` never cleared — best-effort `updateUserById` call added to `set-invite-password` after password set; smoke tested end-to-end on Demo RTO; 1 migration, 4 edge functions](audit/2026-06-08-invitation-auth-flow-fixes.md) · tag: `audit-2026-06-08-invitation-auth-flow-fixes` · author: Khian (Brian)

- [2026-06-05 — Role backfill: primary/secondary contacts — `tenant_users.role` corrected `child→parent` (369 rows) and `tenant_members.role` corrected `General User→Admin` (367 rows) for all primary and secondary contacts platform-wide; root cause: May 2025 initial data import applied `role='child'` universally; 6 users were actively signed in with wrong permissions; prior fixes (2026-05-06, 2026-05-12, 2026-05-26) addressed subsets only; verification: 0 remaining wrong rows; open: `activate-ghost-user` does not apply role mapping at activation time — future ghost activations will re-introduce the problem until edge function is patched](audit/2026-06-05-role-backfill-primary-secondary-contacts.md) · tag: `audit-2026-06-05-role-backfill-primary-secondary-contacts` · author: Carl

- [2026-06-03 — L10 meeting fixes + features — B1: null-tenant risks invisible in IDS (`useMeetingIssues` nested OR query); B2: next segment double-click/skip (useRef sync lock + Loader2 spinner); B3: meeting close blocked (`startFirstSegment` `.select()` verification + `close_meeting_with_validation` force-close bypass when segments started); B4: `set_issue_status` null-tenant crash (relaxed meeting lookup + guarded audit insert with COALESCE); F1: L10 auto-schedule every Monday (series created for Vivacity, 7 meetings pre-seeded Jun 8–Jul 20, trigger chain active); F2: rock owner + quarter editing in live meeting (pencil button + RockFormDialog); 2 migrations, 4 frontend files, 1 direct DB data change](audit/2026-06-03-l10-meeting-fixes.md) · author: Khian (Brian)
- [2026-06-02 — Governance Documents dedup fix — `v_client_governance_documents` rewritten with `ROW_NUMBER()` to deduplicate to one row per `(tenant_id, document_id)`, latest non-cancelled `package_instance` wins; eliminated 5,231 duplicate groups / 6,785 extra rows platform-wide affecting 170+ tenants; root cause: `start_client_package` does not supersede prior `document_instances` on package transition or annual renewal — view fix resolves client-visible symptom; 1 migration, view replacement only, no frontend or schema changes](audit/2026-06-02-governance-documents-dedup-fix.md) · tag: `audit-2026-06-02-governance-documents-dedup-fix` · author: Carl

- [2026-06-03 — Invitation flow complete — test user deleted; Demo RTO + 2 ghost users created; Cohort Sender per-row selection + `launch_cohort_job` extended with `p_include_uuids`; Manage Invites Copy link button; Mailgun webhook integration (`delivery_status`, `delivery_event_at`; HMAC-SHA256 verification; Bounced/Delivery failed/Spam report badges + filters); `activate-ghost-user` now captures `mailgun_message_id`; `resend-invite` 4 bug fixes (wrong body, 24h→7d expiry, wrong VIVACITY_TENANT_ID, delivery_status not cleared) + `skip_email` support; Manage Invites stat cards + filter consistency fixed (`is_in_auth`); expired invites no longer bleed into pending filter; 3 migrations, 3 edge functions, 2 frontend files](audit/2026-06-03-invitation-flow-complete.md) · tag: `audit-2026-06-03-invitation-flow-complete` · author: Khian (Brian)

### May

- [2026-05-31 — Invitation flow & Cohort Sender fixes — ghost user UUID preservation confirmed; Cohort Sender added to nav; Manage Invites 3 bugs fixed (expiry 24h→7d, ghost verified, re-invite button); `activate-ghost-user` returns `action_link` on email block; cohort job results: copy link column, tenant names, null tenant guard; `invite-user` skip_email now stamps `tenant_id`; `v_auth_user_state` COALESCE fallback via `tenant_users` (26→4 invisible ghosts); smoke tested end-to-end; 3 migrations, 3 edge functions, 4 frontend files](audit/2026-05-31-invitation-flow-cohort-sender-fixes.md) · tag: `audit-2026-05-31-invitation-flow-cohort-sender-fixes` · author: Khian (Brian)
- [2026-05-29 — Client Governance Documents page: framework filter + view button + per-document SharePoint button + packages column (active only, grouped in view) + framework short labels + scroll-to-top — new `v_client_governance_documents` view (`security_invoker=true`, `STRING_AGG` active packages); `get-sharepoint-parent-folder` new edge function (3 fix rounds: `getClaims` → `getUser`, wrong tenant gate removed, invalid `/parent` Graph endpoint → two-step `parentReference` lookup); 1 migration, 1 new edge function, 1 frontend file](audit/2026-05-29-client-governance-documents-features.md) · tag: `audit-2026-05-29-client-governance-documents-features` · author: Khian (Brian)
- [2026-05-28 — Client Files tab: shared folder link + inline browser — `shared_folder_url` column added to `tenant_sharepoint_settings`; admin `SharedFolderSection` saves URL on select/clears on clear; root folder card removed from client Files tab; shared folder card with "Open Shared Folder" button + embedded inline SharePoint folder browser (breadcrumb, back nav, download); `browse-sharepoint-folder` boundary hardened (`effectiveRootId` hoisted, list + download both scoped to shared folder when `use_shared_folder: true`); `useSharePointBrowser` download fix (passes `use_shared_folder` flag); 1 migration, 4 files; 14 tenants with null `shared_folder_url` pending AJ/Ezel re-selection](audit/2026-05-28-client-files-shared-folder-browser.md) · tag: `audit-2026-05-28-client-files-shared-folder-browser` · author: Khian (Brian)
- [2026-05-26 — Academy UI fixes — PDP duplicate heading removed (`AcademyPageWrapper` title props stripped, `PdpHeaderBand` is now sole heading); "Open my PDP cycle" button renamed to "Open my PDP"; lesson viewer top bar no longer shows raw lesson ID (guard added to `titleFromPath`); lesson sidebar readability improved (course name suffix stripped, module/lesson code rendered as monospace badge); UI-only, no migrations](audit/2026-05-26-academy-ui-fixes.md) · tag: `audit-2026-05-26-academy-ui-fixes` · author: Khian (Brian)
- [2026-05-26 — BUG-041 ghost user role fix + account activation — `set_relationship_role` two-stage fix (dropped `unicorn_role` enum type variable + FK exception handler for ghost users on `audit_eos_events`); new `activate-ghost-user` edge function + `is_ghost_user` DB function; TenantUsersTab "Activate account" button for Vivacity staff; 470 ghost users confirmed (394 primary contacts); architectural debt flagged (`audit_eos_events.user_id` FK should reference `public.users`); BUG-042 logged (`is_eos_admin` dropped `eos_role` enum); 3 migrations, 1 edge function, 1 frontend commit](audit/2026-05-26-bug-041-ghost-user-role-fix-and-activation.md) · tag: `audit-2026-05-26-bug-041-ghost-user-role-fix-and-activation` · author: Khian (Brian)
- [2026-05-25 — L10 meeting fixes — agenda segment order corrected (Customer/Employee Headlines before IDS) across 388 templates; "Conclude" renamed to "Conclude / One Phrase Close"; todo assignee name now displayed in live meeting view; meeting close failure fixed for non-super-admin users (broken permission check in `generate_meeting_summary` + missing exception handler in `close_meeting_with_validation`); 2 migrations, 1 codebase commit](audit/2026-05-25-l10-meeting-fixes.md) · tag: `audit-2026-05-25-l10-meeting-fixes` · author: Khian (Brian)
- [2026-05-25 — BUG-008, CSC-017 — Preview session cleanup + portal preview simplification — backfill 78 orphaned `audit_client_impersonation` rows (`ended_at = started_at + 4h`); cron job 12 `close-stale-preview-sessions` (`0 */4 * * *`) prevents future accumulation; `ViewAsClientButton.tsx` portal path now starts preview immediately without reason dialog (Academy dialog retained for acting-user picker); 1 migration, 1 codebase commit, no schema or RLS changes](audit/2026-05-25-bug-008-csc-017-preview-session-cleanup.md) · tag: `audit-2026-05-25-bug-008-csc-017-preview-session-cleanup` · author: Khian (Brian)
- [2026-05-21 — CSC-002, CSC-003, BUG-025, BUG-032, CSC-018 — Outlook calendar sync (fan-out cron Edge Function `sync-outlook-calendar-cron` + pg_cron job every 30 min; all 8 consultants syncing); Outlook email cache scoping fix (`useLinkedEmails` scoped invalidation); impersonation picker inactive-user error message + picker exclusion (BUG-025); email function `created_at DESC` harmonisation (BUG-032); calendar overlapping events side-by-side layout with brand colour rotation (CSC-018)](audit/2026-05-21-csc-002-003-bug-025-032-outlook-calendar-fixes.md) · tag: `audit-2026-05-21-csc-002-003-bug-025-032-outlook-calendar-fixes` · author: Khian (Brian)
- [2026-05-21 — BUG-005, BUG-022, BUG-023 — RPC permission hardening + portal access fix — REVOKE PUBLIC/anon EXECUTE from `complete_enrollment_as_impersonator` and `enrol_as_impersonator`; `canAccessClientPortal` widened to include `relationship_role = 'user'` (10 users unblocked); BUG-023 confirmed pre-existing fix; 3 users promoted to `secondary_contact` (Jenelle Watson / Watto, Dayan Kasturiratna / Australian College, Emily Myatt / National Skills Training); Christopher Wilson confirmed `user`; 2 migrations + 1 codebase commit](audit/2026-05-21-bug-005-022-023-rpc-permissions-portal-access.md) · tag: `audit-2026-05-21-bug-005-022-023-rpc-permissions-portal-access` · author: Khian (Brian)
- [2026-05-21 — Phase 5Z (legacy enum archive + Post-Project Data Integrity Report) — 8 Phase 5 legacy enum types archived to `archive` schema (`eos_todo_status`, `eos_function_type`, `eos_seat_role_type`, `eos_participant_role`, `eos_meeting_type`, `meeting_status`, `meeting_role`, `eos_meeting_role`); retention COMMENTs added to all 8; patch migration added missing COMMENTs on `archive.tenant_role` and `archive.vivacity_role` (Phase 1 omission); TypeScript types regenerated (79 lines removed — 8 legacy enum unions removed from `Database["public"]["Enums"]`); all 5 post-deploy checks passed; project end state: 47→70 `dd_` tables, 16 archived enums; post-project data integrity scan across all phases — no data loss, no FK violations, no nulls introduced, no orphaned values; all 7 integrity layers passed](audit/2026-05-21-enum-phase-5z.md) · tag: `audit-2026-05-21-enum-phase-5z` · author: Khian (Brian)
- [2026-05-21 — Phase 5G (`meeting_role` → `dd_meeting_role`) — `dd_meeting_role` created and seeded (6 rows: owner, attendee, guest, visionary, integrator, core_team); `eos_meeting_attendees.role_in_meeting` → text NOT NULL + FK (183 rows preserved); `add_meeting_attendee` DROPped by exact signature (enum in param type) and recreated with text param + GRANT; `seed_meeting_attendees` and `seed_meeting_attendees_from_roles` recreated via CREATE OR REPLACE (cast removal); `trg_seed_meeting_attendees` unchanged; 6 functions confirmed not affected (all read `eos_meeting_participants.role`, different column); hand-written `MeetingRole` union in `useMeetingAttendance.tsx` unchanged; all 9 post-deploy checks passed; legacy enum retained in public](audit/2026-05-21-enum-phase-5g.md) · tag: `audit-2026-05-21-enum-phase-5g` · author: Khian (Brian)
- [2026-05-20 — `public.users.last_sign_in_at` sync from `auth.users` — "Last active" column on `/client/users` was rendering "Never" for every user because `v_client_tenant_users` reads `public.users.last_sign_in_at` which was seeded once and never re-synced (28 of 35 joined auth↔public rows out of sync, ≈80% drift); root cause is that `auth.users` RLS restricts `authenticated` to their own row, so a `security_invoker=true` view can't join across the auth boundary; fix extends the existing `handle_user_login` trigger function (already AFTER UPDATE on `auth.users` for `last_sign_in_at`, already writing to `public.user_activity`) to ALSO mirror `NEW.last_sign_in_at` into `public.users.last_sign_in_at` — single guard, no new trigger DDL, no new lock on `auth.users`; function hardening upgraded (`search_path = ''` from legacy `'public'`, full schema-qualification, `REVOKE ALL ... FROM PUBLIC`); idempotent backfill UPDATE in same migration; KB pattern note added documenting "mirror via trigger, don't view-join" for future auth column needs; other auth columns + `ManageInvites` admin-SDK redundancy parked](audit/2026-05-20-last-sign-in-at-sync.md) · tag: `audit-2026-05-20-last-sign-in-at-sync` · author: Carl
- [2026-05-19 — Phase 5E (`eos_meeting_type` → `dd_eos_meeting_type`) — `dd_eos_meeting_type` created and seeded (6 rows, byte-identical); 3 columns converted (2,029-row `eos_agenda_templates` + 7-row `eos_meeting_series` + 17-row `eos_meetings`); 3 FKs added; 4 views DROP + RECREATE (pass-through views require this even with no enum casts — pg_depend on column type); 7 SQL fns updated (5 with actual casts removed, 2 byte-identical recreate); `create_meeting_series` required DROP by exact signature (enum was in param type, not body cast); `idx_quarterly_meeting_unique` recreated without `::eos_meeting_type` (`::meeting_status` preserved — Phase 5F scope); `MeetingType` TS union in `src/types/eos.ts:4` unchanged; all 10 post-deploy checks passed; nothing broke; legacy enum retained in `public`](audit/2026-05-19-enum-phase-5e.md) · tag: `audit-2026-05-19-enum-phase-5e` · author: Khian (Brian)
- [2026-05-19 — Phase 5D (`eos_participant_role` → `dd_eos_participant_role`) — `dd_eos_participant_role` created and seeded (3 rows: Leader, Member, Observer); `eos_meeting_participants.role` → text NULL + FK + default `'Member'`; 4 SQL fns updated (10 casts removed: `create_meeting_basic` int overload, both `create_meeting_from_template` overloads, `sync_l10_meeting_participants`); `enforce_level10_participants` trigger untouched (plain string comparisons); 0 views required DROP; all 7 post-deploy checks passed; TS union literals at `src/types/eos.ts:378` + `useFacilitatorChange.tsx:8` left as-is; legacy enum retained](audit/2026-05-19-enum-phase-5d.md) · tag: `audit-2026-05-19-enum-phase-5d` · author: Khian (Brian)
- [2026-05-19 — Notification system survey (read-only) — end-to-end map of the notification subsystem after the morning's academy-leak fix; 7 tables documented (`user_notifications` in-app + `notification_outbox` Teams/email transactional-outbox pattern + 5 supporting), 6 edge function writers + 6 DB triggers + 4 surviving direct-insert sites + 2 unscheduled audit PL/pgSQL functions catalogued; email provider identified as **Mailgun** (8 edge functions; `MAILGUN_API_KEY`/`_DOMAIN`/`_REGION`/`_FROM_EMAIL` env vars; the function named `resend-invite` uses Mailgun, "resend"="send again"); all 12 production notification types' writers identified including the previously-mystery `support_ticket`, `capacity_alert`, `follower`, `utilisation_warning`; sanity check on cross-tenant leak hypothesis ran against full `user_notifications` table and **disproved** the suspected `useNotifications` tenant-filter gap — 11 "suspicious" rows split into 10 `meeting_upcoming` false positives (meeting participants legitimately don't have `tenant_users` rows for the meeting's tenant) + 1 orphan from a hard-deleted user; **dead** `notification_audit_log` (defined, zero writers) and **legacy** `notification_schedule` (only written by unscheduled audit functions) surfaced; quiet hours columns on `notification_rules` exist + respected by delivery cron but no UI sets them; 3 `@vivacity.com.au` staff have `unicorn_role='User'` instead of Team-level (notification-correct but can't pass the morning's new RLS for cross-user inserts); priority list captured — P0 is `audit_send_evidence_reminders` + `audit_send_24hr_confirmation` (broken filter + no pg_cron invoker, blocked on workflow-ownership conversation); P1 dedup keys on `follower`/`support_ticket`/`note_shared` + quiet hours wire-or-drop decision; no DB or code changes shipped this session](audit/2026-05-19-notification-system-survey.md) · tag: `audit-2026-05-19-notification-system-survey` · author: Carl
- [2026-05-19 — Phase 5C (`eos_issue_status` → `dd_eos_issue_status`) — `dd_eos_issue_status` created and seeded (8 rows, byte-identical); 3 columns converted (`eos_issues.status`, `eos_issue_status_transitions.from_status`/`to_status`); composite PK dropped + recreated as text (19 transition rows preserved); `set_issue_status` 5 casts removed; `eos_issue_status_options` view replaced (unnest(enum_range) → SELECT from dd_); 13 dependent views DROP + RECREATE in dependency order (3 migration attempts required — PostgreSQL surfaced additional view dependencies each time); all 20 post-deploy checks passed; legacy enum archived to `archive` schema in same session (zero pg_depend entries confirmed before archiving)](audit/2026-05-19-enum-phase-5c.md) · tag: `audit-2026-05-19-enum-phase-5c` · author: Khian (Brian)
- [2026-05-19 — BUG-033 + BUG-040 EOS dead-code fixes — test fixture `function_type: "integrator"` → `"operations"` (invalid `eos_function_type` value, Phase 5B FK pre-emption); `v_client_eos_summary.completed_todos` dead condition `'Done'` → `'Complete'` (never a valid `eos_todo_status`; column had returned 0 for all tenants since creation); migration `20260519030910`; commit `ec1e426d`; no blast radius on either fix](audit/2026-05-19-bug-033-040-eos-dead-code.md) · tag: `audit-2026-05-19-bug-033-040-eos-dead-code` · author: Khian (Brian)
- [2026-05-19 — Academy notification leak fix — Academy-only user (Khian, tenant 7517) was seeing a `suggestion_submitted` notification in the Academy bell; four-layer fix: (1) `AcademyTopBar.tsx` hides `<NotificationDropdown />` when `isAcademyOnly`; (2) `notify-suggestion-submitted` edge function added caller-level + per-tenant `access_scope='academy_only'` checks → 403; (0.5) two new service-role edge functions `notify-merge-fields-updated` and `notify-action-shared` replace direct client-caller `user_notifications.insert(...)` in `useMissingMergeFields` and `ClientActionItemsTab`, preserving row shape (added `dedupe_key`); (3) migration tightened `user_notifications_authenticated_insert` RLS — `WITH CHECK (user_id = auth.uid() OR is_super_admin_safe OR is_vivacity_team_safe)`, `TO authenticated`; (4) data migration purged 1 stale row (only match across DB); audit-time discovery that `clients_legacy.manager` is dead (0/407 tenants) — `tenant_csc_assignments` is canonical (80/407 covered); historical access_scope for Khian on 2026-05-06 unrecoverable, `ClientRouteGuard` blocks the path today; `generate-notifications` edge function not reviewed + 4 staff-caller direct-insert sites left in place parked](audit/2026-05-19-academy-notification-leak-fix.md) · tag: `audit-2026-05-19-academy-notification-leak-fix` · author: Carl
- [2026-05-19 — Phase 5B (`eos_function_type` + `eos_seat_role_type` → `dd_eos_function_type` + `dd_eos_seat_role_type`) — Decision #6 scoped exception: two enums grouped (same accountability chart feature, identical 0-function coupling profile); `dd_eos_function_type` seeded 6 rows, `dd_eos_seat_role_type` seeded 4 rows; `accountability_functions.function_type` + `accountability_seats.eos_role_type` → text with FK; `seat_linked_data` view DROP + RECREATE (inventory note disproved — pg_depend normal dependency on enum type requires DROP before ALTER COLUMN TYPE); all 7 post-deploy checks passed; BUG-033 surfaced (`eos-test-data.ts:263` uses `function_type: "integrator"` — invalid value, will cause FK violation); legacy enums retained with COMMENT retention notices; TS widening handled with explicit cast in SeatDetailPanel.tsx](audit/2026-05-19-enum-phase-5b.md) · tag: `audit-2026-05-19-enum-phase-5b` · author: Khian (Brian)
- [2026-05-19 — Phase 5A (`eos_todo_status` → `dd_eos_todo_status`) — `dd_eos_todo_status` created and seeded (3 rows: Open, Complete, Cancelled); `eos_todos.status` → text NULL + FK; `v_client_eos_summary` DROP + RECREATE (pg_depend on enum type); dead `WHERE status = 'Done'` condition in view preserved byte-identical (BUG-040 tracked); all 6 post-deploy checks passed; legacy enum retained with COMMENT retention notice](audit/2026-05-19-enum-phase-5a.md) · tag: `audit-2026-05-19-enum-phase-5a` · author: Khian (Brian)
- [2026-05-19 — Phase 4D completion (`unicorn_role` sub-phases 4D-1, 4D-4, 4D-5, 4D-6) — completes the unicorn_role enum-to-dd migration; 4D-1: `dd_unicorn_roles` seed corrected (snake_case → byte-identical enum labels, 6 rows); 4D-4: `::unicorn_role` casts removed from `handle_new_user`, `admin_set_role_type`, `accept_invitation_v2`; 4D-5: `useAuth.tsx` `UserProfile.unicorn_role` type widened to include `Academy User` + TS regeneration; 4D-6: `public.unicorn_role` archived to `archive` schema via ALTER TYPE SET SCHEMA; all post-deploy checks passed; Phase 4 cleanup migration (remaining legacy enums) blocked on Carl decision re `archive.backup_users`; see `audit/2026-05-18-enum-phase-4d-2-3.md` for 4D-2+3](audit/2026-05-19-enum-phase-4d-completion.md) · tag: `audit-2026-05-19-enum-phase-4d-completion` · author: Khian (Brian)
- [2026-05-15 — Client portal "Request document" feature disabled — `tenant_document_requests_category_check` violation traced to `"Documents"` value in frontend `CATEGORIES` array + prefill default (DB CHECK allows only Compliance/Evidence/Admin/Other); investigation surfaced feature is half-shipped (no CSC-side inbox/fulfilment/status UI; `emit_notification` writes to `notification_outbox` with no consumer draining into `user_notifications`); 6 entry points disabled with shadcn `disabled` + tooltip (ClientHomePage ×2, ClientFooter, ClientLayout context noop'd as defence-in-depth, ClientTgaDetailsPage, ClientDocumentsPage, ClientDocumentRequests "New request"); list + cancel actions left intact; latent ClientFooter.tsx:58 "Message your CSC" label/wiring mismatch parked; UI-only, no DB touches](audit/2026-05-15-document-request-disable.md) · tag: `audit-2026-05-15-document-request-disable` · author: Carl
- [2026-05-14 — `InviteUserDialog` Unicorn-1 mapping update silently fails (discovery) — confirmed latent bug: frontend's `.schema('unicorn1').from('users').update(...)` at L185-189 silently fails in production (no error check + `authenticated` has zero grants on `unicorn1`); team has been working around via two Feb 2026 batch backfill migrations; user-facing impact = duplicate-import risk via the dialog's "unmapped only" search filter; fix parked for focused session (Option A: SECURITY DEFINER RPC, or Option B: fold into invite-user edge function)](audit/2026-05-14-invite-dialog-unicorn1-update-silent-failure.md) · tag: `audit-2026-05-14-invite-dialog-unicorn1-update-silent-failure` · author: Carl
- [2026-05-18 — Evidence-request workflow restoration on client portal — RLS relaxation on `public.client_audits` (additive policy `client_audits_tenant_read_active` for `status IN ('draft','in_progress','review','complete')`) so real client logins see "Audit Readiness" tile and "Upcoming Compliance Audit" section (previously only working in staff View-as-Client preview); paired with six frontend↔DB value-mismatch fixes (`sort_order`→`display_order`, `reviewed_by_user_id`→`reviewed_by`, `revision_requested`→`resubmit_requested`, `evidence_requests.status` `sent`→`open`, `portal_documents.direction` `inbound`→`client_to_vivacity`, `source` `client_upload`→`evidence_response`) across 7 client/staff files; `AuditProgressCard` gated on `status='complete'` to hide partial scores mid-audit; KB convention note added re: broad-RLS + frontend column whitelist discipline; View-as-Client class-of-bug + `audit_send_evidence_reminders` cron + System 1 (general non-audit evidence requests) parked](audit/2026-05-18-evidence-request-workflow-restoration.md) · tag: `audit-2026-05-18-evidence-request-workflow-restoration` · author: Carl
- [2026-05-18 — Phase 4D-2+4D-3 (`unicorn_role` column type change + 87 RLS policy rewrites) — heaviest coupling migration in the enum stream; 4D-2 and 4D-3 merged into one atomic migration after PostgreSQL blocked ALTER COLUMN TYPE due to 87 dependent RLS policies; 3 retry attempts required (trigger column-level specificity, then full RLS dependency chain, then duplicate policy on loop close); `users.unicorn_role` → text NOT NULL + FK to `dd_unicorn_roles(value)`; `user_protected_fields_unchanged_safe` param 2 text; 86 policies rewritten via capture loop + `users_update_own` manually; 3 triggers and `v_dashboard_labour_efficiency` view recreated; legacy enum retained; all 13 post-deploy checks passed](audit/2026-05-18-enum-phase-4d-2-3.md) · tag: `audit-2026-05-18-enum-phase-4d-2-3` · author: Khian (Brian)
- [2026-05-18 — Phase 4C (`tenant_user_role` → `dd_relationship_role`) + Bug A (L10 segment reorder: IDS before To-Do List) + Bug B (help centre chatbot reply not rendering + raw markdown) — Phase 4C complete: `dd_relationship_role` created and seeded (4 rows), both `tenant_users.relationship_role` and `user_invitations.relationship_role` converted to `text` with FK, 2 RLS policies rewritten, 2 functions updated, unique constraints recreated, view rebuilt, legacy enum retained; Bug A: data-only migration swapped IDS and To-Do List positions across all L10 `eos_agenda_templates`, `eos_agenda_template_versions`, and `eos_meeting_segments` — verification returned 0 wrong rows; Bug B: `ChatTab.tsx` fixed to fetch reply from DB after edge function call (resolves reply-not-showing) and render assistant messages via `ReactMarkdown` (resolves raw markdown display)](audit/2026-05-18-phase-4c-bugs-todo-reorder.md) · tag: `audit-2026-05-18-phase-4c-bugs-todo-reorder` · author: Khian (Brian)
- [2026-05-15 — enum-to-dd Phase 3B (`notification_status` → `dd_notification_status`) + notification cron fixes — Phase 3B migration complete (4 rows seeded, `notification_outbox.status` text-typed with FK, both partial indexes recreated without enum cast, all 9 post-deploy checks passed); BUG-036 resolved (242 stale queued rows skipped + new cron job 10 `process-notification-outbox` running every 5 mins via `private.cron_function_jwt()`); BUG-037 resolved (three audit-email SQL functions fixed — `body := v_payload::text` → `body := v_payload`, `current_setting('app.supabase_service_key', true)` → `private.cron_function_jwt()`); Dijan opening-meeting confirmation email to `rebecca.bond@sunrise.org.au` was the trigger that surfaced BUG-037 — flagged to Angela for manual follow-up](audit/2026-05-15-enum-phase-3b-and-cron-fixes.md) · tag: `audit-2026-05-15-enum-phase-3b-and-cron-fixes` · author: Khian (Brian)
- [2026-05-14 — Client portal CTA routing and inbox deep-link compatibility — sidebar/footer `Message CSC` now open Help Center CSC; Documents page `Request a document` opens the document request modal; package dashboard CSC CTA aligned to side panel; footer + attention notifications link directly to Inbox notifications; Inbox now accepts legacy `conversation=` deep links as fallback to current `thread=`; frontend-only Lovable changes, no DB touches](audit/2026-05-14-client-portal-cta-routing.md) · tag: `audit-2026-05-14-client-portal-cta-routing` · author: Khian (Brian)
- [2026-05-14 — Consult tables Tier 3 archive (`consult_entries` + `consult_logs_unmapped_quarantine`) — closes the truly-orphan half of the 8 May P1 consult tables consolidation; 2 ALTER SET SCHEMA archive + COMMENTs; policies/triggers/outgoing-FKs all travelled; full intent reconstruction in the audit (consult_entries was the granular timer log; quarantine was the Feb 2026 backfill failsafe that never fired); Tier 2 (consults + consult_time_entries, referenced by merge_tenants v_tables array) deferred to a session that updates the function in lockstep](audit/2026-05-14-consult-tier3-archive.md) · tag: `audit-2026-05-14-consult-tier3-archive` · author: Carl
- [2026-05-14 — Admin Edge Function `verify_jwt` closure (8 May P2 reconciliation) — read-only reconciliation; of the 10 admin functions flagged 8 May with `verify_jwt = false`, 4 deleted, 4 hardened to default, 2 (`bulk-user-action` + `tenant-lifecycle`) handle auth themselves correctly; zero remaining exposure on named list; closure organic via unrelated cleanup over past week; broader 96-function `verify_jwt = false` audit parked as separate workstream](audit/2026-05-14-admin-edge-fn-verify-jwt-closure.md) · tag: `audit-2026-05-14-admin-edge-fn-verify-jwt-closure` · author: Carl
- [2026-05-14 — `_tenant_users_contact_backfill_20260512` RLS enable + deny-all — closes Supabase advisor "RLS disabled in public" lint on the 42-row pre-backfill snapshot from the 12 May contact-role-canonicalisation; RLS enabled + 2 deny-all policies + COMMENT documenting the role AND the `DROP AFTER 2026-06-12` lifecycle; third reuse of the deny-all+COMMENT pattern this session; pattern worth codifying in pinned/conventions.md](audit/2026-05-14-tenant-users-contact-backfill-rls.md) · tag: `audit-2026-05-14-tenant-users-contact-backfill-rls` · author: Carl
- [2026-05-14 — `unicorn1."U1_XeroURL"` RLS deny-all + comment (minimum-touch) — closes the second 8 May P2 RLS-on-no-policy lint; in-place policies on a 122-row frozen legacy backfill source (consumed by migration 20260228063003 Feb 2026, never read since); archive move proposed and rejected mid-flight after pre-flight surfaced that `authenticated` has USAGE on archive (would have downgraded defense-in-depth); momentum-reflex lesson captured](audit/2026-05-14-u1-xerourl-rls.md) · tag: `audit-2026-05-14-u1-xerourl-rls` · author: Carl
- [2026-05-14 — `tenant_rto_scope_staging` RLS deny-all + comment — closes 8 May audit P2 (RLS-on-no-policy lint) for the tga-rto-sync staging buffer; explicit `false` policies for `authenticated`+`anon` + `COMMENT ON TABLE` documenting the service-role-only design; edge function unaffected (service_role BYPASSRLS); internal precedent cited from migration 20260406002617](audit/2026-05-14-tenant-rto-scope-staging-rls.md) · tag: `audit-2026-05-14-tenant-rto-scope-staging-rls` · author: Carl
- [2026-05-14 — Archive legacy `audit_log` + `log_user_change()` — closes 8 May audit P0-c residual; table + orphan-broken trigger function moved to `archive` schema via 2 `ALTER … SET SCHEMA` statements; 0 data rows; 3 RLS policies + 2 outgoing FKs + owned sequence travelled with the table; consistent with enum-to-dd archive-not-drop precedent; reversible](audit/2026-05-14-audit-log-archive.md) · tag: `audit-2026-05-14-audit-log-archive` · author: Carl
- [2026-05-14 — `drop_rock_to_issue` strip `client_id` (Scope A hotfix) — closes the runtime bug introduced during the eos_rocks rename: DB function + frontend (`RockProgressControl.tsx:36`) both wrote `integer → uuid` into `eos_issues.client_id`; bug was dormant on 98/100 rocks because frontend short-circuited on null `client_tenant_id`; strip chosen over uuid-bridge to avoid writing semantically meaningless data; 6 dead-by-omission EOS tables + 5 untouched RPCs surfaced for Scope B product decision](audit/2026-05-14-eos-drop-rock-to-issue-strip-client-id.md) · tag: `audit-2026-05-14-eos-drop-rock-to-issue-strip-client-id` · author: Carl
- [2026-05-14 — `eos_rocks.client_tenant_id` RPC rebind (Path C) — minimal patch closing the regression from the eos_rocks rename; `cascade_items` rock branch + `get_client_eos_overview` rocks count rebound via `tenants.id_uuid` bridge; legacy UUID model on `eos_issues`/`eos_todos`/`eos_meetings`/`users`/`eos_item_clients` retained; client-viewer EOS feature confirmed dead-but-routed (0/502 users.client_id populated); Path A/B (resurrect vs tear out) parked as product decision](audit/2026-05-14-eos-rocks-client-tenant-id-rpc-rebind.md) · tag: `audit-2026-05-14-eos-rocks-client-tenant-id-rpc-rebind` · author: Carl
- [2026-05-14 — `eos_rocks.client_id` → `client_tenant_id` rename — column + FK + index renamed, 2 RPCs rebound, 7 frontend files updated (incl. 4 audit-miss fixes surfaced by build + origin/main grep), 5 doc sites in `EOS_LEVEL10_SPECIFICATION.md`; closes the lexical trap that mis-led two prior audits since 13 Feb 2026; `cascade_items` / `get_client_eos_overview` / `eos_issues.client_id` parked as "EOS RPCs unmigrated from clients_legacy" follow-up](audit/2026-05-14-eos-rocks-client-tenant-id-rename.md) · tag: `audit-2026-05-14-eos-rocks-client-tenant-id-rename` · author: Carl
- [2026-05-14 — Docs refresh (architecture doc drift) — 8 May audit §3 closure; six plan-mode Lovable prompts; 25 file changes across `sql-setup/`, `CONTRIBUTING.md`, `docs/EOS_LEVEL10_SPECIFICATION.md`, EOS satellite docs, README, SUPABASE_SETUP; `sql-setup/02-tenant-functions.sql` renamed to `.HISTORICAL.sql`; no DB changes; memory confirmed canonical; EOS §3 DDL + EOS RLS dedup follow-up + README L65 residual parked](audit/2026-05-14-docs-refresh.md) · tag: `audit-2026-05-14-docs-refresh` · author: Carl
- [2026-05-14 — Deployment checklist closure — all 12 May open items reconciled; avatars bucket reduced from 12 to 4 policies; P1-f/P1-g/NEW-003 deferred; checklist fully closed](audit/2026-05-14-deployment-checklist-closure.md) · tag: `audit-2026-05-14-deployment-checklist-closure` · author: Carl
- [2026-05-14 — P1-c closure + `client_audit_log` FK + `tenant_users` user_left trigger — P1-c confirmed already resolved (0 bare auth.uid() in consult_* live); FK added NOT VALID with ON DELETE SET NULL (106 orphaned rows from deleted tenants preserved); user_left DELETE trigger verified smoke test count=1](audit/2026-05-14-p1c-consult-rls-closed-and-followons.md) · tag: `audit-2026-05-14-p1c-consult-rls-closed-and-followons` · author: Carl
- [2026-05-14 — Phase 3 trigger-based logging — 5 SECURITY DEFINER trigger functions on `tenant_users`, `users`, `client_audit_responses`, `client_audit_findings`, `tenant_engagement_settings`; all verified; Migration 4 `subject_tenant_id` bug caught and fixed in-session](audit/2026-05-14-phase3-trigger-logging.md) · tag: `audit-2026-05-14-phase3-trigger-logging` · author: Carl
- [2026-05-14 — enum-to-dd Phase 1C-C (`vivacity_role` archive) — legacy ghost enum (never attached to any column; superseded by `unicorn_role` Oct 2025) archived to `archive` schema via single `ALTER TYPE` statement; no `dd_` table created; Phase 1 fully closed](audit/2026-05-14-enum-to-dd-phase-1cc.md) · tag: `audit-2026-05-14-enum-to-dd-phase-1cc` · author: Khian (Brian)
- [2026-05-14 — enum-to-dd Phase 1 progress — Phase 1A (`feature_flag`), 1B (`meeting_type`, `rock_type`, `stage_state`, `task_status`), 1A-C (`invite_status` skipped), 1B-C (`tenant_role` legacy cleanup — enum archived, ghost column dropped, 7 RLS bugs fixed, `dd_tenant_role` created); `tenant_members.role` audited; BUG-033/034/035 logged; enumtodd skill updated](audit/2026-05-14-enum-to-dd-phase-1.md) · tag: `audit-2026-05-14-enum-to-dd-phase-1` · author: Khian (Brian)
- [2026-05-14 — `client_audit_log` RLS Team Member fix — manual `unicorn_role IN ('Super Admin','Team Leader')` replaced with `is_vivacity_team_safe()` on both INSERT and SELECT policies; `Team Member` access restored; 31 frontend call sites unblocked](audit/2026-05-14-client-audit-log-rls-team-member.md) · tag: `audit-2026-05-14-client-audit-log-rls-team-member` · author: Carl
- [2026-05-14 — `addin_audit_log` M365 add-in audit table — pure create; 3 RLS policies, 4 indexes, FK to users + tenants; federated as 21st branch of `v_workspace_audit_log` gated on `tenant_id IS NOT NULL`; `addinAudit.ts` + 2 edge functions unblocked; `client_audit_log` RLS gap (Plan B) deferred](audit/2026-05-14-addin-audit-log.md) · tag: `audit-2026-05-14-addin-audit-log` · author: Carl
- [2026-05-14 — `v_workspace_audit_log` federated audit view — 20-table UNION ALL across all tenanted event log tables; 16 migrations (1 column, 13 indexes, 1 view, 1 grant change); `security_invoker=true`; `service_role` only; `package_builder_audit_log` excluded (no tenant context); `audit_events` and `audit_dashboard_events` excluded (instrumentation/telemetry); 12 active domains at deploy, 8 zero-row domains included](audit/2026-05-14-v-workspace-audit-log.md) · tag: `audit-2026-05-14-v-workspace-audit-log` · author: Carl
- [2026-05-13 — P1-b RLS `auth.uid()` subquery hardening — 428 policies across all `public` schema tables replaced bare `auth.uid()` with `(SELECT auth.uid())`; 11 migrations applied via Lovable; P1-a eos_* tables already clean; `dd_meeting_type` straggler caught by final verification; `consult_*` tables excluded (P1-c still blocked)](audit/2026-05-13-p1b-rls-auth-uid-subquery.md) · tag: `audit-2026-05-13-p1b-rls-auth-uid-subquery` · author: Carl
- [2026-05-13 - enum-to-dd Phase 2 closure - `evidence_type`, `sch_booking_status`, and `srto_source_type` converted to `dd_` lookup-backed fields; `srto_corpus.source_type` now text + validated FK to `dd_srto_source`; `match_srto_chunks` text-typed; generated types and local build verified; legacy `srto_source_type` enum retained for rollback](audit/2026-05-13-enum-to-dd-phase-2.md) · tag: `audit-2026-05-13-enum-to-dd-phase-2` · author: Khian (Brian)
- [2026-05-13 — NEW-006 `audit_user_events.tenant_id` — nullable `tenant_id bigint` FK + composite index + tenant-admin SELECT policy added; table was 0 rows so no backfill; write path still pending; `actor_user_uuid` nullability noted for future callers](audit/2026-05-13-new-006-audit-user-events-tenant-id.md) · tag: `audit-2026-05-13-new-006-audit-user-events-tenant-id` · author: Carl
- [2026-05-13 — P1-a EOS SELECT policy deduplication — 20 EOS tables; 46 old SELECT policies dropped; 19 consolidated policies created across 3 phases; `eos_issues` deleted_at IS NULL now applied uniformly; client_viewer policies preserved as distinct on eos_issues/rocks/headlines; `can_access_qc()` confirmed canonical for qc satellite tables](audit/2026-05-13-p1a-eos-select-policy-dedup.md) · tag: `audit-2026-05-13-p1a-eos-select-policy-dedup` · author: Carl
- [2026-05-13 — NEW-002 `pdp_cycles` tenant-admin SELECT policy rekey — boolean `primary_contact = true` check replaced with `relationship_role IN ('primary_contact','secondary_contact')` + `access_scope = 'full'` guard; `secondary_contact` latent access gap closed; policy verified via live `pg_policy` query](audit/2026-05-13-new-002-pdp-cycles-tenant-admin-relationship-role.md) · tag: `audit-2026-05-13-new-002-pdp-cycles-tenant-admin-relationship-role` · author: Carl
- [2026-05-13 — BUG-005 accept-invitation access_scope verification — `accept_invitation_v2` CASE block correctly maps `academy_user` → `access_scope='academy_only'` + `tm_status='inactive'`; no `academy_user` invites sent in production yet; drift row is Khian's test account manually modified; `pg_temp` in search_path flagged for P1-e-ii](audit/2026-05-13-bug-005-accept-invitation-access-scope.md) · tag: `audit-2026-05-13-bug-005-accept-invitation-access-scope` · author: Carl
- [2026-05-13 — TICKET-003 `enrol_as_impersonator` tenant match — 3-arg signature adds `p_tenant_id`; five guards (two new: `target_user_not_in_tenant`, `existing_enrolment_different_tenant`); idempotency corrected for `UNIQUE (course_id, user_id)` constraint; legacy 2-arg overload dropped; `useEnrolCourse` passes `previewTenant.id` with three friendly error mappings](audit/2026-05-13-ticket-003-enrol-as-impersonator-tenant-match.md) · tag: `audit-2026-05-13-ticket-003-enrol-as-impersonator-tenant-match` · author: Carl
- [2026-05-13 — TICKET-008 RPC error UX — 10 hardcoded toast descriptions in `useTimeInbox.tsx` updated to `error.message || '...'`; bulk/snooze operations (7 new) caught in origin/main sweep; `extractEdgeError` confirmed out of scope for direct RPC callers](audit/2026-05-13-ticket-008-rpc-error-ux.md) · tag: `audit-2026-05-13-ticket-008-rpc-error-ux` · author: Carl
- [2026-05-12 — BUG-017 academy user data isolation — academy-only users were still treated as active tenant members through legacy membership data; Lovable shipped DB + code fixes, live drift count is now 0, academy-only tenant access now returns false while full-access contacts still work](audit/2026-05-12-bug-017-academy-user-data-isolation.md) · tag: `audit-2026-05-12-bug-017-academy-user-data-isolation` · author: Khian (Brian)
- [2026-05-12 — TICKET-007 acting-user picker auth gate — `list_acting_user_options` RPC gates Academy impersonation picker on `auth.users.email_confirmed_at`; `anon` EXECUTE revoked; FK violation on `academy_lesson_progress` closed; previous Lovable audit (wrong hook) caught and corrected before implementation](audit/2026-05-12-ticket-007-acting-user-picker-auth-gate.md) · tag: `audit-2026-05-12-ticket-007-acting-user-picker-auth-gate` · author: Carl
- [2026-05-12 — P1-e mutable search_path — 28 public functions missing SET search_path patched in single migration; match_srto_chunks gets public+extensions for pgvector; Lovable hallucination caught and corrected before implementation](audit/2026-05-12-p1e-mutable-search-path.md) · tag: `audit-2026-05-12-p1e-mutable-search-path` · author: Carl
- [2026-05-12 — contact-role canonicalisation — `relationship_role` made canonical on `tenant_users`; trigger rekeyed from `role='parent'` to RR-driven boolean sync; 42 drift rows backfilled; BUG-001 (bulk-send-invitations), BUG-004 (ClientTenantContext), send-email-graph, send-composed-email, SendPreliminarySummaryDialog all migrated from boolean to RR reads; 8/8 tests green](audit/2026-05-12-contact-role-canonicalisation.md) · tag: `audit-2026-05-12-contact-role-canonicalisation` · author: Carl
- [2026-05-12 - academy access routing RLS - SuperAdmin Academy admin links exposed; Academy-only users granted SELECT-only access to their own enabled tenant row so `AcademyAccessGate` can read `academy_access_enabled`; no edit/manage-user access granted](audit/2026-05-12-academy-access-routing-rls.md) · tag: `audit-2026-05-12-academy-access-routing-rls` · author: Khian (Brian)
- [2026-05-12 — pdp_cycles tenant admin RLS fix — `pdp_cycles: tenant admins view their tenant` policy checked `role IN ('admin','owner')` — values that do not exist; 0 rows ever matched; replaced with `canManagePortalUsers` logic (`access_scope='full'` + contact flag); 441 users gain intended SELECT access](audit/2026-05-12-pdp-cycles-tenant-admin-rls-fix.md) · tag: `audit-2026-05-12-pdp-cycles-tenant-admin-rls-fix` · author: Carl
- [2026-05-12 — pdp-export storage buckets — `doc-templates` (50 MB, staff-only CRUD) and `generated-docs` (10 MB, DOCX-only, owner+staff SELECT) created; 5 RLS policies added; path-segment join to pdp_cycles rejected in favour of signed-URL pattern](audit/2026-05-12-pdp-export-storage-buckets.md) · tag: `audit-2026-05-12-pdp-export-storage-buckets` · author: Carl
- [2026-05-12 — pdp_cycles RLS fix — no INSERT policy for non-staff users blocked createCycle(); UPDATE WITH CHECK prevented cycle closure; hardened INSERT (status whitelist + tenant membership) + UPDATE (under_review added to USING, completed_by guard) applied via single migration](audit/2026-05-12-pdp-cycles-rls-fix.md) · tag: `audit-2026-05-12-pdp-cycles-rls-fix` · author: Carl
- [2026-05-12 — Academy-only notification bug fix — `fn_tm_on_message_insert` lacked `access_scope` filter; 27 contaminated rows deleted; fix confirmed live via migration `20260511071820`; deprecated `messages` pipeline documented](audit/2026-05-12-academy-notification-bug-fix.md) · tag: `audit-2026-05-12-academy-notification-bug-fix` · author: Khian (Brian)
- [2026-05-11 — PDP views security_invoker fix — v_pdp_user_currency and v_pdp_cycle_summary ran as DB owner, bypassing RLS on all four underlying tables; security_invoker=true applied, verified live; manager policy gap flagged to Angela](audit/2026-05-11-pdp-views-security-invoker-fix.md) · tag: `audit-2026-05-11-pdp-views-security-invoker-fix` · author: Khian (Brian)
- [2026-05-11 — BUG-018 email delivery investigation — confirmed non-issue; Mailgun delivered successfully (2.0.0 OK), email landed in Gmail Promotions tab; same-browser session conflict during QA also a non-bug](audit/2026-05-11-bug-018-email-delivery-investigation.md) · tag: `audit-2026-05-11-bug-018-email-delivery-investigation` · author: Khian (Brian)
- [2026-05-11 — BUG-011/019 login routing timeout fix — staff and full-access users routed to Academy fallback; profile load race condition in useUserAccess.ts; single-line fix, commit 893fc01a](audit/2026-05-11-bug-011-019-login-routing-timeout.md) · tag: `audit-2026-05-11-bug-011-019-login-routing-timeout` · author: Khian (Brian)
- [2026-05-11 — BUG-002/003 secondary contact portal access fix — Invite button, checkboxes, bulk actions disabled; getTenantRole() replaced with canManagePortalUsers in ClientUsersPage + ClientTasksPage; BUG-020 follow-on parked](audit/2026-05-11-bug-002-003-secondary-contact-portal-access.md) · tag: `audit-2026-05-11-bug-002-003-secondary-contact-portal-access` · author: Khian (Brian)
- [2026-05-11 — Cron JWT rotation blocked — private schema + helper deployed; job command update blocked by supabase_read_only_user ownership; Supabase support ticket required](audit/2026-05-11-cron-jwt-rotation-blocked.md) · tag: `audit-2026-05-11-cron-jwt-rotation-blocked` · author: Carl
- [2026-05-11 — Angela sessions 7–10 May — Academy impersonation backend, support tickets system, security hardening (users_update_own escalation fix, storage lockdown, security_invoker), research findings RLS, academy routes restructure (249 commits, 14 migrations)](audit/2026-05-11-angela-sessions-may7-10-academy-support.md) · tag: `audit-2026-05-11-angela-sessions-may7-10-academy-support` · author: Carl
- [2026-05-08 — Staff directory RPC hardening — get_vivacity_team_directory + get_vivacity_team_directory_staff, useVivacityTeamUsers switched to RPC, 2 duplicate raw queries removed, email/job_title no longer returned to client callers](audit/2026-05-08-staff-directory-rpc-hardening.md) · tag: `audit-2026-05-08-staff-directory-rpc-hardening`
- [2026-05-08 — Messaging unread/read visibility + nav badge inflation + Direct label — staff/client unread row contrast, badge scoped to participant-only convos + realtime clear fix, "Message your CSC" subject replaced with "Direct message" in staff view](audit/2026-05-08-messaging-unread-badge-direct-label.md) · tag: `audit-2026-05-08-messaging-unread-badge-direct-label` · author: Khian (Brian)
- [2026-05-08 — Client packages action buttons fix — "Open Tasks" routed to staff route (→ homepage), "Messages CSC" triggered mailto popup; both fixed to correct client portal routes, dead code removed](audit/2026-05-08-client-packages-action-buttons-fix.md) · tag: `audit-2026-05-08-client-packages-action-buttons-fix` · author: Khian (Brian)
- [2026-05-08 — Bug 2 + Bug 12 — dedupe_key index cleanup (drop duplicate partial index, keep both arbiters) + Today's Focus smoke-test close](audit/2026-05-08-bug2-bug12-dedupe-index-cleanup.md) · tag: `audit-2026-05-08-bug2-bug12-dedupe-index-cleanup` · author: Khian (Brian)
- [2026-05-08 — Client URL access audit — full route inventory, 13 HIGH + 8 MEDIUM unguarded staff routes, staff member enumeration via useVivacityTeamUsers, Phase 2/3 plan](audit/2026-05-08-client-url-access-audit.md) · tag: `audit-2026-05-08-client-url-access-audit`
- [2026-05-08 — Client packages RLS perf fix — `security_invoker` timeout root cause, RLS semi-join rewrite, SECURITY DEFINER RPC `get_client_package_dashboard`, Academy lesson count fallback](audit/2026-05-08-rls-perf-client-package-dashboard.md) · tag: `audit-2026-05-08-rls-perf-client-package-dashboard`
- [2026-05-08 — Dashboard bugs 9/10/11 + Bug 7/8 smoke-test close — label text, View Tasks nav, 999-days v_dashboard_tenant_portfolio fix (391 tenants corrected)](audit/2026-05-08-dashboard-bugs-9-10-11-fix.md) · tag: `audit-2026-05-08-dashboard-bugs-9-10-11-fix` · author: Khian (Brian)
- [2026-05-07 — Bug 7 + Bug 8 — CSC realtime messages + client unread/bell clear (filtered subscription root cause, source_id backfill, regression fix)](audit/2026-05-07-bug7-bug8-realtime-messaging-client-unread.md) · tag: `audit-2026-05-07-bug7-bug8-realtime-messaging-client-unread` · author: Brian
- [2026-05-07 — Day 2 messaging unread badge — realtime bell, source_id trigger fix, CSC unread dot, bell-clears-on-open, client bell realtime, Bugs 3/4/5 resolved](audit/2026-05-07-day2-messaging-unread-badge.md) · tag: `audit-2026-05-07-day2-messaging-unread-badge` · author: Brian
- [2026-05-07 — Client home "Message" button routed to CSC tab — two `openHelpCenter("chatbot")` → `openHelpCenter("csc")` swaps in `ClientHomePage.tsx`](audit/2026-05-07-client-home-message-csc-tab-fix.md) · tag: `audit-2026-05-07-client-home-message-csc-tab-fix` · author: Khian (Brian)
- [2026-05-07 — Angela security hardening — auth_tokens, profiles, eos_workspaces, task-evidence, task-files, views security_invoker (7 fixes, 1 accepted legacy risk)](audit/2026-05-07-angela-security-hardening-rls-storage-views.md) · tag: `audit-2026-05-07-angela-security-hardening-rls-storage-views` · author: Khian (Brian)
- [2026-05-06 — CSC card "Not yet assigned" RLS fix — missing users_select_assigned_csc policy on users table](audit/2026-05-06-csc-card-not-yet-assigned-rls-fix.md) · tag: `audit-2026-05-06-csc-card-not-yet-assigned-rls-fix` · author: Khian (Brian)
- [2026-05-06 — Invite acceptance tenant_members fix — accept_invitation_v2 extended, 416-row backfill (83 active restored, 333 inactive)](audit/2026-05-06-invite-acceptance-tenant-members-fix.md) · tag: `audit-2026-05-06-invite-acceptance-tenant-members-fix` · author: Khian (Brian)
- [2026-05-06 — Messaging RLS cutover — tenant_messages wired, participant-scoped RLS, audit triggers, Help Center CSC unified, 15 live CI tests](audit/2026-05-06-messaging-rls-cutover.md) · tag: `audit-2026-05-06-messaging-rls-cutover`
- [2026-05-06 — Suggestion Register client portal — is_client_visible flag, 4 migrations, RLS hardening, notify edge function, client portal pages](audit/2026-05-06-suggest-register-client-portal.md) · tag: `audit-2026-05-06-suggest-register-client-portal`
- [2026-05-05 — Ask Viv client Gemini integration — replaced deterministic formatter with Gemini, fixed two silent tasks tenant_id bugs](audit/2026-05-05-ask-viv-client-gemini-integration.md) · tag: `audit-2026-05-05-ask-viv-client-gemini-integration`
- [2026-05-04 — Angela's client portal rebuild, package dashboard, invitations overhaul, RLS hardening (296 commits, 23 migrations)](audit/2026-05-04-angela-client-portal-packages-invitations.md) · tag: `audit-2026-05-04-angela-client-portal-packages-invitations`

### April

- [2026-04-30 — Ask Viv client mode — edge function + frontend build (Prompts 2–5 of build-ask-viv-client-mode-v1)](audit/2026-04-30-ask-viv-client-mode-build.md) · tag: `audit-2026-04-30-ask-viv-client-mode-build`
- [2026-04-30 — Ask Viv client mode — ai_client_query_usage migration (Prompt 1 of build-ask-viv-client-mode-v1)](audit/2026-04-30-ask-viv-client-mode-migration.md) · tag: `audit-2026-04-30-ask-viv-client-mode-migration`
- [2026-04-30 — Angela's AI Executive Summary Drafter & multi-framework corpus (26 commits, 2 migrations, 2 edge functions + patches)](audit/2026-04-30-angela-ai-executive-summary-multi-framework-corpus.md) · tag: `audit-2026-04-30-angela-ai-executive-summary-multi-framework-corpus`
- [2026-04-30 — Angela's AI RAG corpus, Finding Drafter & Evidence Analyser (26 commits, 4 migrations, 5 edge functions)](audit/2026-04-30-angela-ai-rag-finding-drafter-evidence-analyser.md) · tag: `audit-2026-04-30-angela-ai-rag-finding-drafter-evidence-analyser`
- [2026-04-29 — Evidence files open/view — RLS verified, Lovable prompt issued](audit/2026-04-29-audit-documents-open-view.md) · tag: `audit-2026-04-29-audit-documents-open-view`
- [2026-04-29 — audit_type full session: constraint fix, filter dropdown, edge function labels, dd_audit_type conversion (Dave direction)](audit/2026-04-29-audit-type-fixes-and-dd-conversion.md) · tag: `audit-2026-04-29-audit-type-fixes-and-dd-conversion` · author: Khian (Brian)
- [2026-04-29 — Ask Viv full restoration (V4 compliance-assistant — 3 bugs fixed, 5 prompts verified)](audit/2026-04-29-ask-viv-restoration.md) · tag: `audit-2026-04-29-ask-viv-restoration`
- [2026-04-29 — Audit workspace sidebar UX fixes (dead `isSelected` prop + false sidebar highlight)](audit/2026-04-29-audit-workspace-sidebar-ux-fixes.md) · tag: `audit-2026-04-29-audit-workspace-sidebar-ux-fixes`
- [2026-04-29 — `client_audits.audit_type` CHECK constraint gap (`due_diligence_combined` missing)](audit/2026-04-29-client-audits-audit-type-constraint.md) · tag: `audit-2026-04-29-client-audits-audit-type-constraint`
- [2026-04-28 — AcceptInvitation blank screen fix + platform-wide anti-pattern sweep](audit/2026-04-28-accept-invitation-blank-screen-fix.md) · tag: `audit-2026-04-28-accept-invitation-blank-screen-fix`
- [2026-04-28 — ManageTenants performance investigation & React Query migration](audit/2026-04-28-manage-tenants-perf-optimisation.md) · tag: `audit-2026-04-28-manage-tenants-perf-optimisation`
- [2026-04-28 — Staff UUID relink fix (infinite spinner, 108 FK CASCADE)](audit/2026-04-28-staff-uuid-relink-fix.md) · tag: `audit-2026-04-28-staff-uuid-relink-fix`
- [2026-04-28 — Clean Architecture proposal review + audit-repo reconciliation](audit/2026-04-28-clean-arch-proposal-and-audit-reality.md) · tag: `audit-2026-04-28`
- [2026-04-27 — Codebase week review (2026-04-20 → 2026-04-25)](audit/2026-04-27-codebase-week-review.md) · tag: `audit-2026-04-27-codebase-week-review`
- [2026-04-24 — KB restructure](audit/2026-04-24-kb-restructure.md) · tag: `audit-2026-04-24`
